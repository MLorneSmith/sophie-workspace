/**
 * Mock for the "server-only" package
 * This is a Next.js specific import that doesn't exist as a real file
 * We provide an empty module export to satisfy the import during tests
 */
export {};
