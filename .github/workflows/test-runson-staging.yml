name: Test RunsOn Staging

on:
  workflow_dispatch:

jobs:
  test-runner:
    name: Test RunsOn Runner
    runs-on: runs-on=${{ github.run_id }}/runner=2cpu-linux-x64
    steps:
      - name: Test step
        run: |
          echo "Testing RunsOn runner on staging branch"
          echo "Runner: $(hostname)"
          echo "Success!"