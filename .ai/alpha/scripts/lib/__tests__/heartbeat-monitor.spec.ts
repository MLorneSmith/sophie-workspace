// Dead code tests removed in #1955 (heartbeat-monitor.ts was never integrated)
import { describe, it } from "vitest";
describe("heartbeat-monitor (removed)", () => {
	it.skip("module deleted in #1955", () => {});
});
