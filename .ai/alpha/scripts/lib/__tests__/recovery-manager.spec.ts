// Dead code tests removed in #1955 (recovery-manager.ts was never integrated)
import { describe, it } from "vitest";
describe("recovery-manager (removed)", () => {
	it.skip("module deleted in #1955", () => {});
});
