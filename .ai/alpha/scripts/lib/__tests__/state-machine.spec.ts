// Dead code tests removed in #1955 (state-machine.ts replaced by feature-transitions.ts)
import { describe, it } from "vitest";
describe("state-machine (removed)", () => {
	it.skip("module replaced by feature-transitions.ts in #1955", () => {});
});
