{
  "data": {
    "library": "/e2b-dev/e2b",
    "version": "latest",
    "topic": "javascript typescript background process timeout long running",
    "tokens": 2907,
    "content": "### Run Command in Background (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.6/commands/page.mdx\n\nStarts a new command in the sandbox and runs it in the background. It returns a `CommandHandle` that can be used to monitor the command's progress or retrieve its results later using `CommandHandle.wait`.\n\n```typescript\nrun(cmd: string, opts?: CommandStartOpts & object): Promise<CommandHandle>\n```\n\n--------------------------------\n\n### Command Start Options - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.1/commands/page.mdx\n\nDefines options for starting a new command within the E2B environment. This includes settings for background execution, working directory, environment variables, stdout/stderr callbacks, request timeout, command timeout, and the user to run the command as.\n\n```ts\noptional background: boolean;\n\noptional cwd: string;\n\noptional envs: Record<string, string>;\n\noptional onStderr: (data: string) => void | Promise<void>;\n\noptional onStdout: (data: string) => void | Promise<void>;\n\noptional requestTimeoutMs: number;\n\noptional timeoutMs: number;\n\noptional user: Username;\n```\n\n--------------------------------\n\n### Command Start Options in TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.5/commands/page.mdx\n\nSpecifies options for initiating a new command. This includes settings for background execution, working directory, environment variables, stdout/stderr callbacks, request timeouts, command timeouts, and the user to run the command as. Defaults are provided for several options.\n\n```typescript\noptional background: boolean;\n```\n\n```typescript\noptional cwd: string;\n// home directory of the user used to start the command\n```\n\n```typescript\noptional envs: Record<string, string>;\n// overrides the default environment variables from Sandbox constructor.\n// Default: {}\n```\n\n```typescript\noptional onStderr: (data: string) => void | Promise<void>;\n```\n\n```typescript\noptional onStdout: (data: string) => void | Promise<void>;\n```\n\n```typescript\noptional requestTimeoutMs: number;\n// Default: 30_000 // 30 seconds\n```\n\n```typescript\noptional timeoutMs: number;\n// Default: 60_000 // 60 seconds\n```\n\n```typescript\noptional user: Username;\n// Default: user\n```\n\n--------------------------------\n\n### TypeScript: Run Command in Background\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.10.0/commands/page.mdx\n\nStarts a new command in the background, returning a CommandHandle for later interaction. Use CommandHandle.wait to retrieve the final result. Takes the command string and optional start options.\n\n```typescript\nrun(cmd: string, opts: CommandStartOpts & object): Promise<CommandHandle>\n```\n\n--------------------------------\n\n### Define TimeoutError in TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.5.0/errors/page.mdx\n\nThrown when a timeout occurs during sandbox operations or request processing. This error inherits from SandboxError and specifies different causes for timeouts.\n\n```typescript\nclass TimeoutError extends SandboxError {\n  message: string;\n\n  constructor(message: string) {\n    super(message);\n    this.message = message;\n  }\n}\n```\n\n--------------------------------\n\n### Request Timeout Option - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.5.1/commands/page.mdx\n\nDefines an optional request timeout in milliseconds for API requests. This allows for specifying how long to wait for a response before giving up. The default timeout is 30 seconds.\n\n```typescript\noptional requestTimeoutMs: number;\n\n/**\n * Timeout for requests to the API in **milliseconds**.\n * @default 30_000 // 30 seconds\n */\n```\n\n--------------------------------\n\n### Kill Sandbox Instance (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.10.0/sandbox/page.mdx\n\nTerminates the running sandbox environment. This action is irreversible and stops all processes within the sandbox. Optional request timeout can be specified.\n\n```typescript\nawait sandbox.kill();\n```\n\n--------------------------------\n\n### CommandStartOpts Interface - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.6/commands/page.mdx\n\nThe `CommandStartOpts` interface defines the configuration options for starting a new command. It includes settings for background execution, working directory, environment variables, callbacks for stdout/stderr, request timeouts, command timeouts, and the user to run the command as. Some properties have default values.\n\n```typescript\ninterface CommandStartOpts {\n  background?: boolean;\n  cwd?: string;\n  envs?: Record<string, string>;\n  onStderr?: (data: string) => void | Promise<void>;\n  onStdout?: (data: string) => void | Promise<void>;\n  requestTimeoutMs?: number;\n  timeoutMs?: number;\n  user?: Username;\n}\n```\n\n--------------------------------\n\n### Start Command Options - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.5/commands/page.mdx\n\nSpecifies options for starting a new command within the sandbox. This includes settings for background execution, working directory, environment variables, standard error/output callbacks, request timeouts, command-specific timeouts, and the user to run the command as. Defaults are provided for most options.\n\n```typescript\noptional background: boolean;\noptional cwd: string;\noptional envs: Record<string, string>;\noptional onStderr: (data: string) => void | Promise<void>;\noptional onStdout: (data: string) => void | Promise<void>;\noptional requestTimeoutMs: number;\noptional timeoutMs: number;\noptional user: Username;\n```\n\n--------------------------------\n\n### Configure Command Start Options in TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.3/commands/page.mdx\n\nDefines the options for starting a new command in the E2B environment. This includes settings for running commands in the background, specifying the working directory (cwd), environment variables (envs), and callbacks for standard error (onStderr) and standard output (onStdout). It also supports request and command timeouts, and specifying the user to run the command as.\n\n```ts\n{\n  background?: boolean;\n  cwd?: string;\n  envs?: Record<string, string>;\n  onStderr?: (data: string) => void | Promise<void>;\n  onStdout?: (data: string) => void | Promise<void>;\n  requestTimeoutMs?: number;\n  timeoutMs?: number;\n  user?: Username;\n}\n```\n\n--------------------------------\n\n### Start Command Options - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.1.1/commands/page.mdx\n\nSpecifies options for starting a new command within the sandbox environment. This includes configuration for running commands in the background, setting the working directory, defining environment variables, and providing callbacks for standard output and error streams. It also includes options for request and command timeouts, and the user to run the command as.\n\n```typescript\nbackground?: boolean;\ncwd?: string;\nenvs?: Record<string, string>;\nonStderr?: (data: string) => void | Promise<void>;\nonStdout?: (data: string) => void | Promise<void>;\nrequestTimeoutMs?: number;\ntimeoutMs?: number;\nuser?: Username;\n```\n\n--------------------------------\n\n### Command Timeout Option - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.0/commands/page.mdx\n\nDefines an optional timeout in milliseconds for a command's execution. This allows setting a maximum duration for a command to run before it is terminated. The default value is 60,000 milliseconds (60 seconds).\n\n```typescript\ntimeoutMs?: number;\n// Default: 60_000 // 60 seconds\n```\n\n--------------------------------\n\n### Start Command Options - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.5.1/commands/page.mdx\n\nSpecifies options for starting a new command within the sandbox. This includes options for running in the background, setting the working directory, defining environment variables, and providing callbacks for standard error and standard output. It also includes request and command-specific timeouts.\n\n```typescript\n/**\n * Options for starting a new command.\n */\ninterface CommandStartOpts {\n  /**\n   * If true, starts command in the background and the method returns immediately.\n   * You can use CommandHandle.wait to wait for the command to finish.\n   */\n  background?: boolean;\n\n  /**\n   * Working directory for the command.\n   * @default // home directory of the user used to start the command\n   */\n  cwd?: string;\n\n  /**\n   * Environment variables used for the command.\n   * This overrides the default environment variables from `Sandbox` constructor.\n   * @default {}\n   */\n  envs?: Record<string, string>;\n\n  /**\n   * Callback for command stderr output.\n   */\n  onStderr?: (data: string) => void | Promise<void>;\n\n  /**\n   * Callback for command stdout output.\n   */\n  onStdout?: (data: string) => void | Promise<void>;\n\n  /**\n   * Timeout for requests to the API in **milliseconds**.\n   * @default 30_000 // 30 seconds\n   */\n  requestTimeoutMs?: number;\n\n  /**\n   * Timeout for the command in **milliseconds**.\n   * @default 60_000 // 60 seconds\n   */\n  timeoutMs?: number;\n\n  /**\n   * User to run the command as.\n   * @default user\n   */\n  user?: Username;\n}\n```\n\n--------------------------------\n\n### TypeScript Optional Timeout Configuration\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.11.0/filesystem/page.mdx\n\nDefines an optional timeout in milliseconds for watch operations. A value of 0 disables the timeout. The default timeout is 60 seconds.\n\n```typescript\noptional timeoutMs: number;\n\n// Default: 60_000 // 60 seconds\n```\n\n--------------------------------\n\n### TypeScript Optional Timeout Configuration\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.0.2/filesystem/page.mdx\n\nDefines an optional timeout in milliseconds for watch operations. A value of 0 disables the timeout. The default timeout is 60,000 milliseconds (60 seconds).\n\n```typescript\noptional timeoutMs: number;\n\n// Default value: 60_000 // 60 seconds\n```\n\n--------------------------------\n\n### Request Timeout Option - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.0/commands/page.mdx\n\nDefines an optional request timeout in milliseconds for API requests. This setting allows specifying how long to wait for a response from the API before timing out. The default value is 30,000 milliseconds (30 seconds).\n\n```typescript\nrequestTimeoutMs?: number;\n// Default: 30_000 // 30 seconds\n```\n\n--------------------------------\n\n### Start Command - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.4/commands/page.mdx\n\nInitiates a new command execution within the sandbox. This function allows configuration of various aspects such as running in the background, specifying the working directory, setting environment variables, and defining callbacks for standard output and error streams. It also supports request and command timeouts, and specifies the user to run the command as. Returns a CommandHandle for managing the started command.\n\n```typescript\nstartCommand(command: string,\n   args?: string[] | undefined,\n   opts?: CommandStartOpts): Promise<CommandHandle>\n```\n\n--------------------------------\n\n### Set Sandbox Timeout using TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.11.0/sandbox/page.mdx\n\nUpdate the timeout for a specified sandbox. The sandbox will be automatically killed once the timeout expires. This method can adjust the existing timeout and returns a void promise.\n\n```typescript\nstatic setTimeout(\n   sandboxId: string,\n   timeoutMs: number,\n   opts?: SandboxApiOpts): Promise<void>\n```\n\n--------------------------------\n\n### TypeScript: TimeoutError Constructor\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.5/errors/page.mdx\n\nRepresents various timeout scenarios, including sandbox timeouts, request timeouts, and command execution timeouts. It takes a string message.\n\n```typescript\nnew TimeoutError(message: string): TimeoutError\n```\n\n--------------------------------\n\n### Run Command in Background (E2B Sandbox)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.0.1/sandbox_async/page.mdx\n\nStarts a command in the E2B sandbox to run in the background, returning an AsyncCommandHandle for interaction. This is ideal for long-running processes that do not require immediate output. The handle allows for subsequent interaction, such as waiting for completion or retrieving output. Supports environment variables, user, working directory, stdout/stderr callbacks, and timeouts.\n\n```python\nfrom typing import Optional, Dict, Union, Literal\nfrom e2b import Username, OutputHandler, Stdout, Stderr, AsyncCommandHandle\n\n# Assuming 'sandbox' is an initialized E2B sandbox client\nsandbox.commands.run(cmd: str,\n                   background: Literal[True],\n                   envs: Optional[Dict[str, str]] = None,\n                   user: Username = \"user\",\n                   cwd: Optional[str] = None,\n                   on_stdout: Optional[OutputHandler[Stdout]] = None,\n                   on_stderr: Optional[OutputHandler[Stderr]] = None,\n                   timeout: Optional[float] = 60,\n                   request_timeout: Optional[float] = None) -> AsyncCommandHandle\n```\n\n--------------------------------\n\n### Define TimeoutError in TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.7/errors/page.mdx\n\nThrown when a timeout error occurs during sandbox operations or requests. This error class distinguishes between different timeout causes like sandbox timeouts, request timeouts, and command execution timeouts. It takes a string message.\n\n```typescript\nclass TimeoutError extends Error {\n  constructor(message: string) {\n    super(message);\n    this.name = \"TimeoutError\";\n  }\n}\n```\n\n--------------------------------\n\n### TypeScript Timeout Configuration for Watch Operation\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.13.0/filesystem/page.mdx\n\nDefines an optional timeout in milliseconds for a watch operation. A value of 0 disables the timeout. The default timeout is 60,000 milliseconds (60 seconds).\n\n```typescript\noptional timeoutMs: number;\n\n// Default\n60_000 // 60 seconds\n```\n\n--------------------------------\n\n### Kill an E2B Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.1/sandbox/page.mdx\n\nTerminates the running sandbox environment. This action stops all processes within the sandbox and releases its resources. Optional connection options can be provided, such as a request timeout for the kill operation.\n\n```typescript\nconst sandbox = await Sandbox.create()\nawait sandbox.kill()\n```\n\n--------------------------------\n\n### Kill Sandbox Process - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.6/sandbox/page.mdx\n\nTerminates the running sandbox environment. This action stops all processes within the sandbox and releases associated resources. Use with caution as it's a destructive operation.\n\n```typescript\nawait sandbox.kill()\n```\n\n--------------------------------\n\n### Define TimeoutError in TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.3.0/errors/page.mdx\n\nDefines the TimeoutError class, covering various timeout scenarios including sandbox timeouts, request timeouts, and command execution timeouts. It accepts a string message.\n\n```typescript\nclass TimeoutError {\n  constructor(message: string): TimeoutError\n}\n```\n\n--------------------------------\n\n### Python: Connect to a Running Command\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.0.2/sandbox_sync/page.mdx\n\nEstablishes a connection to an already running command identified by its process ID (PID). This allows for interaction with background processes. The function returns a `CommandHandle` object, which can be used to wait for the command's completion or access its output. Connection timeouts and request timeouts can be configured.\n\n```python\nfrom typing import Optional\nfrom e2b_dev.sandbox.command import CommandHandle\n\n# Assuming 'sandbox' is an initialized object with the commands module\nsandbox.commands.connect(pid: int,\n                        timeout: Optional[float] = 60,\n                        request_timeout: Optional[float] = None) -> CommandHandle\n```\n\n--------------------------------\n\n### Run TypeScript code with Axios in E2B\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/code-interpreting/supported-languages/javascript/page.mdx\n\nThis snippet demonstrates how to run TypeScript code that utilizes the `axios` library to fetch data from an API. It shows the process of creating a sandbox, installing external packages like `axios` using `npm install`, and then executing the TypeScript code. The example includes handling of top-level await and ESM-style imports.\n\n```javascript\nimport { Sandbox } from \"@e2b/code-interpreter\";\n\n// Create a new sandbox\nconst sbx = await Sandbox.create();\n\n// Install the axios package\nawait sbx.commands.run(\"npm install axios\");\n\n// Run the code\nconst execution = await sbx.runCode(`\n  import axios from \"axios\";\n\n  const url: string = \"https://api.github.com/status\";\n  const response = await axios.get(url);\n  response.data;\n`,\n  { language: \"ts\" }\n);\n\nconsole.log(execution);\n```\n\n```python\nfrom e2b_code_interpreter import Sandbox\n\n# Create a new sandbox\nsbx = Sandbox.create()\n\n# Install the axios package\nsbx.commands.run(\"npm install axios\")\n\n#  Run the code\nexecution = sbx.run_code(\"\"\"\n  import axios from \"axios\";\n\n  const url: string = \"https://api.github.com/status\";\n  const response = await axios.get(url);\n  response.data;\n\"\"\",\n    language=\"ts\",\n)\n\nprint(execution)\n```\n\n--------------------------------\n\n### Set Command Timeout - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.7/commands/page.mdx\n\nDefines an optional timeout in milliseconds for a command to complete. The default timeout is 60,000 milliseconds (60 seconds).\n\n```typescript\noptional timeoutMs: number;\n```\n\n--------------------------------\n\n### Start Command in Background - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.7/commands/page.mdx\n\nSpecifies whether a command should start in the background. If true, the method returns immediately, and the command's completion can be awaited using CommandHandle.wait.\n\n```typescript\noptional background: boolean;\n```\n\n--------------------------------\n\n### Send Input to PTY Process using TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.11.0/commands/page.mdx\n\nSends data as input to a pseudo-terminal (PTY) process. The input is provided as a Uint8Array. This function is useful for interacting with running processes in the terminal. Optional connection options can be provided.\n\n```typescript\nsendInput(pid: number, data: Uint8Array, opts?: Pick<ConnectionOpts, \"requestTimeoutMs\">): Promise<void>\n```\n\n--------------------------------\n\n### TypeScript: Request Timeout Configuration\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.0.0/commands/page.mdx\n\nDefines a configurable timeout for API requests in milliseconds. This option can be applied to various operations to prevent indefinite waiting. The default timeout is 60 seconds.\n\n```typescript\noptional requestTimeoutMs: number;\n\n// Default:\n60_000 // 60 seconds\n```\n\n--------------------------------\n\n### Run Background Command Handle with Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.1.2/sandbox_async/page.mdx\n\nInitiates a command execution in the background within the sandbox. Returns an AsyncCommandHandle for managing the running process. Supports environment variables, user, working directory, stdout/stderr callbacks, and command/request timeouts.\n\n```python\n@overload\nasync def run(self, cmd: str,\n              background: Literal[True],\n              envs: Optional[Dict[str, str]] = None,\n              user: Username = \"user\",\n              cwd: Optional[str] = None,\n              on_stdout: Optional[OutputHandler[Stdout]] = None,\n              on_stderr: Optional[OutputHandler[Stderr]] = None,\n              timeout: Optional[float] = 60,\n              request_timeout: Optional[float] = None) -> AsyncCommandHandle:\n```\n\n--------------------------------\n\n### Connect to Running Command (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.6/commands/page.mdx\n\nConnects to a command that is already running in the sandbox, identified by its process ID. It returns a CommandHandle for further interaction, such as waiting for completion using `CommandHandle.wait`.\n\n```typescript\nconnect(pid: number, opts?: CommandConnectOpts): Promise<CommandHandle>\n```\n\n--------------------------------\n\n### Run commands in background (JavaScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/commands/background/page.mdx\n\nExecute a command in the background using the `commands.run()` method with the `background: true` option. The command continues to run after the method returns. It can be terminated later using `command.kill()`. The `onStdout` callback receives data as it's produced.\n\n```javascript\nimport { Sandbox } from '@e2b/code-interpreter'\n\nconst sandbox = await Sandbox.create()\n\n// Start the command in the background\nconst command = await sandbox.commands.run('echo hello; sleep 10; echo world', {\n  background: true, // $HighlightLine\n  onStdout: (data) => {\n    console.log(data)\n  },\n})\n\n// Kill the command\nawait command.kill()\n```\n\n--------------------------------\n\n### Default Sandbox Timeout in TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.1/sandbox/page.mdx\n\nSets the default value for the sandbox timeout in milliseconds. The default is 300,000 milliseconds, equivalent to 5 minutes. This value is subject to platform-specific maximum runtime limits.\n\n```typescript\n300_000 // 5 minutes\n```\n\n--------------------------------\n\n### Run Command (Background) in Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.2.0/sandbox_async/page.mdx\n\nStarts a new command in the sandbox and returns a handle for interaction. This is used for commands that should run in the background. It accepts the command string, environment variables, user, working directory, output handlers, and timeouts. It returns an AsyncCommandHandle to manage the background process.\n\n```python\nawait sandbox.commands.run(cmd: str,\n              background: Literal[True],\n              envs: Optional[Dict[str, str]] = None,\n              user: Username = \"user\",\n              cwd: Optional[str] = None,\n              on_stdout: Optional[OutputHandler[Stdout]] = None,\n              on_stderr: Optional[OutputHandler[Stderr]] = None,\n              timeout: Optional[float] = 60,\n              request_timeout: Optional[float] = None) -> AsyncCommandHandle\n```\n\n--------------------------------\n\n### TypeScript: Run Command (Flexible Mode)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.10.0/commands/page.mdx\n\nStarts a new command with flexible options. If opts.background is true, returns a CommandHandle for background execution. Otherwise, waits for completion and returns a CommandResult.\n\n```typescript\nrun(cmd: string, opts?: CommandStartOpts & object): Promise<CommandResult | CommandHandle>\n```\n\n--------------------------------\n\n### Run Command in Background Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.11.0/sandbox_sync/page.mdx\n\nStarts a command in the background and returns a handle to interact with it. Supports environment variables, user, working directory, and timeouts. Ideal for long-running processes.\n\n```python\n@overload\ndef run(cmd: str,\n        background: Literal[True],\n        envs: Optional[Dict[str, str]] = None,\n        user: Username = \"user\",\n        cwd: Optional[str] = None,\n        on_stdout: None = None,\n        on_stderr: None = None,\n        timeout: Optional[float] = 60,\n        request_timeout: Optional[float] = None) -> CommandHandle:\n```\n\n--------------------------------\n\n### Sandbox Timeout Configuration in TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.9.1/sandbox/page.mdx\n\nSets an optional timeout for the sandbox in milliseconds. There are limits based on user tiers: 24 hours for Pro users and 1 hour for Hobby users. Defaults to 300,000 milliseconds (5 minutes).\n\n```typescript\noptional timeoutMs: number;\n\n// Default\n300_000 // 5 minutes\n```\n\n--------------------------------\n\n### TypeScript Optional Timeout Configuration\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.1.3/filesystem/page.mdx\n\nDefines an optional timeout in milliseconds for watch operations. A value of 0 disables the timeout. The default timeout is 60,000 milliseconds (60 seconds).\n\n```typescript\noptional timeoutMs: number;\n\n// Default value:\n60_000 // 60 seconds\n```\n\n--------------------------------\n\n### Python: Run Command in Background\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.0.2/sandbox_sync/page.mdx\n\nStarts a command in the background within the sandbox environment. This overload of the `run` function returns a `CommandHandle` object immediately, allowing asynchronous interaction with the running process. Environment variables, user, and working directory can be configured. Timeouts for the command connection and the request can also be specified.\n\n```python\nfrom typing import Optional, Dict, Literal\nfrom e2b_dev.sandbox.command import CommandHandle, Username\n\n# Assuming 'sandbox' is an initialized object with the commands module\nsandbox.commands.run(cmd: str,\n                    background: Literal[True],\n                    envs: Optional[Dict[str, str]] = None,\n                    user: Username = \"user\",\n                    cwd: Optional[str] = None,\n                    on_stdout: None = None,\n                    on_stderr: None = None,\n                    timeout: Optional[float] = 60,\n                    request_timeout: Optional[float] = None) -> CommandHandle\n```\n\n--------------------------------\n\n### Kill Running Command (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.6/commands/page.mdx\n\nTerminates a running command in the sandbox using its process ID. This method forcibly kills the command using the `SIGKILL` signal. It returns a boolean indicating success or failure.\n\n```typescript\nkill(pid: number, opts?: CommandRequestOpts): Promise<boolean>\n```",
    "chunks": null,
    "metadata": {}
  },
  "cached_at": 1767726974.9426033,
  "params": {
    "owner": "e2b-dev",
    "repo": "e2b",
    "version": "latest",
    "topic": "javascript typescript background process timeout long running",
    "tokens": 5000,
    "format": "txt"
  }
}