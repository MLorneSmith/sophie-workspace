{
  "data": {
    "library": "/e2b-dev/e2b",
    "version": "latest",
    "topic": "sandbox connect keep alive websocket reconnect",
    "tokens": 2193,
    "content": "### Connect to Sandbox (Instance) - Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.1.4/sandbox_async/page.mdx\n\nConnects to an existing sandbox instance. If the sandbox is paused, it will be automatically resumed. This method is useful for reconnecting to the same sandbox from different environments. A timeout can be specified in seconds.\n\n```python\nawait sandbox.connect(timeout: Optional[int] = None, **opts: Unpack[ApiParams])\n```\n\n--------------------------------\n\n### Connect to an existing E2B Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.1.0/sandbox_sync/page.mdx\n\nEstablishes a connection to an E2B sandbox, automatically resuming it if it's paused. This method can be used with an existing sandbox instance or by providing a sandbox ID. It's useful for reconnecting to a sandbox from different locations or environments.\n\n```python\nfrom e2b import Sandbox\n\n# Connect to a paused sandbox instance\nsandbox = Sandbox.create()\nsandbox.beta_pause()\n\nsame_sandbox = sandbox.connect()\nprint(f\"Connected to sandbox: {same_sandbox.sandbox_id}\")\n\n# Connect using sandbox ID\nsandbox_id_to_connect = same_sandbox.sandbox_id\n\n# First, pause the sandbox using its ID (if not already paused)\n# Sandbox.beta_pause(sandbox_id_to_connect) # Uncomment if needed\n\nconnected_by_id = Sandbox.connect(sandbox_id_to_connect)\nprint(f\"Connected to sandbox by ID: {connected_by_id.sandbox_id}\")\n```\n\n--------------------------------\n\n### Connect to an Existing E2B Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.1.1/sandbox_sync/page.mdx\n\nConnects to an existing E2B sandbox, resuming it if it's paused. This allows for reconnecting to a sandbox from different locations or environments using its ID.\n\n```python\nfrom e2b import Sandbox\n\n# Create a sandbox first to get its ID\nsandbox = Sandbox.create()\nsandbox_id = sandbox.sandbox_id\nprint(f\"Created sandbox with ID: {sandbox_id}\")\n\n# Pause the sandbox (optional, to demonstrate resume)\nsandbox.beta_pause()\n\n# Connect to the sandbox using its ID\nconnected_sandbox = Sandbox.connect(sandbox_id=sandbox_id, timeout=60)\nprint(\"Successfully connected to the sandbox.\")\n\n# You can also connect to a sandbox instance directly if it's in the same process\n# This requires the sandbox to be running or paused\n# same_sandbox_instance = sandbox.connect()\n\n```\n\n--------------------------------\n\n### Connect to an Existing Sandbox with AsyncSandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.3.0/sandbox_async/page.mdx\n\nEstablishes a connection to a pre-existing sandbox using its unique identifier. Allows specifying API key, domain, and debug mode for the connection. This is useful for reconnecting to a sandbox from different locations or services.\n\n```python\nsandbox_id = 'some-sandbox-id'\nsame_sandbox = await AsyncSandbox.connect(\n    sandbox_id=sandbox_id,\n    api_key='YOUR_API_KEY',\n    domain='e2b.dev',\n    debug=False\n)\n```\n\n--------------------------------\n\n### Connect to Existing Sandbox (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.8.0/sandbox_async/page.mdx\n\nAsynchronously connects to an existing sandbox using its ID. Allows reconnection from different environments. Requires the sandbox ID and optionally accepts API key and proxy configurations. Returns a sandbox instance.\n\n```python\n@classmethod\nasync def connect(cls, \n                  sandbox_id: str,\n                  api_key: Optional[str] = None,\n                  domain: Optional[str] = None,\n                  debug: Optional[bool] = None,\n                  proxy: Optional[ProxyTypes] = None) -> 'AsyncSandbox':\n    # Connect to an existing sandbox.\n    # Arguments:\n    #   sandbox_id: Sandbox ID\n    #   api_key: E2B API Key, defaults to E2B_API_KEY env var\n    #   proxy: Proxy to use for the request and sandbox\n    # Returns:\n    #   sandbox instance for the existing sandbox\n    pass\n\n# Example Usage:\nsandbox = await AsyncSandbox.create()\nsandbox_id = sandbox.sandbox_id\nsame_sandbox = await AsyncSandbox.connect(sandbox_id)\n```\n\n--------------------------------\n\n### Connect to Existing Sandbox (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.1.0/sandbox/page.mdx\n\nConnects to an existing sandbox using its ID. If the sandbox is paused, it will be automatically resumed. This function allows multiple connections to the same sandbox. Requires the sandbox to be running or paused.\n\n```typescript\nstatic connect<S>(\n   this: S, \n   sandboxId: string, \nopts?: SandboxConnectOpts): Promise<InstanceType<S>>\n```\n\n--------------------------------\n\n### Connect to Existing Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.6.0/sandbox_sync/page.mdx\n\nEstablishes a connection to a pre-existing sandbox using its unique identifier. Allows for reconnecting to the same sandbox from different environments. Supports API key, domain, debug mode, and proxy configuration.\n\n```python\nfrom typing import Optional\nfrom e2b import ProxyTypes\n\n@classmethod\ndef connect(cls, sandbox_id: str, api_key: Optional[str] = None, domain: Optional[str] = None, debug: Optional[bool] = None, proxy: Optional[ProxyTypes] = None):\n    # Implementation details for connecting to an existing sandbox\n    pass\n\n# Example Usage:\nsandbox = Sandbox()\nsandbox_id = sandbox.sandbox_id\nsame_sandbox = Sandbox.connect(sandbox_id)\nprint(f\"Successfully connected to sandbox: {same_sandbox.sandbox_id}\")\n```\n\n--------------------------------\n\n### Connect to Sandbox (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.0.2/sandbox/page.mdx\n\nConnects to an existing sandbox using its ID. If the sandbox is paused, it will be automatically resumed. The sandbox must be either running or paused to connect. This allows multiple connections to the same sandbox.\n\n```typescript\nstatic connect<S>(this: S, sandboxId: string, opts?: SandboxConnectOpts): Promise<InstanceType<S>>\n```\n\n```typescript\nconst sandbox = await Sandbox.create()\nconst sandboxId = sandbox.sandboxId\n\n// Connect to the same sandbox.\nconst sameSandbox = await Sandbox.connect(sandboxId)\n```\n\n--------------------------------\n\n### Connect to Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.1.3/sandbox_async/page.mdx\n\nConnects to an existing sandbox. If the sandbox is paused, it will be automatically resumed. This method allows connecting to the same sandbox from multiple locations. A timeout can be specified in seconds. The sandbox must be in a running or paused state.\n\n```python\n@overload\nasync def connect(timeout: Optional[int] = None,\n                  **opts: Unpack[ApiParams]) -> Self\n```\n\n```python\n@overload\n@classmethod\nasync def connect(cls,\n                  sandbox_id: str,\n                  timeout: Optional[int] = None,\n                  **opts: Unpack[ApiParams]) -> Self\n```\n\n```python\n@class_method_variant(\"_cls_connect\")\nasync def connect(timeout: Optional[int] = None,\n                  **opts: Unpack[ApiParams]) -> Self\n```\n\n--------------------------------\n\n### Connect to an existing E2B Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.1.4/sandbox_sync/page.mdx\n\nConnects to an existing sandbox, resuming it if it's paused. You can connect either to a sandbox instance directly or by providing its ID. The sandbox must be running or paused. A timeout can be specified for the connection.\n\n```python\nfrom e2b import Sandbox\n\n# Create and pause a sandbox\nsandbox = Sandbox.create()\nsandbox.beta_pause()\n\n# Connect to the paused sandbox using the instance\nconnected_sandbox_instance = sandbox.connect(timeout=10)\nprint(f\"Connected to sandbox ID: {connected_sandbox_instance.sandbox_id}\")\n\n# Connect to the sandbox using its ID\nsandbox_id = sandbox.sandbox_id\nconnected_sandbox_id = Sandbox.connect(sandbox_id, timeout=10)\nprint(f\"Connected to sandbox ID: {connected_sandbox_id.sandbox_id}\")\n```\n\n--------------------------------\n\n### Connect to Sandbox (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.5.0/sandbox_sync/page.mdx\n\nEstablishes a connection to an existing sandbox using its ID. This allows for reconnecting to the same sandbox environment from different locations. It supports authentication via API key and proxy configuration.\n\n```python\nfrom e2b import Sandbox\n\n# Connect to an existing sandbox using its ID\nsandbox = Sandbox()\nsandbox_id = sandbox.sandbox_id\nsame_sandbox = Sandbox.connect(sandbox_id)\n\n# Example with explicit parameters\n# same_sandbox = Sandbox.connect(sandbox_id=sandbox_id, api_key=\"YOUR_API_KEY\", domain=\"e2b.dev\")\n```\n\n--------------------------------\n\n### Connect to Existing Sandbox (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.9.0/sandbox_async/page.mdx\n\nEstablishes a connection to an existing sandbox using its unique ID. This allows for reconnecting to a sandbox from different locations or in different execution environments. It supports API key and proxy configurations.\n\n```python\n@classmethod\nasync def connect(cls, sandbox_id: str, api_key: Optional[str] = None, domain: Optional[str] = None, debug: Optional[bool] = None, proxy: Optional[ProxyTypes] = None) -> 'AsyncSandbox':\n    \"\"\"\n    Connect to an existing sandbox.\n\n    With sandbox ID you can connect to the same sandbox from different places or environments (serverless functions, etc).\n\n    Arguments:\n        sandbox_id: Sandbox ID\n        api_key: E2B API Key to use for authentication, defaults to `E2B_API_KEY` environment variable\n        proxy: Proxy to use for the request and for the **requests made to the returned sandbox**\n\n    Returns:\n        sandbox instance for the existing sandbox\n    \"\"\"\n    pass\n\n# Example Usage:\nsandbox = await AsyncSandbox.create()\nsandbox_id = sandbox.sandbox_id\nsame_sandbox = await AsyncSandbox.connect(sandbox_id)\n```\n\n--------------------------------\n\n### Connect to Existing Sandbox (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.2.1/sandbox_async/page.mdx\n\nClass method to connect to an existing sandbox using its ID. Useful for reconnecting to a sandbox from different environments or processes.\n\n```python\nfrom e2b import Sandbox\n\nasync def connect_to_sandbox(sandbox_id):\n    existing_sandbox = await Sandbox.connect(sandbox_id)\n    print(f\"Connected to sandbox: {existing_sandbox.sandbox_id}\")\n    await existing_sandbox.close()\n```\n\n--------------------------------\n\n### Connect to an Existing Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.1.2/sandbox_sync/page.mdx\n\nEstablishes a connection to an existing E2B sandbox, resuming it if it's paused. This allows interaction with a sandbox from different contexts or after it has been paused. You can connect using an instance or a sandbox ID.\n\n```python\nfrom e2b import Sandbox\n\n# Connect using an instance\nsandbox = Sandbox.create()\nsandbox.beta_pause()\nsame_sandbox = sandbox.connect()\n\n# Connect using sandbox ID\nsandbox_instance = Sandbox.create()\nSandbox.beta_pause(sandbox_instance.sandbox_id)\nsame_sandbox_by_id = Sandbox.connect(sandbox_instance.sandbox_id)\n```\n\n--------------------------------\n\n### Connect to Existing Sandbox - Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.7.0/sandbox_sync/page.mdx\n\nConnects to an existing sandbox using its ID. This allows for reconnecting to the same sandbox from different locations or environments. Requires an API key for authentication.\n\n```python\nfrom e2b import Sandbox\nfrom typing import Optional\nfrom e2b.types import ProxyTypes\n\ndef connect_sandbox(sandbox_id: str,\n                    api_key: Optional[str] = None,\n                    domain: Optional[str] = None,\n                    debug: Optional[bool] = None,\n                    proxy: Optional[ProxyTypes] = None) -> Sandbox:\n    \"\"\"Connects to an existing Sandbox.\"\"\"\n    # Placeholder for actual API call\n    print(f\"Connecting to sandbox ID: {sandbox_id}\")\n    print(f\"API Key: {api_key}\")\n    print(f\"Domain: {domain}\")\n    print(f\"Debug: {debug}\")\n    print(f\"Proxy: {proxy}\")\n    # Example of returning a mock sandbox object\n    class MockSandbox:\n        def __init__(self):\n            self.sandbox_id = sandbox_id\n            self.sandbox_domain = \"mock-domain.e2b.dev\"\n        def is_running(self):\n            return True\n        def kill(self):\n            print(\"Mock sandbox killed\")\n            return True\n    return MockSandbox() # Replace with actual Sandbox.connect() call\n```\n\n--------------------------------\n\n### Connect to Sandbox (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.0.0/sandbox/page.mdx\n\nConnects to an existing sandbox using its ID. If the sandbox is paused, it will be automatically resumed. The sandbox must be in a running or paused state. This method returns a promise for the sandbox instance. It can be used to connect from different environments.\n\n```typescript\nstatic connect<S>(this: S, sandboxId: string, opts?: SandboxConnectOpts): Promise<InstanceType<S>>\n```\n\n--------------------------------\n\n### Connect to Sandbox (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.1.2/sandbox_async/page.mdx\n\nConnects to an existing sandbox, resuming it if it's paused. This method allows connecting to the same sandbox from multiple locations. It supports a timeout parameter for the connection process.\n\n```python\n@overload\nasync def connect(timeout: Optional[int] = None,\n                  **opts: Unpack[ApiParams]) -> Self:\n```\n\n```python\n@overload\n@classmethod\nasync def connect(cls,\n                  sandbox_id: str,\n                  timeout: Optional[int] = None,\n                  **opts: Unpack[ApiParams]) -> Self:\n```\n\n```python\n@class_method_variant(\"_cls_connect\")\nasync def connect(timeout: Optional[int] = None,\n                  **opts: Unpack[ApiParams]) -> Self:\n```\n\n--------------------------------\n\n### Connect to a sandbox using Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.0.1/sandbox_async/page.mdx\n\nConnects to an existing sandbox instance. If the sandbox is paused, it will be automatically resumed. This method allows connecting to the same sandbox from multiple locations. It accepts an optional timeout in seconds.\n\n```python\nawait AsyncSandbox.connect(\n    timeout: Optional[int] = None,\n    **opts: Unpack[ApiParams]\n) -> Self\n```\n\n```python\nawait AsyncSandbox.connect(\n    sandbox_id: str,\n    timeout: Optional[int] = None,\n    **opts: Unpack[ApiParams]\n) -> Self\n```\n\n```python\nawait sandbox.connect(\n    timeout: Optional[int] = None,\n    **opts: Unpack[ApiParams]\n) -> Self\n```\n\n--------------------------------\n\n### Connect to an existing E2B Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.0.0/sandbox_sync/page.mdx\n\nConnects to an existing sandbox, resuming it if it is paused. This can be done using an instance of a sandbox or by providing a sandbox ID. This allows re-establishing a connection to a sandbox from different contexts.\n\n```python\nfrom e2b import Sandbox\n\n# Connect using an existing sandbox instance\nsandbox = Sandbox.create()\nsandbox.beta_pause()\nsame_sandbox = sandbox.connect()\n\n# Connect using sandbox ID\nsandbox_for_id = Sandbox.create()\nSandbox.beta_pause(sandbox_for_id.sandbox_id)\nconnected_sandbox = Sandbox.connect(sandbox_for_id.sandbox_id)\n```\n\n--------------------------------\n\n### Pause and Resume an E2B Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.1.0/sandbox/page.mdx\n\nShows how to pause a running sandbox using `betaPause()` and then reconnect to it using `connect()`. Pausing saves the sandbox state, allowing for later resumption without losing progress.\n\n```typescript\nconst sandbox = await Sandbox.create();\nawait sandbox.betaPause();\n\n// Connect to the same sandbox.\nconst sameSandbox = await sandbox.connect();\n```\n\n--------------------------------\n\n### Connect to Sandbox Instance - Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.0.2/sandbox_async/page.mdx\n\nConnects to an existing sandbox instance, automatically resuming it if it's paused. This method can be used to connect to the same sandbox from different locations. It is designed for instance-level operations.\n\n```python\nsandbox = await AsyncSandbox.create()\nawait sandbox.beta_pause()\nsame_sandbox = await sandbox.connect()\n\n```\n\n--------------------------------\n\n### Connect to Sandbox by ID (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.1.1/sandbox_async/page.mdx\n\nConnects to a specific sandbox using its ID. If the sandbox is paused, it will be resumed automatically. This allows connection from different environments. The sandbox must be running or paused, and the method returns a running sandbox instance.\n\n```python\n@classmethod\nasync def connect(\n    cls,\n    sandbox_id: str,\n    timeout: Optional[int] = None,\n    **opts: Unpack[ApiParams]\n) -> Self\n```\n\n--------------------------------\n\n### Connect to Existing Sandbox (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.10.0/sandbox_async/page.mdx\n\nAsynchronously connects to an existing sandbox using its ID. Allows re-establishing a connection to a sandbox from different locations. Requires the sandbox ID and accepts optional API key, domain, debug flag, and proxy settings. Returns the sandbox instance. Useful for multi-environment access to the same sandbox.\n\n```python\n@classmethod\nasync def connect(cls,\n                  sandbox_id: str,\n                  api_key: Optional[str] = None,\n                  domain: Optional[str] = None,\n                  debug: Optional[bool] = None,\n                  proxy: Optional[ProxyTypes] = None):\n    \"\"\"\n    Connect to an existing sandbox.\n\n    Arguments:\n        sandbox_id: Sandbox ID\n        api_key: E2B API Key to use for authentication\n        proxy: Proxy to use for the request and for the **requests made to the returned sandbox**\n\n    Returns:\n        sandbox instance for the existing sandbox\n    \"\"\"\n    pass\n\n# Example Usage:\nsandbox = await AsyncSandbox.create()\nsame_sandbox = await AsyncSandbox.connect(sandbox.sandbox_id)\nprint(same_sandbox)\n```\n\n--------------------------------\n\n### Connect to Sandbox - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.0.3/sandbox/page.mdx\n\nConnects to an existing sandbox using its ID. If the sandbox is paused, it will be automatically resumed. The sandbox must be either running or paused to connect.\n\n```typescript\nstatic connect<S>(\n   this: S, \n   sandboxId: string, \nopts?: SandboxConnectOpts): Promise<InstanceType<S>>\n```\n\n```typescript\nconst sandbox = await Sandbox.create()\nconst sandboxId = sandbox.sandboxId\n\n// Connect to the same sandbox.\nconst sameSandbox = await Sandbox.connect(sandboxId)\n```\n\n--------------------------------\n\n### Connect to an Existing Sandbox Instance\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.0.0/sandbox/page.mdx\n\nConnects to a sandbox instance, automatically resuming it if it's paused. This allows multiple connections to the same sandbox from different environments. It returns a Sandbox object representing the connected instance.\n\n```typescript\nconst sandbox = await Sandbox.create()\nawait sandbox.betaPause()\n\n// Connect to the same sandbox.\nconst sameSandbox = await sandbox.connect()\n```\n\n--------------------------------\n\n### Connect to Existing E2B Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.10.0/sandbox_sync/page.mdx\n\nEstablishes a connection to a pre-existing sandbox using its ID. Allows specifying API key, domain, debug mode, and proxy. Useful for reconnecting to a sandbox from different contexts.\n\n```python\n@classmethod\ndef connect(cls,\n            sandbox_id: str,\n            api_key: Optional[str] = None,\n            domain: Optional[str] = None,\n            debug: Optional[bool] = None,\n            proxy: Optional[ProxyTypes] = None)\n```\n\n```python\nsandbox = Sandbox()\nsandbox_id = sandbox.sandbox_id\n\nsame_sandbox = Sandbox.connect(sandbox_id)\n```",
    "chunks": null,
    "metadata": {}
  },
  "cached_at": 1767726982.0135794,
  "params": {
    "owner": "e2b-dev",
    "repo": "e2b",
    "version": "latest",
    "topic": "sandbox connect keep alive websocket reconnect",
    "tokens": 4000,
    "format": "txt"
  }
}