{
  "data": {
    "library": "/e2b-dev/e2b",
    "version": "latest",
    "topic": "process exit on_exit event callback error handling",
    "tokens": 2723,
    "content": "### TypeScript: Parse Execution Output and Events\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/code-interpreter-js-sdk/v1.5.0/messaging/page.mdx\n\nThe `parseOutput()` function asynchronously processes an `Execution` object, parsing lines of output. It accepts optional callback functions for handling stdout, stderr, results, and errors, enabling real-time event processing during execution.\n\n```typescript\nfunction parseOutput(\n   execution: Execution,\n   line: string,\n   onStdout?: (output: OutputMessage) => any,\n   onStderr?: (output: OutputMessage) => any,\n   onResult?: (data: Result) => any,\n  onError?: (error: ExecutionError) => any): Promise<void>\n```\n\n--------------------------------\n\n### TypeScript: Parse Execution Output\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/code-interpreter-js-sdk/v1.0.4/messaging/page.mdx\n\nParses the output of an execution, handling standard output, standard error, results, and errors using provided callback functions. This is essential for managing the lifecycle of a sandboxed process.\n\n```typescript\n/**\n * Parses the output of an execution, handling stdout, stderr, results, and errors.\n * @param {Execution} execution The execution context.\n * @param {string} line The current line of output to parse.\n * @param {(output: OutputMessage) => any} [onStdout] Callback for standard output messages.\n * @param {(output: OutputMessage) => any} [onStderr] Callback for standard error messages.\n * @param {(data: Result) => any} [onResult] Callback for result data.\n * @param {(error: ExecutionError) => any} [onError] Callback for execution errors.\n * @returns {Promise<void>} A promise that resolves when parsing is complete.\n */\nfunction parseOutput(\n   execution: Execution, \n   line: string, \n   onStdout?: (output: OutputMessage) => any, \n   onStderr?: (output: OutputMessage) => any, \n   onResult?: (data: Result) => any,\n   onError?: (error: ExecutionError) => any): Promise<void>\n```\n\n--------------------------------\n\n### AsyncCommandHandle Properties in Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.5.2/sandbox_async/page.mdx\n\nRepresents a handle for an asynchronously executing command. It provides properties to access the command's process ID (pid), standard output (stdout), standard error (stderr), execution error messages (error), and exit code (exit_code). The exit_code is None if the command is still running.\n\n```python\nclass AsyncCommandHandle():\n    @property\n    def pid(self):\n        pass\n\n    @property\n    def stdout(self):\n        pass\n\n    @property\n    def stderr(self):\n        pass\n\n    @property\n    def error(self):\n        pass\n\n    @property\n    def exit_code(self):\n        pass\n```\n\n--------------------------------\n\n### Manage Command Execution Handle (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.0.6/sandbox_async/page.mdx\n\nRepresents a handle to an executing command within the sandbox. Provides methods to access process ID, stdout, stderr, error messages, exit codes, and to disconnect, wait for completion, or kill the command.\n\n```python\nclass AsyncCommandHandle():\n    @property\n    def pid(self) -> int:\n        # Implementation details omitted for brevity\n        pass\n\n    @property\n    def stdout(self) -> str:\n        # Implementation details omitted for brevity\n        pass\n\n    @property\n    def stderr(self) -> str:\n        # Implementation details omitted for brevity\n        pass\n\n    @property\n    def error(self) -> str:\n        # Implementation details omitted for brevity\n        pass\n\n    @property\n    def exit_code(self) -> Optional[int]:\n        # Implementation details omitted for brevity\n        pass\n\n    async def disconnect(self) -> None:\n        # Implementation details omitted for brevity\n        pass\n\n    async def wait(self) -> CommandResult:\n        # Implementation details omitted for brevity\n        pass\n\n    async def kill(self) -> bool:\n        # Implementation details omitted for brevity\n        pass\n```\n\n--------------------------------\n\n### Python: Wait for Command Completion and Handle Output\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.0.2/sandbox_sync/page.mdx\n\nWaits for a command to finish execution in the sandbox. It can optionally process PTY, stdout, and stderr output via callbacks. Throws a CommandExitException if the command returns a non-zero exit code.\n\n```python\nfrom typing import Optional, Callable\n\ndef wait(on_pty: Optional[Callable[[PtyOutput], None]] = None,\n         on_stdout: Optional[Callable[[str], None]] = None,\n         on_stderr: Optional[Callable[[str], None]] = None) -> CommandResult:\n    # Implementation details...\n    pass\n```\n\n--------------------------------\n\n### AsyncCommandHandle Properties (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.5.0/sandbox_async/page.mdx\n\nRepresents a handle to an asynchronous command execution. Provides properties to access the command's process ID (pid), stdout, stderr, error messages, and exit code. The exit_code is None if the command is still running.\n\n```python\nclass AsyncCommandHandle():\n    @property\n    def pid(self) -> int:\n        # ... implementation details ...\n        pass\n\n    @property\n    def stdout(self) -> str:\n        # ... implementation details ...\n        pass\n\n    @property\n    def stderr(self) -> str:\n        # ... implementation details ...\n        pass\n\n    @property\n    def error(self) -> Optional[str]:\n        # ... implementation details ...\n        pass\n\n    @property\n    def exit_code(self) -> Optional[int]:\n        # ... implementation details ...\n        pass\n```\n\n--------------------------------\n\n### Watch Directory for Filesystem Events (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.0.1/sandbox_async/page.mdx\n\nThe `watch_dir` function monitors a specified directory for filesystem events. It accepts callbacks for handling events and optional callbacks for exit conditions, along with user and timeout configurations. It returns an `AsyncWatchHandle` to manage the watching process.\n\n```python\nasync def watch_dir(path: str,\n                    on_event: OutputHandler[FilesystemEvent],\n                    on_exit: Optional[OutputHandler[Exception]] = None,\n                    user: Username = \"user\",\n                    request_timeout: Optional[float] = None,\n                    timeout: Optional[float] = 60) -> AsyncWatchHandle:\n    \"\"\"Watch directory for filesystem events.\"\"\"\n    pass\n```\n\n--------------------------------\n\n### Command Execution Handle\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.5.6/sandbox_async/page.mdx\n\nRepresents a handle to an executing command, allowing interaction with its process. It provides access to the process ID, standard output, standard error, execution errors, and exit code. Methods are available to disconnect, wait for completion, and kill the command.\n\n```python\nclass AsyncCommandHandle()\n\n@property\ndef pid()\n\n@property\ndef stdout()\n\n@property\ndef stderr()\n\n@property\ndef error()\n\n@property\ndef exit_code()\n\nasync def disconnect() -> None\n\nasync def wait() -> CommandResult\n\nasync def kill() -> bool\n```\n\n--------------------------------\n\n### AsyncCommandHandle Properties in Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.5.3/sandbox_async/page.mdx\n\nRepresents a handle to an asynchronously running command. Provides properties to access the command's process ID (`pid`), standard output (`stdout`), standard error (`stderr`), execution error message (`error`), and exit code (`exit_code`).\n\n```python\nclass AsyncCommandHandle():\n    @property\n    def pid() -> int:\n        # Implementation details...\n        pass\n\n    @property\n    def stdout() -> str:\n        # Implementation details...\n        pass\n\n    @property\n    def stderr() -> str:\n        # Implementation details...\n        pass\n\n    @property\n    def error() -> Optional[str]:\n        # Implementation details...\n        pass\n\n    @property\n    def exit_code() -> Optional[int]:\n        # Implementation details...\n        pass\n```\n\n--------------------------------\n\n### Configure Stderr Output Callback - TypeScript\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.7/commands/page.mdx\n\nSets up an optional callback function to handle standard error output from a command. The callback receives output as a string and can be asynchronous.\n\n```typescript\noptional onStderr: (data: string) => void | Promise<void>;\n```\n\n--------------------------------\n\n### Connect to Running Command (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v2.0.2/sandbox_async/page.mdx\n\nConnects to an existing process in the sandbox, allowing interaction with its stdout, stderr, and exit status. It supports timeouts and callbacks for output handling. Dependencies include `Optional`, `OutputHandler`, `Stdout`, `Stderr`, and `AsyncCommandHandle` from the e2b library.\n\n```python\nasync def connect(\n        pid: int,\n        timeout: Optional[float] = 60,\n        request_timeout: Optional[float] = None,\n        on_stdout: Optional[OutputHandler[Stdout]] = None,\n        on_stderr: Optional[OutputHandler[Stderr]] = None\n) -> AsyncCommandHandle:\n    # Function implementation to connect to a running command\n```\n\n--------------------------------\n\n### AsyncCommandHandle Properties (Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.0.3/sandbox_async/page.mdx\n\nProvides access to properties of an `AsyncCommandHandle` object, which represents a running command. These properties allow retrieval of the command's process ID (pid), standard output (stdout), standard error (stderr), any execution error message (error), and the command's exit code (exit_code). The `exit_code` is `None` if the command is still running.\n\n```python\nclass AsyncCommandHandle():\n    @property\n    def pid(self) -> int:\n        # Property implementation details omitted for brevity\n        pass\n\n    @property\n    def stdout(self) -> str:\n        # Property implementation details omitted for brevity\n        pass\n\n    @property\n    def stderr(self) -> str:\n        # Property implementation details omitted for brevity\n        pass\n\n    @property\n    def error(self) -> Optional[str]:\n        # Property implementation details omitted for brevity\n        pass\n\n    @property\n    def exit_code(self) -> Optional[int]:\n        # Property implementation details omitted for brevity\n        pass\n```\n\n--------------------------------\n\n### AsyncCommandHandle Class for Command Execution in Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.3.4/sandbox_async/page.mdx\n\nThe AsyncCommandHandle class provides a handle for managing asynchronous command executions. It allows users to retrieve process IDs, standard output and error streams, error messages, exit codes, and to disconnect, wait for, or kill the running command. It is essential for interacting with commands executed within the sandbox environment.\n\n```python\nclass AsyncCommandHandle():\n    \"\"\"Command execution handle.\"\"\"\n\n    @property\n    def pid(self):\n        \"\"\"Command process ID.\"\"\"\n        ...\n\n    @property\n    def stdout(self):\n        \"\"\"Command stdout output.\"\"\"\n        ...\n\n    @property\n    def stderr(self):\n        \"\"\"Command stderr output.\"\"\"\n        ...\n\n    @property\n    def error(self):\n        \"\"\"Command execution error message.\"\"\"\n        ...\n\n    @property\n    def exit_code(self):\n        \"\"\"Command execution exit code.\n\n        `0` if the command finished successfully.\n\n        It is `None` if the command is still running.\n        \"\"\"\n        ...\n\n    async def disconnect(self) -> None:\n        \"\"\"Disconnects from the command.\n\nThe command is not killed, but SDK stops receiving events from the command.\nYou can reconnect to the command using `sandbox.commands.connect` method.\n        \"\"\"\n        ...\n\n    async def wait(self) -> CommandResult:\n        \"\"\"Wait for the command to finish and return the result.\n\n        If the command exits with a non-zero exit code, it throws a `CommandExitException`.\n\n        **Returns**:\n            `CommandResult` result of command execution\n        \"\"\"\n        ...\n\n    async def kill(self) -> bool:\n        \"\"\"Kills the command.\n\n        It uses `SIGKILL` signal to kill the command\n\n        **Returns**:\n            `True` if the command was killed successfully, `False` if the command was not found\n        \"\"\"\n        ...\n```\n\n--------------------------------\n\n### Python AsyncCommandHandle for Command Execution\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.3.1/sandbox_async/page.mdx\n\nThe AsyncCommandHandle class provides a way to manage and interact with commands executed within the E2B sandbox. It allows retrieving process information (PID, stdout, stderr, exit code), disconnecting from the command, waiting for its completion, and terminating it. The error property returns the command execution error message, and exit_code is None if the command is still running.\n\n```python\nclass AsyncCommandHandle:\n    @property\n    def pid(self)\n\n    @property\n    def stdout(self)\n\n    @property\n    def stderr(self)\n\n    @property\n    def error(self)\n\n    @property\n    def exit_code(self)\n\n    async def disconnect(self) -> None\n\n    async def wait(self) -> CommandResult\n\n    async def kill(self) -> bool\n```\n\n--------------------------------\n\n### AsyncCommandHandle Properties - Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.7.1/sandbox_async/page.mdx\n\nProperties of the AsyncCommandHandle class to access command execution details. These include the process ID (`pid`), standard output (`stdout`), standard error (`stderr`), execution error message (`error`), and the command's exit code (`exit_code`).\n\n```python\n@property\ndef pid()\n\n@property\ndef stdout()\n\n@property\ndef stderr()\n\n@property\ndef error()\n\n@property\ndef exit_code()\n```\n\n--------------------------------\n\n### AsyncCommandHandle Properties in Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.5.4/sandbox_async/page.mdx\n\nRepresents a handle for an asynchronous command execution. It provides access to the process ID, stdout, stderr, execution error, and exit code.\n\n```python\n@property\ndef pid()\n\n```\n\n```python\n@property\ndef stdout()\n\n```\n\n```python\n@property\ndef stderr()\n\n```\n\n```python\n@property\ndef error()\n\n```\n\n```python\n@property\ndef exit_code()\n\n```\n\n--------------------------------\n\n### Python AsyncCommandHandle for Process Management\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.2.0/sandbox_async/page.mdx\n\nThe AsyncCommandHandle class provides a way to manage the lifecycle of a command executed within the sandbox. It allows retrieval of process information such as PID, stdout, stderr, error messages, and exit codes. It also supports disconnecting, waiting for completion, and killing the command.\n\n```python\nclass AsyncCommandHandle():\n    @property\n    def pid(self)\n\n    @property\n    def stdout(self)\n\n    @property\n    def stderr(self)\n\n    @property\n    def error(self)\n\n    @property\n    def exit_code(self)\n\n    async def disconnect(self) -> None\n\n    async def wait(self) -> CommandResult\n\n    async def kill(self) -> bool\n```\n\n--------------------------------\n\n### Stream stdout and stderr from code execution\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/code-interpreting/streaming/page.mdx\n\nHandle standard output and standard error streams from code executed in the E2B sandbox. This involves providing `onStdout`/`onStderr` (JavaScript) or `on_stdout`/`on_stderr` (Python) callbacks to capture and process the output lines, including runtime errors.\n\n```javascript\nimport { Sandbox } from '@e2b/code-interpreter'\n\nconst codeToRun = `\n  import time\n  import sys\n  print(\"This goes first to stdout\")\n  time.sleep(3)\n  print(\"This goes later to stderr\", file=sys.stderr)\n  time.sleep(3)\n  print(\"This goes last\")\n`\nconst sandbox = await Sandbox.create()\nsandbox.runCode(codeToRun, {\n  // Use `onError` to handle runtime code errors\n  onError: error => console.error('error:', error), \n  onStdout: data => console.log('stdout:', data), \n  onStderr: data => console.error('stderr:', data), \n})\n```\n\n```python\nfrom e2b_code_interpreter import Sandbox\n\ncode_to_run = \"\"\"\n  import time\n  import sys\n  print(\"This goes first to stdout\")\n  time.sleep(3)\n  print(\"This goes later to stderr\", file=sys.stderr)\n  time.sleep(3)\n  print(\"This goes last\")\n\"\"\"\n\nsandbox = Sandbox.create()\nsandbox.run_code(\n  code_to_run,\n  # Use `on_error` to handle runtime code errors\n  on_error=lambda error: print('error:', error), \n  on_stdout=lambda data: print('stdout:', data), \n  on_stderr=lambda data: print('stderr:', data), \n)\n```\n\n--------------------------------\n\n### Run Command (Foreground) - Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.5.3/sandbox_sync/page.mdx\n\nStarts a command in the foreground and waits for its completion. It handles standard output and error streams via optional callbacks and supports timeouts. Returns a CommandResult upon successful execution or throws an exception on non-zero exit codes.\n\n```python\nfrom typing import Union, Literal, Optional, Dict, Callable\n\n# Assuming CommandResult, Username, and other types are defined elsewhere\n\n@overload\ndef run(cmd: str,\n        background: Union[Literal[False], None] = None,\n        envs: Optional[Dict[str, str]] = None,\n        user: Username = \"user\",\n        cwd: Optional[str] = None,\n        on_stdout: Optional[Callable[[str], None]] = None,\n        on_stderr: Optional[Callable[[str], None]] = None,\n        timeout: Optional[float] = 60,\n        request_timeout: Optional[float] = None) -> CommandResult\n```\n\n--------------------------------\n\n### AsyncCommandHandle: Manage Command Execution in E2B\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.3.0/sandbox_async/page.mdx\n\nThe AsyncCommandHandle class provides an interface to manage running commands within an E2B sandbox. It allows retrieval of process information like PID, stdout, stderr, error messages, and exit codes. It also supports disconnecting from the command without killing it, waiting for its completion, and forcefully killing the process.\n\n```python\nclass AsyncCommandHandle:\n    @property\n    def pid(self)\n    @property\n    def stdout(self)\n    @property\n    def stderr(self)\n    @property\n    def error(self)\n    @property\n    def exit_code(self)\n    async def disconnect(self) -> None\n    async def wait(self) -> CommandResult\n    async def kill(self) -> bool\n```\n\n--------------------------------\n\n### Python: AsyncCommandHandle for Command Execution\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.2.1/sandbox_async/page.mdx\n\nThe AsyncCommandHandle class provides a mechanism to manage and interact with a running command within the sandbox. It allows retrieval of process ID, stdout, stderr, error messages, and exit codes. It also supports disconnecting from the command, waiting for its completion, and killing the process.\n\n```python\nclass AsyncCommandHandle:\n    \"\"\"Command execution handle.\"\"\"\n\n    @property\n    def pid(self) -> int:\n        \"\"\"Command process ID.\"\"\"\n        ...\n\n    @property\n    def stdout(self) -> str:\n        \"\"\"Command stdout output.\"\"\"\n        ...\n\n    @property\n    def stderr(self) -> str:\n        \"\"\"Command stderr output.\"\"\"\n        ...\n\n    @property\n    def error(self) -> Optional[str]:\n        \"\"\"Command execution error message.\"\"\"\n        ...\n\n    @property\n    def exit_code(self) -> Optional[int]:\n        \"\"\"Command execution exit code.\n\n        `0` if the command finished successfully.\n        It is `None` if the command is still running.\n        \"\"\"\n        ...\n\n    async def disconnect(self) -> None:\n        \"\"\"Disconnects from the command.\n\n        The command is not killed, but SDK stops receiving events from the command.\n        You can reconnect to the command using `sandbox.commands.connect` method.\n        \"\"\"\n        ...\n\n    async def wait(self) -> CommandResult:\n        \"\"\"Wait for the command to finish and return the result.\n\n        If the command exits with a non-zero exit code, it throws a `CommandExitException`.\n\n        **Returns**:\n\n        `CommandResult` result of command execution\n        \"\"\"\n        ...\n\n    async def kill(self) -> bool:\n        \"\"\"Kills the command.\n\n        It uses `SIGKILL` signal to kill the command\n\n        **Returns**:\n\n        `True` if the command was killed successfully, `False` if the command was not found\n        \"\"\"\n        ...\n```\n\n--------------------------------\n\n### AsyncCommandHandle Properties\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.7.0/sandbox_async/page.mdx\n\nProvides access to properties of an asynchronous command execution handle, including its process ID (pid), standard output (stdout), standard error (stderr), execution error, and exit code. These properties allow monitoring the state and results of a running command.\n\n```python\n@property\ndef pid() -> int\n```\n\n```python\n@property\ndef stdout() -> str\n```\n\n```python\n@property\ndef stderr() -> str\n```\n\n```python\n@property\ndef error() -> Optional[str]\n```\n\n```python\n@property\ndef exit_code() -> Optional[int]\n```\n\n--------------------------------\n\n### TypeScript: Parse Execution Output\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/code-interpreter-js-sdk/v1.1.0/messaging/page.mdx\n\nThe `parseOutput` function asynchronously processes an `Execution` object and a given line of output. It supports optional callbacks for handling stdout, stderr, results, and errors, enabling detailed monitoring of execution.\n\n```typescript\nfunction parseOutput(\n   execution: Execution,\n   line: string,\n   onStdout?: (output: OutputMessage) => any,\n   onStderr?: (output: OutputMessage) => any,\n   onResult?: (data: Result) => any,\n onError?: (error: ExecutionError) => any): Promise<void>\n```\n\n--------------------------------\n\n### AsyncCommandHandle Class - Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.7.1/sandbox_async/page.mdx\n\nRepresents a handle to a command's execution, providing methods to manage its lifecycle. It offers properties to access process ID, stdout, stderr, error messages, and exit codes, along with methods to disconnect, wait for completion, and kill the command.\n\n```python\nclass AsyncCommandHandle()\n```\n\n--------------------------------\n\n### AsyncCommandHandle class for managing command execution in Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.5.1/sandbox_async/page.mdx\n\nRepresents a handle for managing a command's execution within the sandbox. It provides properties to access the command's PID, stdout, stderr, error messages, and exit code, as well as methods for waiting for completion and interacting with the process.\n\n```python\nclass AsyncCommandHandle()\n\nCommand execution handle.\n\nIt provides methods for waiting for the command to finish, retrieving stdout/stderr, and killing the command.\n\n### pid\n\n@property\ndef pid()\n\nCommand process ID.\n\n### stdout\n\n@property\ndef stdout()\n\nCommand stdout output.\n\n### stderr\n\n@property\ndef stderr()\n\nCommand stderr output.\n\n### error\n\n@property\ndef error()\n\nCommand execution error message.\n\n### exit_code\n\n@property\ndef exit_code()\n\nCommand execution exit code.\n\n`0` if the command finished successfully.\n\nIt is `None` if the command is still running.\n```\n\n--------------------------------\n\n### Manage Command Execution with AsyncCommandHandle\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.6.0/sandbox_async/page.mdx\n\nProvides methods to interact with a running command in the E2B sandbox. It allows checking the process ID, stdout/stderr, error status, exit code, disconnecting, waiting for completion, and killing the process.\n\n```python\nclass AsyncCommandHandle()\n\n    @property\n    def pid()\n\n    @property\n    def stdout()\n\n    @property\n    def stderr()\n\n    @property\n    def error()\n\n    @property\n    def exit_code()\n\n    async def disconnect() -> None\n\n    async def wait() -> CommandResult\n\n    async def kill() -> bool\n```\n\n--------------------------------\n\n### AsyncCommandHandle Properties and Methods - Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.0.0/sandbox_async/page.mdx\n\nDefines the interface for interacting with a running command via an `AsyncCommandHandle`. This includes accessing process ID, stdout, stderr, error messages, and exit codes, as well as methods to disconnect or wait for completion.\n\n```python\nclass AsyncCommandHandle():\n    @property\n    def pid(self) -> int:\n        # ...\n\n    @property\n    def stdout(self) -> Any:\n        # ...\n\n    @property\n    def stderr(self) -> Any:\n        # ...\n\n    @property\n    def error(self) -> Optional[str]:\n        # ...\n\n    @property\n    def exit_code(self) -> Optional[int]:\n        # ...\n\n    async def disconnect(self) -> None:\n        # ...\n\n    async def wait(self) -> CommandResult:\n        # ...\n\n```",
    "chunks": null,
    "metadata": {}
  },
  "cached_at": 1769531437.370364,
  "params": {
    "owner": "e2b-dev",
    "repo": "e2b",
    "version": "latest",
    "topic": "process exit on_exit event callback error handling",
    "tokens": 5000,
    "format": "txt"
  }
}