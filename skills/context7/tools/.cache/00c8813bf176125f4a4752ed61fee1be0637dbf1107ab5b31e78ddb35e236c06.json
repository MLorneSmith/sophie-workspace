{
	"data": {
		"library": "/e2b-dev/e2b",
		"version": "latest",
		"topic": "get_host web server http",
		"tokens": 1800,
		"content": "### Get host address for a sandbox port\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.13.1/sandbox/page.mdx\n\nObtains the host address for a specified port within the sandbox. This allows external connections to services running on that port inside the sandbox, such as HTTP or WebSocket servers. The example shows how to start a Python HTTP server and then get its accessible URL.\n\n```typescript\nimport { Sandbox } from 'e2b';\n\nconst sandbox = await Sandbox.create();\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000');\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000);\n```\n\n--------------------------------\n\n### Access Sandbox Port with getHost\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.1.0/sandbox/page.mdx\n\nDemonstrates how to get the host address for a specific port running inside the sandbox. This allows external connections to services hosted within the sandbox, such as HTTP servers.\n\n```typescript\nconst sandbox = await Sandbox.create();\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000');\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000);\n```\n\n--------------------------------\n\n### Get Sandbox Port Host Address\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.2/sandbox/page.mdx\n\nObtains the host address for a given port within the sandbox. This is essential for establishing external connections to services running inside the sandbox, such as HTTP or WebSocket servers.\n\n```typescript\nconst sandbox = await Sandbox.create()\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000')\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000)\n\n```\n\n--------------------------------\n\n### Get Sandbox Host Address for a Port\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.0/sandbox/page.mdx\n\nExplains how to retrieve the host address for a specific port within the sandbox. This is useful for connecting to services like HTTP or WebSocket servers running inside the sandbox from outside. It requires a running sandbox and knowledge of the port number.\n\n```typescript\nconst sandbox = await Sandbox.create()\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000')\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000)\n```\n\n--------------------------------\n\n### Get sandbox port host address\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.5/sandbox/page.mdx\n\nObtains the host address for a specified port within the sandbox. This is useful for connecting to services running inside the sandbox, like HTTP servers or WebSocket endpoints, from external applications.\n\n```typescript\nconst sandbox = await Sandbox.create()\n// Start an HTTP server on port 3000\nawait sandbox.commands.exec('python3 -m http.server 3000')\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000)\n```\n\n--------------------------------\n\n### Get Sandbox Port Host Address\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.11.0/sandbox/page.mdx\n\nObtains the host address for a specific port exposed by the sandbox. This allows external connections to services running within the sandbox, such as HTTP servers or WebSockets. Requires the port number.\n\n```typescript\ngetHost(port: number): string\n\n// Example:\nconst sandbox = await Sandbox.create()\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000')\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000)\n```\n\n--------------------------------\n\n### Get Host Address for Sandbox Port\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.13.2/sandbox/page.mdx\n\nObtains the host address for a specific port running inside the sandbox. This is useful for connecting to services (like HTTP servers or WebSockets) hosted within the sandbox from external applications. It requires the port number as an argument.\n\n```typescript\ngetHost(port: number): string\n```\n\n```typescript\nconst sandbox = await Sandbox.create();\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000');\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000);\n```\n\n--------------------------------\n\n### Get Sandbox Port Host Address\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.3/sandbox/page.mdx\n\nObtains the host address for a given port within the sandbox. This allows external connections to services running on specific ports inside the sandbox, such as HTTP servers or WebSocket endpoints. It requires the port number as input.\n\n```typescript\ngetHost(port: number): string\n\n// Example:\nconst sandbox = await Sandbox.create()\nawait sandbox.commands.exec('python3 -m http.server 3000')\nconst serverURL = sandbox.getHost(3000)\n```\n\n--------------------------------\n\n### Run HTTP Server and Fetch Content in E2B Sandbox (JS/Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sandbox/internet-access/page.mdx\n\nIllustrates how to start an HTTP server within an E2B sandbox and then fetch its content using `curl` or `fetch`. This example highlights running a background process and interacting with services exposed via the sandbox's public URL. It also includes steps for killing the server process afterwards.\n\n```js\nimport { Sandbox } from '@e2b/code-interpreter'\n\nconst sandbox = await Sandbox.create()\n\n// Start a simple HTTP server inside the sandbox.\nconst process = await sandbox.commands.run('python -m http.server 3000', { background: true })\nconst host = sandbox.getHost(3000)\nconst url = `https://${host}`\nconsole.log('Server started at:', url)\n\n// Fetch data from the server inside the sandbox.\nconst response = await fetch(url);\nconst data = await response.text();\nconsole.log('Response from server inside sandbox:', data);\n\n// Kill the server process inside the sandbox.\nawait process.kill()\n```\n\n```python\nfrom e2b_code_interpreter import Sandbox\n\nsandbox = Sandbox.create()\n\n# Start a simple HTTP server inside the sandbox.\nprocess = sandbox.commands.run(\"python -m http.server 3000\", background=True)\nhost = sandbox.get_host(3000)\nurl = f\"https://{host}\"\nprint('Server started at:', url)\n\n# Fetch data from the server inside the sandbox.\nresponse = sandbox.commands.run(f\"curl {url}\")\ndata = response.stdout\nprint(\"Response from server inside sandbox:\", data)\n\n# Kill the server process inside the sandbox.\nprocess.kill()\n```\n\n--------------------------------\n\n### Get Public URL for E2B Sandbox Services (JS/Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sandbox/internet-access/page.mdx\n\nShows how to obtain the public URL for a running sandbox, which is necessary for accessing services hosted within it. A port number must always be provided to `getHost()` or `get_host()` to generate the correct URL. This is essential for external connectivity to sandbox-based applications.\n\n```js\nimport { Sandbox } from '@e2b/code-interpreter'\n\nconst sandbox = await Sandbox.create()\n\n// You need to always pass a port number to get the host\nconst host = sandbox.getHost(3000) // $HighlightLine\nconsole.log(`https://${host}`)\n```\n\n```python\nfrom e2b_code_interpreter import Sandbox\n\nsandbox = Sandbox.create()\n\n# You need to always pass a port number to get the host\nhost = sandbox.get_host(3000) # $HighlightLine\nprint(f'https://{host}')\n```\n\n--------------------------------\n\n### getHost()\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.5.2/sandbox/page.mdx\n\nGets the host address for a specified sandbox port.\n\n```APIDOC\n## getHost()\n\n### Description\nGet the host address for the specified sandbox port. This address can be used to connect to the sandbox port from outside the sandbox via HTTP or WebSocket.\n\n### Method\n`getHost(port: number): string`\n\n### Endpoint\nN/A (Method on Sandbox instance)\n\n### Parameters\n#### Path Parameters\nNone\n\n#### Query Parameters\nNone\n\n#### Request Body\nNone\n\n#### Parameters\n- **port** (`number`) - Required - The port number within the sandbox.\n\n### Request Example\n```ts\nconst sandbox = await Sandbox.create()\nawait sandbox.commands.exec('python3 -m http.server 3000')\nconst serverURL = sandbox.getHost(3000)\n```\n\n### Response\n#### Success Response\n- **hostAddress** (`string`) - The host address of the specified sandbox port.\n\n#### Response Example\n```\n\"http://localhost:8080\"\n```\n```\n\n--------------------------------\n\n### Get Host\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.2/sandbox/page.mdx\n\nRetrieves the host address for a specific port within the sandbox.\n\n```APIDOC\n## Get Sandbox Port Host\n\n### Description\nGets the external host address and port for accessing a service running inside the sandbox. This is useful for connecting to servers or applications exposed on specific ports within the sandbox.\n\n### Method\n`getHost(port: number): string`\n\n### Parameters\n#### Path Parameters\n- **port** (number) - Required - The port number within the sandbox for which to get the host address.\n\n### Request Example\n```ts\n// Assume an HTTP server is running on port 3000 inside the sandbox\nconst serverURL = sandbox.getHost(3000)\nconsole.log(`Access the server at: ${serverURL}`)\n```\n\n### Response\n#### Success Response (200)\n- **hostAddress** (string) - The external host address and port to access the sandbox service.\n\n#### Response Example\n```json\n\"http://localhost:3000\"\n```\n```\n\n--------------------------------\n\n### Get Host Address\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.10.0/sandbox/page.mdx\n\nObtains the host address for a specific port within the sandbox, allowing external connections.\n\n```APIDOC\n## Get Host Address\n\n### Description\nGet the host address for the specified sandbox port. You can then use this address to connect to the sandbox port from outside the sandbox via HTTP or WebSocket.\n\n### Method\n`sandbox.getHost(port: number): string`\n\n### Parameters\n#### Path Parameters\n- **port** (number) - Required - Number of the port in the sandbox.\n\n### Response\n#### Success Response (200)\n- **string** - Host address of the sandbox port.\n\n### Request Example\n```ts\nconst sandbox = await Sandbox.create()\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000')\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000)\n```\n```\n\n--------------------------------\n\n### Get Sandbox Host\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.7.0/sandbox/page.mdx\n\nObtains the host address to access a specific port within the sandbox from outside.\n\n```APIDOC\n## GET /sandbox/{sandboxId}/host\n\n### Description\nRetrieves the external host address and port mapping for a service running inside the sandbox on a given internal port.\n\n### Method\nGET\n\n### Endpoint\n/sandbox/{sandboxId}/host\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The ID of the sandbox.\n\n#### Query Parameters\n- **port** (number) - Required - The port number within the sandbox to get the host address for.\n\n### Request Example\n```bash\nGET /sandbox/sbx_abcdef123456/host?port=3000\n```\n\n### Response\n#### Success Response (200)\n- **host** (string) - The host address (e.g., a domain name or IP) to access the specified sandbox port.\n\n#### Response Example\n```json\n{\n  \"host\": \"http://localhost:12345\"\n}\n```\n```\n\n--------------------------------\n\n### Get Host Address\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.3.0/sandbox/page.mdx\n\nObtains the host address for a specific port within the sandbox, allowing external connections.\n\n```APIDOC\n## Get Host Address\n\n### Description\nRetrieves the external-facing host address and port for a service running inside the sandbox. This allows you to connect to ports exposed by processes within the sandbox.\n\n### Method\nGET (implicitly via SDK)\n\n### Endpoint\nN/A (SDK method)\n\n### Parameters\n#### Path Parameters\n- **`port`** (number) - Required - The port number within the sandbox for which to get the host address.\n\n### Request Example\n```ts\nconst sandbox = await Sandbox.create()\nawait sandbox.commands.exec('python3 -m http.server 3000')\nconst serverURL = sandbox.getHost(3000)\nconsole.log(serverURL)\n```\n\n### Response\n#### Success Response (200)\n- **hostAddress** (string) - The host address (e.g., `http://localhost:8080`) to access the service running on the specified port inside the sandbox.\n\n#### Response Example\n```json\n{\n  \"hostAddress\": \"http://localhost:8080\"\n}\n```\n```\n\n--------------------------------\n\n### Sandbox getHost()\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.13.1/sandbox/page.mdx\n\nResolves a port within the sandbox to its external host address.\n\n```APIDOC\n## GET /sandboxes/{sandboxId}/host\n\n### Description\nGets the host address for a specified sandbox port. This allows external connections to services running on that port within the sandbox.\n\n### Method\nGET\n\n### Endpoint\n/sandboxes/{sandboxId}/host\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The ID of the sandbox.\n\n#### Query Parameters\n- **port** (number) - Required - The port number within the sandbox.\n- **requestTimeoutMs** (number) - Optional - Timeout for the request.\n\n### Request Example\n```ts\nconst sandbox = await Sandbox.create()\n// Start an HTTP server in the sandbox on port 8080\nawait sandbox.commands.exec('python3 -m http.server 8080')\n\nconst host = sandbox.getHost(8080)\nconsole.log(`Access your server at: ${host}`)\n```\n\n### Response\n#### Success Response (200)\n- **host** (string) - The external host address (e.g., `http://localhost:3000` or `ws://localhost:3001`) for the specified sandbox port.\n\n#### Response Example\n```json\n{\n  \"host\": \"http://localhost:12345\"\n}\n```\n```\n\n--------------------------------\n\n### Get Sandbox Domain in Python\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/python-sdk/v1.10.0/sandbox_sync/page.mdx\n\nRetrieves the domain name where the sandbox is hosted. This is a read-only property.\n\n```python\n@property\ndef sandbox_domain() -> str\n```",
		"chunks": null,
		"metadata": {}
	},
	"cached_at": 1764196993.7901552,
	"params": {
		"owner": "e2b-dev",
		"repo": "e2b",
		"version": "latest",
		"topic": "get_host web server http",
		"tokens": 3000,
		"format": "txt"
	}
}
