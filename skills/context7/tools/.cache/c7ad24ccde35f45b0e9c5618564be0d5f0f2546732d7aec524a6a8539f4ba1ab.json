{
  "data": {
    "library": "/e2b-dev/e2b",
    "version": "latest",
    "topic": "getHost port url preview",
    "tokens": 3137,
    "content": "### getHost()\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.5.2/sandbox/page.mdx\n\nGets the host address for a specified sandbox port.\n\n```APIDOC\n## getHost()\n\n### Description\nGet the host address for the specified sandbox port. This address can be used to connect to the sandbox port from outside the sandbox via HTTP or WebSocket.\n\n### Method\n`getHost(port: number): string`\n\n### Endpoint\nN/A (Method on Sandbox instance)\n\n### Parameters\n#### Path Parameters\nNone\n\n#### Query Parameters\nNone\n\n#### Request Body\nNone\n\n#### Parameters\n- **port** (`number`) - Required - The port number within the sandbox.\n\n### Request Example\n```ts\nconst sandbox = await Sandbox.create()\nawait sandbox.commands.exec('python3 -m http.server 3000')\nconst serverURL = sandbox.getHost(3000)\n```\n\n### Response\n#### Success Response\n- **hostAddress** (`string`) - The host address of the specified sandbox port.\n\n#### Response Example\n```\n\"http://localhost:8080\"\n```\n```\n\n--------------------------------\n\n### Get Sandbox Host URL for a Port (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.8.0/sandbox/page.mdx\n\nThis function retrieves the host address for a specific port within the sandbox. This is useful for connecting to services running inside the sandbox from an external environment. It requires the sandbox instance and the port number as input, returning a string representing the host URL.\n\n```typescript\nasync function getSandboxHost(sandbox: Sandbox, port: number): Promise<string> {\n  return sandbox.getHost(port);\n}\n```\n\n--------------------------------\n\n### Sandbox getHost()\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.13.1/sandbox/page.mdx\n\nResolves a port within the sandbox to its external host address.\n\n```APIDOC\n## GET /sandboxes/{sandboxId}/host\n\n### Description\nGets the host address for a specified sandbox port. This allows external connections to services running on that port within the sandbox.\n\n### Method\nGET\n\n### Endpoint\n/sandboxes/{sandboxId}/host\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The ID of the sandbox.\n\n#### Query Parameters\n- **port** (number) - Required - The port number within the sandbox.\n- **requestTimeoutMs** (number) - Optional - Timeout for the request.\n\n### Request Example\n```ts\nconst sandbox = await Sandbox.create()\n// Start an HTTP server in the sandbox on port 8080\nawait sandbox.commands.exec('python3 -m http.server 8080')\n\nconst host = sandbox.getHost(8080)\nconsole.log(`Access your server at: ${host}`)\n```\n\n### Response\n#### Success Response (200)\n- **host** (string) - The external host address (e.g., `http://localhost:3000` or `ws://localhost:3001`) for the specified sandbox port.\n\n#### Response Example\n```json\n{\n  \"host\": \"http://localhost:12345\"\n}\n```\n```\n\n--------------------------------\n\n### Access Sandbox Port with getHost\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.1.0/sandbox/page.mdx\n\nDemonstrates how to get the host address for a specific port running inside the sandbox. This allows external connections to services hosted within the sandbox, such as HTTP servers.\n\n```typescript\nconst sandbox = await Sandbox.create();\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000');\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000);\n```\n\n--------------------------------\n\n### Get Public URL for E2B Sandbox Services (JS/Python)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sandbox/internet-access/page.mdx\n\nShows how to obtain the public URL for a running sandbox, which is necessary for accessing services hosted within it. A port number must always be provided to `getHost()` or `get_host()` to generate the correct URL. This is essential for external connectivity to sandbox-based applications.\n\n```js\nimport { Sandbox } from '@e2b/code-interpreter'\n\nconst sandbox = await Sandbox.create()\n\n// You need to always pass a port number to get the host\nconst host = sandbox.getHost(3000) // $HighlightLine\nconsole.log(`https://${host}`)\n```\n\n```python\nfrom e2b_code_interpreter import Sandbox\n\nsandbox = Sandbox.create()\n\n# You need to always pass a port number to get the host\nhost = sandbox.get_host(3000) # $HighlightLine\nprint(f'https://{host}')\n```\n\n--------------------------------\n\n### Get host address for a sandbox port\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.13.1/sandbox/page.mdx\n\nObtains the host address for a specified port within the sandbox. This allows external connections to services running on that port inside the sandbox, such as HTTP or WebSocket servers. The example shows how to start a Python HTTP server and then get its accessible URL.\n\n```typescript\nimport { Sandbox } from 'e2b';\n\nconst sandbox = await Sandbox.create();\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000');\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000);\n```\n\n--------------------------------\n\n### Get Sandbox Port Host Address\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.3/sandbox/page.mdx\n\nObtains the host address for a given port within the sandbox. This allows external connections to services running on specific ports inside the sandbox, such as HTTP servers or WebSocket endpoints. It requires the port number as input.\n\n```typescript\ngetHost(port: number): string\n\n// Example:\nconst sandbox = await Sandbox.create()\nawait sandbox.commands.exec('python3 -m http.server 3000')\nconst serverURL = sandbox.getHost(3000)\n```\n\n--------------------------------\n\n### Get Sandbox Port Host Address (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.7/sandbox/page.mdx\n\nThis function returns the host address for a given port within the sandbox. This is essential for accessing services running on specific ports inside the sandbox from outside. It takes the port number as input and returns the host address string.\n\n```typescript\nconst serverURL = sandbox.getHost(3000)\n```\n\n--------------------------------\n\n### Get Sandbox File Download URL (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.7/sandbox/page.mdx\n\nThis method retrieves a pre-signed URL for downloading a specific file from the sandbox. The URL can be used to fetch the file content from the sandbox environment. It takes the file path as a parameter and returns the download URL as a string.\n\n```typescript\nconst downloadUrl = sandbox.downloadUrl('/path/to/your/file')\n```\n\n--------------------------------\n\n### Get Upload URL\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.1.3/sandbox/page.mdx\n\nRetrieves a URL to upload a file to the sandbox. The file should be sent as a POST request with multipart/form-data to this URL.\n\n```APIDOC\n## uploadUrl()\n\n### Description\nGets the URL to upload a file to the sandbox. You have to send a POST request to this URL with the file as multipart/form-data.\n\n### Method\nGET\n\n### Endpoint\n`/sandbox/{sandboxId}/uploadUrl`\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The ID of the sandbox.\n\n#### Query Parameters\n- **path** (string) - Optional - The path to the file within the sandbox.\n- **opts** (object) - Optional - Upload URL options.\n\n### Request Example\n```json\n{\n  \"path\": \"/app/file.txt\",\n  \"opts\": {}\n}\n```\n\n### Response\n#### Success Response (200)\n- **uploadUrl** (string) - The URL for uploading the file.\n\n#### Response Example\n```json\n{\n  \"uploadUrl\": \"https://e2b.dev/upload/xyz123\"\n}\n```\n```\n\n--------------------------------\n\n### Get Download URL for a File\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.4/sandbox/page.mdx\n\nRetrieves a URL to download a specific file from the sandbox. This function takes the file path as input and returns a string representing the download URL.\n\n```typescript\nconst downloadUrl = sandbox.downloadUrl('/path/to/file')\n```\n\n--------------------------------\n\n### Sandbox Port Host\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.7/sandbox/page.mdx\n\nGet the host address for a specific sandbox port.\n\n```APIDOC\n## GET /sandbox/{sandboxId}/getHost\n\n### Description\nGets the host address for a specified port within the sandbox. This address can be used to connect to the sandbox port from outside the sandbox via HTTP or WebSocket.\n\n### Method\nGET\n\n### Endpoint\n/sandbox/{sandboxId}/getHost\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The ID of the sandbox.\n\n#### Query Parameters\n- **port** (number) - Required - The port number within the sandbox to get the host address for.\n\n### Response\n#### Success Response (200)\n- **hostAddress** (string) - The host address of the specified sandbox port.\n\n#### Response Example\n```json\n{\n  \"hostAddress\": \"http://localhost:8080\"\n}\n```\n```\n\n--------------------------------\n\n### E2B Sandbox: Get Video Stream URL\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/desktop-js-sdk/v1.0.1/sandbox/page.mdx\n\nRetrieves the URL for the video stream. Returns a Promise that resolves with a string representing the URL.\n\n```typescript\ngetVideoStreamUrl(): Promise<string>\n```\n\n--------------------------------\n\n### Get Sandbox Port Host\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.7.1/sandbox/page.mdx\n\nObtain the host address to access a specific port running inside the sandbox.\n\n```APIDOC\n## Get Sandbox Port Host\n\n### Description\nRetrieve the host address that allows external access to a specific network port running within the sandbox. This is useful for connecting to services like HTTP servers or WebSocket servers hosted inside the sandbox.\n\n### Method\n`getHost(port: number): string`\n\n### Endpoint\nN/A (Client-side SDK method)\n\n### Parameters\n#### Path Parameters\n- **port** (number) - Required - The port number running inside the sandbox.\n\n### Request Example\n```ts\nconst sandbox = await Sandbox.create()\n// Start an HTTP server on port 3000\nawait sandbox.commands.exec('python3 -m http.server 3000')\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000)\nconsole.log(`Connect to: http://${serverURL}`)\n```\n\n### Response\n#### Success Response (200)\n- **string** - The host address (e.g., `localhost:XXXXX`) for accessing the specified sandbox port.\n\n#### Response Example\n```\n\"localhost:12345\"\n```\n```\n\n--------------------------------\n\n### Get Sandbox Port Host Address\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.11.0/sandbox/page.mdx\n\nObtains the host address for a specific port exposed by the sandbox. This allows external connections to services running within the sandbox, such as HTTP servers or WebSockets. Requires the port number.\n\n```typescript\ngetHost(port: number): string\n\n// Example:\nconst sandbox = await Sandbox.create()\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000')\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000)\n```\n\n--------------------------------\n\n### Get Sandbox Host Port\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.1.5/sandbox/page.mdx\n\nRetrieves the host address for a specific port within the sandbox.\n\n```APIDOC\n## GET /sandbox/{sandboxId}/host?port={port}\n\n### Description\nGets the host address for a specific port exposed by the sandbox. This allows external services to connect to processes running inside the sandbox on the specified port.\n\n### Method\nGET\n\n### Endpoint\n/sandbox/{sandboxId}/host\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The unique identifier of the sandbox.\n\n#### Query Parameters\n- **port** (number) - Required - The port number within the sandbox to get the host address for.\n\n### Response\n#### Success Response (200)\n- **host** (string) - The host address (e.g., URL or IP address) to access the specified port from outside the sandbox.\n\n#### Response Example\n```json\n{\n  \"host\": \"http://localhost:8080\"\n}\n```\n```\n\n--------------------------------\n\n### Get Sandbox Port Host\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.5.3/sandbox/page.mdx\n\nRetrieves the host address for a specific port within the sandbox.\n\n```APIDOC\n## GET /sandboxes/{sandboxId}/host\n\n### Description\nGets the host address for a specified sandbox port. This address can be used to connect to the port from outside the sandbox.\n\n### Method\nGET\n\n### Endpoint\n/sandboxes/{sandboxId}/host\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The unique identifier of the sandbox.\n\n#### Query Parameters\n- **port** (number) - Required - The port number within the sandbox.\n\n### Response\n#### Success Response (200)\n- **host** (string) - The host address for the specified sandbox port.\n\n#### Response Example\n```json\n{\n  \"host\": \"http://localhost:8080\"\n}\n```\n```\n\n--------------------------------\n\n### Get Upload URL\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.0.0/sandbox/page.mdx\n\nRetrieves a URL for uploading a file to a specific path within the sandbox. The file should be sent as a POST request with multipart/form-data to this URL.\n\n```APIDOC\n## uploadUrl()\n\n### Description\nRetrieves a URL for uploading a file to a specific path within the sandbox. The file should be sent as a POST request with multipart/form-data to this URL.\n\n### Method\nGET\n\n### Endpoint\n`/sandboxes/{sandboxId}/upload-url`\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The ID of the sandbox.\n\n#### Query Parameters\n- **path** (string) - Optional - The destination path for the file within the sandbox.\n- **opts** (object) - Optional - Options for the upload URL.\n  - (Specific options for `SandboxUrlOpts` would be detailed here if known)\n\n### Request Example\n(No request body for GET request)\n\n### Response\n#### Success Response (200)\n- **uploadUrl** (string) - The URL to upload the file to.\n\n#### Response Example\n```json\n{\n  \"uploadUrl\": \"https://e2b.com/upload/signed-url-for-file\"\n}\n```\n```\n\n--------------------------------\n\n### Get Sandbox Host Port\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.5.1/sandbox/page.mdx\n\nRetrieve the host address for a specific port within the sandbox.\n\n```APIDOC\n## GET /sandboxes/{sandboxId}/ports/{port}\n\n### Description\nGets the host address for a specified sandbox port. This address can be used to connect to services running on that port from outside the sandbox.\n\n### Method\nGET\n\n### Endpoint\n/sandboxes/{sandboxId}/ports/{port}\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The unique identifier of the sandbox.\n- **port** (number) - Required - The port number within the sandbox.\n\n#### Query Parameters\nNone\n\n#### Request Body\nNone\n\n### Request Example\nNone\n\n### Response\n#### Success Response (200 OK)\n- **host** (string) - The host address for the specified sandbox port.\n\n#### Response Example\n```json\n{\n  \"host\": \"http://localhost:12345\"\n}\n```\n```\n\n--------------------------------\n\n### Sandbox Port Host\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v2.0.0/sandbox/page.mdx\n\nGet the host address for a specific sandbox port.\n\n```APIDOC\n## GET /sandbox/{sandbox_id}/host/{port}\n\n### Description\nGets the host address for the specified sandbox port. This address can be used to connect to the sandbox port from outside the sandbox via HTTP or WebSocket.\n\n### Method\nGET\n\n### Endpoint\n/sandbox/{sandbox_id}/host/{port}\n\n### Parameters\n#### Path Parameters\n- **sandbox_id** (string) - Required - The unique identifier of the sandbox.\n- **port** (number) - Required - The port number within the sandbox.\n\n### Response\n#### Success Response (200)\n- **host** (string) - The host address of the sandbox port.\n\n#### Response Example\n```json\n{\n  \"host\": \"http://localhost:8080\"\n}\n```\n```\n\n--------------------------------\n\n### Get File Download URL\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.3.0/sandbox/page.mdx\n\nRetrieves a pre-signed URL to download a specific file from the sandbox. This URL is temporary and can be used by external applications to fetch files.\n\n```typescript\nconst downloadUrl = sandbox.downloadUrl('/path/to/your/file.txt');\n```\n\n--------------------------------\n\n### Get Host\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.2/sandbox/page.mdx\n\nRetrieves the host address for a specific port within the sandbox.\n\n```APIDOC\n## Get Sandbox Port Host\n\n### Description\nGets the external host address and port for accessing a service running inside the sandbox. This is useful for connecting to servers or applications exposed on specific ports within the sandbox.\n\n### Method\n`getHost(port: number): string`\n\n### Parameters\n#### Path Parameters\n- **port** (number) - Required - The port number within the sandbox for which to get the host address.\n\n### Request Example\n```ts\n// Assume an HTTP server is running on port 3000 inside the sandbox\nconst serverURL = sandbox.getHost(3000)\nconsole.log(`Access the server at: ${serverURL}`)\n```\n\n### Response\n#### Success Response (200)\n- **hostAddress** (string) - The external host address and port to access the sandbox service.\n\n#### Response Example\n```json\n\"http://localhost:3000\"\n```\n```\n\n--------------------------------\n\n### Sandbox Port Hostname\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.11.0/sandbox/page.mdx\n\nObtains the external host address for a specific port within the sandbox.\n\n```APIDOC\n## GET /sandboxes/{sandboxId}/host\n\n### Description\nGet the host address for the specified sandbox port. This address can be used to connect to the sandbox port from outside the sandbox via HTTP or WebSocket.\n\n### Method\nGET\n\n### Endpoint\n/sandboxes/{sandboxId}/host\n\n### Parameters\n#### Query Parameters\n- **port** (number) - Required - The port number within the sandbox.\n\n### Response\n#### Success Response (200)\n- **host** (string) - The host address of the specified sandbox port.\n\n#### Response Example\n```typescript\nconst sandbox = await Sandbox.create()\n// Start an HTTP server\nawait sandbox.commands.exec('python3 -m http.server 3000')\n// Get the hostname of the HTTP server\nconst serverURL = sandbox.getHost(3000)\nconsole.log(serverURL) // e.g., \"http://localhost:8080\"\n```\n```\n\n--------------------------------\n\n### Get Download URL for a File (TypeScript)\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.0.4/sandbox/page.mdx\n\nThis code shows how to retrieve a URL for downloading a specific file from the sandbox. The `downloadUrl` method requires the path to the file within the sandbox and returns a string representing the download URL.\n\n```typescript\nconst sandbox = await Sandbox.create()\nconst downloadURL = sandbox.downloadUrl('/home/user/my_file.txt')\n```\n\n--------------------------------\n\n### Get Sandbox Port Host\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.12.0/sandbox/page.mdx\n\nObtains the host address to connect to a specific port within the sandbox.\n\n```APIDOC\n## GET /sandbox/{sandboxId}/host\n\n### Description\nRetrieves the external host address and port to connect to a service running on a specific port inside the sandbox. This is useful for accessing web servers or other network services started within the sandbox from outside.\n\n### Method\nGET\n\n### Endpoint\n/sandbox/{sandboxId}/host\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The ID of the sandbox.\n\n#### Query Parameters\n- **port** (number) - Required - The port number within the sandbox for which to get the host address.\n\n### Request Example\n```typescript\nconst sandbox = await Sandbox.create()\n// Start an HTTP server on port 3000 inside the sandbox\nawait sandbox.commands.exec('python3 -m http.server 3000')\n// Get the host address to access the server from outside\nconst serverURL = sandbox.getHost(3000)\nconsole.log(`Access the server at: http://${serverURL}`)\n```\n\n### Response\n#### Success Response (200)\n- **host** (string) - The host address (e.g., 'localhost' or a specific IP) and potentially a forwarded port to access the sandbox service.\n\n#### Response Example\n```json\n{\n  \"host\": \"localhost:34567\"\n}\n```\n```\n\n--------------------------------\n\n### Sandbox Upload URL\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.9.1/sandbox/page.mdx\n\nRetrieves a URL to upload a file to the sandbox.\n\n```APIDOC\n## POST /sandbox/{sandboxId}/upload\n\n### Description\nGets the URL to upload a file to the sandbox. You must send a POST request to this URL with the file as multipart/form-data.\n\n### Method\nPOST\n\n### Endpoint\n/sandbox/{sandboxId}/upload\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The ID of the sandbox.\n\n#### Query Parameters\n- **path** (string) - Optional - The destination path for the file in the sandbox.\n- **opts** (SandboxUrlOpts) - Optional - Additional options for the upload URL.\n\n### Request Example\n```ts\nimport { Sandbox } from 'e2b'\n\nconst sandbox = await Sandbox.create();\nconst uploadUrl = await sandbox.uploadUrl('/app/data.json');\n// Use fetch or another HTTP client to POST the file to uploadUrl\n```\n\n### Response\n#### Success Response (200)\n- **url** (string) - The URL for uploading the file.\n\n#### Response Example\n```json\n{\n  \"url\": \"https://e2b-upload-url.com/path/to/upload\"\n}\n```\n```\n\n--------------------------------\n\n### Get URL to Upload File to Sandbox\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.5.3/sandbox/page.mdx\n\nProvides a URL for uploading files into the sandbox. A POST request with the file as multipart/form-data should be sent to this URL.\n\n```typescript\nuploadUrl(path?: string, opts?: SandboxUrlOpts): string\n```\n\n--------------------------------\n\n### Sandbox Download URL\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.9.1/sandbox/page.mdx\n\nRetrieves a URL to download a file from the sandbox.\n\n```APIDOC\n## GET /sandbox/{sandboxId}/download\n\n### Description\nGets the URL to download a specified file from the sandbox.\n\n### Method\nGET\n\n### Endpoint\n/sandbox/{sandboxId}/download\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The ID of the sandbox.\n\n#### Query Parameters\n- **path** (string) - Required - The path to the file within the sandbox.\n- **opts** (SandboxUrlOpts) - Optional - Additional options for the download URL.\n\n### Request Example\n```ts\nimport { Sandbox } from 'e2b'\n\nconst sandbox = await Sandbox.create();\nconst downloadUrl = await sandbox.downloadUrl('/app/output.log');\nconsole.log(downloadUrl);\n```\n\n### Response\n#### Success Response (200)\n- **url** (string) - The URL for downloading the file.\n\n#### Response Example\n```json\n{\n  \"url\": \"https://e2b-download-url.com/path/to/file\"\n}\n```\n```\n\n--------------------------------\n\n### Sandbox Port Host Resolution API\n\nSource: https://github.com/e2b-dev/e2b/blob/main/apps/web/src/app/(docs)/docs/sdk-reference/js-sdk/v1.2.0/sandbox/page.mdx\n\nAPI to get the host address for a specific port within the sandbox, enabling external connections.\n\n```APIDOC\n## GET /sandbox/{sandboxId}/get-host\n\n### Description\nRetrieves the host address for a given port within the sandbox. This allows you to connect to services running on that port from outside the sandbox.\n\n### Method\nGET\n\n### Endpoint\n/sandbox/{sandboxId}/get-host\n\n### Parameters\n#### Path Parameters\n- **sandboxId** (string) - Required - The unique identifier of the sandbox.\n\n#### Query Parameters\n- **port** (number) - Required - The port number within the sandbox for which to get the host address.\n\n### Request Body\n(Not applicable for this endpoint)\n\n### Request Example\n```ts\nimport { Sandbox } from 'e2b'\n\nconst sandbox = await Sandbox.create();\n// Assume a server is started on port 3000 inside the sandbox\nawait sandbox.commands.exec('python3 -m http.server 3000 &');\nconst serverHost = sandbox.getHost(3000);\nconsole.log(`Connect to server at: ${serverHost}`);\n```\n\n### Response\n#### Success Response (200)\n- **host** (string) - The host address (e.g., an IP address or hostname) to access the specified port from outside the sandbox.\n\n#### Response Example\n```json\n{\n  \"host\": \"127.0.0.1:3000\"\n}\n```\n```",
    "chunks": null,
    "metadata": {}
  },
  "cached_at": 1766086668.2283316,
  "params": {
    "owner": "e2b-dev",
    "repo": "e2b",
    "version": "latest",
    "topic": "getHost port url preview",
    "tokens": 5000,
    "format": "txt"
  }
}