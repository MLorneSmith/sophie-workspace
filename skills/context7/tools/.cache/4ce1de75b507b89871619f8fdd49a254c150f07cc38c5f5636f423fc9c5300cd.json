{
  "data": {
    "library": "/anthropics/claude-agent-sdk-python",
    "version": "latest",
    "topic": "orchestration multi-agent coordination",
    "tokens": 3983,
    "content": "### Handle Concurrent Message Processing with Claude Agent SDK Python\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis snippet illustrates how to concurrently send messages to and receive responses from the Claude Agent SDK using Python's `asyncio`. It sets up a background task to continuously listen for assistant messages while the main task sends multiple user queries with delays, enabling responsive, non-blocking interactions.\n\n```python\nimport asyncio\nfrom claude_agent_sdk import ClaudeSDKClient, AssistantMessage, TextBlock\n\nasync def main():\n    async with ClaudeSDKClient() as client:\n        # Background task to continuously receive messages\n        async def receive_messages():\n            async for message in client.receive_messages():\n                if isinstance(message, AssistantMessage):\n                    for block in message.content:\n                        if isinstance(block, TextBlock):\n                            print(f\"Claude: {block.text}\")\n\n        # Start receiving in background\n        receive_task = asyncio.create_task(receive_messages())\n\n        # Send multiple messages with delays\n        questions = [\n            \"What is 2 + 2?\",\n            \"What is the square root of 144?\",\n            \"What is 10% of 80?\"\n        ]\n\n        for question in questions:\n            print(f\"User: {question}\")\n            await client.query(question)\n            await asyncio.sleep(3)\n\n        # Clean up\n        await asyncio.sleep(2)\n        receive_task.cancel()\n        try:\n            await receive_task\n        except asyncio.CancelledError:\n            pass\n\nasyncio.run(main)\n```\n\n--------------------------------\n\n### Implement a Stateful Data Store with Claude Agent SDK Tools (Python)\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis snippet defines a `DataStore` class to manage application state (a list of items and a counter). It then exposes `add_item` and `list_items` as `tool` functions using the `claude-agent-sdk` decorator, allowing an agent to interact with the store. The `main` function demonstrates initializing the SDK client with these tools and querying the agent to add and list items.\n\n```python\nclass DataStore:\n    def __init__(self):\n        self.items: list[str] = []\n        self.counter: int = 0\n\nstore = DataStore()\n\n@tool(\"add_item\", \"Add an item to the store\", {\"item\": str})\nasync def add_item(args: dict[str, Any]) -> dict[str, Any]:\n    store.items.append(args[\"item\"])\n    store.counter += 1\n    return {\n        \"content\": [{\n            \"type\": \"text\",\n            \"text\": f\"Added '{args['item']}'. Total items: {store.counter}\"\n        }]\n    }\n\n@tool(\"list_items\", \"List all items in the store\", {})\nasync def list_items(args: dict[str, Any]) -> dict[str, Any]:\n    if not store.items:\n        return {\"content\": [{\"type\": \"text\", \"text\": \"Store is empty\"}]}\n    items_text = \"\\n\".join(f\"- {item}\" for item in store.items)\n    return {\n        \"content\": [{\"type\": \"text\", \"text\": f\"Items:\\n{items_text}\"}]\n    }\n\nasync def main():\n    server = create_sdk_mcp_server(\"store\", tools=[add_item, list_items])\n    options = ClaudeAgentOptions(\n        mcp_servers={\"store\": server},\n        allowed_tools=[\"mcp__store__add_item\", \"mcp__store__list_items\"]\n    )\n\n    async with ClaudeSDKClient(options=options) as client:\n        await client.query(\"Add 'apple' and 'banana' to the store, then list all items\")\n        async for msg in client.receive_response():\n            # Process messages...\n            pass\n\nimport asyncio\nasyncio.run(main)\n```\n\n--------------------------------\n\n### Configure Mixed SDK and External MCP Servers\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis Python example demonstrates how to configure `ClaudeAgentOptions` to use both in-process SDK MCP servers and external subprocess MCP servers simultaneously. This allows for flexible tool integration where some tools run within the application and others as separate processes.\n\n```python\noptions = ClaudeAgentOptions(\n    mcp_servers={\n        \"internal\": sdk_server,      # In-process SDK server\n        \"external\": {                # External subprocess server\n            \"type\": \"stdio\",\n            \"command\": \"external-server\"\n        }\n    }\n)\n```\n\n--------------------------------\n\n### Manage Interactive Conversations with ClaudeSDKClient in Python\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis snippet illustrates how to use `ClaudeSDKClient` for stateful, multi-turn conversations with Claude. It demonstrates basic conversational flow with context management and handling follow-up questions. It also shows how to configure the client with `ClaudeAgentOptions` for settings like allowed tools, budget limits, thinking token limits, and environment variables, along with manual message processing.\n\n```python\nimport asyncio\nfrom claude_agent_sdk import (\n    ClaudeSDKClient,\n    ClaudeAgentOptions,\n    AssistantMessage,\n    TextBlock,\n    ResultMessage,\n    UserMessage,\n    ToolUseBlock\n)\n\nasync def main():\n    # Basic usage with context manager\n    async with ClaudeSDKClient() as client:\n        # First turn\n        await client.query(\"What's the capital of France?\")\n        async for msg in client.receive_response():\n            if isinstance(msg, AssistantMessage):\n                for block in msg.content:\n                    if isinstance(block, TextBlock):\n                        print(f\"Claude: {block.text}\")\n\n        # Follow-up question in same session\n        await client.query(\"What's the population of that city?\")\n        async for msg in client.receive_response():\n            if isinstance(msg, AssistantMessage):\n                for block in msg.content:\n                    if isinstance(block, TextBlock):\n                        print(f\"Claude: {block.text}\")\n\n    # With configuration options\n    options = ClaudeAgentOptions(\n        allowed_tools=[\"Read\", \"Write\", \"Bash\"],\n        system_prompt=\"You are a helpful assistant.\",\n        max_budget_usd=5.0,  # Stop after spending $5\n        max_thinking_tokens=10000,  # Limit extended thinking\n        env={\"ANTHROPIC_MODEL\": \"claude-sonnet-4-5\"}\n    )\n\n    async with ClaudeSDKClient(options=options) as client:\n        await client.query(\"Analyze this codebase and suggest improvements\")\n\n        # Manual message handling with custom logic\n        async for message in client.receive_messages():\n            if isinstance(message, AssistantMessage):\n                for block in message.content:\n                    if isinstance(block, TextBlock):\n                        print(f\"Response: {block.text}\")\n                    elif isinstance(block, ToolUseBlock):\n                        print(f\"Using tool: {block.name}\")\n                        print(f\"Input: {block.input}\")\n            elif isinstance(message, ResultMessage):\n                print(f\"Total cost: ${message.total_cost_usd:.4f}\")\n                break\n\nasyncio.run(main)\n```\n\n--------------------------------\n\n### Define and Use Pre-ToolUse Hooks in Claude Agent SDK (Python)\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis Python code demonstrates how to define an asynchronous hook function to intercept and evaluate tool use requests before they are executed by the Claude agent. The example hook checks for forbidden patterns in Bash commands and denies execution if found, providing deterministic control over agent actions. It leverages `claude_agent_sdk` components like `ClaudeAgentOptions`, `ClaudeSDKClient`, and `HookMatcher`.\n\n```python\nfrom claude_agent_sdk import ClaudeAgentOptions, ClaudeSDKClient, HookMatcher\n\nasync def check_bash_command(input_data, tool_use_id, context):\n    tool_name = input_data[\"tool_name\"]\n    tool_input = input_data[\"tool_input\"]\n    if tool_name != \"Bash\":\n        return {}\n    command = tool_input.get(\"command\", \"\")\n    block_patterns = [\"foo.sh\"]\n    for pattern in block_patterns:\n        if pattern in command:\n            return {\n                \"hookSpecificOutput\": {\n                    \"hookEventName\": \"PreToolUse\",\n                    \"permissionDecision\": \"deny\",\n                    \"permissionDecisionReason\": f\"Command contains invalid pattern: {pattern}\",\n                }\n            }\n    return {}\n\noptions = ClaudeAgentOptions(\n    allowed_tools=[\"Bash\"],\n    hooks={\n        \"PreToolUse\": [\n            HookMatcher(matcher=\"Bash\", hooks=[check_bash_command]),\n        ],\n    }\n)\n\nasync with ClaudeSDKClient(options=options) as client:\n    # Test 1: Command with forbidden pattern (will be blocked)\n    await client.query(\"Run the bash command: ./foo.sh --help\")\n    async for msg in client.receive_response():\n        print(msg)\n\n    print(\"\\n\" + \"=\" * 50 + \"\\n\")\n\n    # Test 2: Safe command that should work\n    await client.query(\"Run the bash command: echo 'Hello from hooks example!'\")\n    async for msg in client.receive_response():\n        print(msg)\n```\n\n--------------------------------\n\n### Quick Start with Claude Agent SDK Query\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis Python snippet demonstrates a basic asynchronous query to Claude Agent using the `query` function from the SDK. It uses `anyio` to run the async function and prints each message received from Claude.\n\n```python\nimport anyio\nfrom claude_agent_sdk import query\n\nasync def main():\n    async for message in query(prompt=\"What is 2 + 2?\"):\n        print(message)\n\nanyio.run(main)\n```\n\n--------------------------------\n\n### Configure ClaudeAgentOptions in Python\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis snippet demonstrates how to initialize `ClaudeAgentOptions` with a wide range of settings, including tool permissions, system prompts, working directory, execution limits, model selection, environment variables, MCP servers, hooks, and custom agents. These options control the behavior of the Claude agent during interaction.\n\n```python\nfrom pathlib import Path\nfrom claude_agent_sdk import ClaudeAgentOptions, query\n\nasync def main():\n    options = ClaudeAgentOptions(\n        # Tools and permissions\n        allowed_tools=[\"Read\", \"Write\", \"Bash\", \"Edit\", \"Glob\", \"Grep\"],\n        permission_mode=\"acceptEdits\",  # \"default\" | \"acceptEdits\" | \"plan\" | \"bypassPermissions\"\n\n        # System behavior\n        system_prompt=\"You are a helpful Python coding assistant.\",\n        # Or use preset: system_prompt={\"type\": \"preset\", \"preset\": \"claude_code\", \"append\": \"Extra instructions\"}\n\n        # Working directory\n        cwd=Path(\"/path/to/project\"),  # Or string path\n\n        # Execution limits\n        max_turns=10,  # Maximum agent turns\n        max_budget_usd=5.0,  # Stop after spending $5\n        max_thinking_tokens=10000,  # Limit extended thinking\n\n        # Model selection\n        model=\"claude-sonnet-4-5\",\n\n        # Environment variables\n        env={\"CUSTOM_VAR\": \"value\"},\n\n        # MCP servers\n        mcp_servers={\n            \"my_tools\": sdk_server_instance,\n            \"external\": {\n                \"type\": \"stdio\",\n                \"command\": \"python\",\n                \"args\": [\"-m\", \"external_server\"]\n            }\n        },\n\n        # Hooks\n        hooks={\n            \"PreToolUse\": [HookMatcher(matcher=\"Bash\", hooks=[validation_hook])],\n            \"PostToolUse\": [HookMatcher(matcher=None, hooks=[review_hook])]\n        },\n\n        # Permission callback\n        can_use_tool=permission_callback,\n\n        # Settings sources\n        setting_sources=[\"user\", \"project\"],  # Load from specific locations\n\n        # Streaming\n        include_partial_messages=True,  # Get partial messages during streaming\n\n        # Session management\n        fork_session=\"previous_session_id\",  # Resume from previous session\n\n        # Custom agents\n        agents={\n            \"reviewer\": {\n                \"description\": \"Code reviewer agent\",\n                \"prompt\": \"Review code for issues\",\n                \"tools\": [\"Read\", \"Grep\"],\n                \"model\": \"sonnet\"\n            }\n        },\n\n        # Plugins\n        plugins=[\n            {\"type\": \"local\", \"path\": \"/path/to/plugin\"}\n        ]\n    )\n\n    async for message in query(prompt=\"Analyze this project\", options=options):\n        # Process messages\n        pass\n\nimport anyio\nanyio.run(main)\n```\n\n--------------------------------\n\n### Define and Use Custom In-Process Tools with Claude Agent SDK (Python)\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nIllustrates the creation of custom mathematical tools (`add`, `multiply`, `divide`) using the `@tool` decorator for the Claude Agent SDK. These tools run directly within the Python application's process, allowing Claude to invoke them and access application state efficiently.\n\n```python\nimport asyncio\nfrom typing import Any\nfrom claude_agent_sdk import (\n    tool,\n    create_sdk_mcp_server,\n    ClaudeAgentOptions,\n    ClaudeSDKClient,\n    AssistantMessage,\n    TextBlock,\n    ToolUseBlock\n)\n\n# Define tools with @tool decorator\n@tool(\"add\", \"Add two numbers\", {\"a\": float, \"b\": float})\nasync def add_numbers(args: dict[str, Any]) -> dict[str, Any]:\n    result = args[\"a\"] + args[\"b\"]\n    return {\n        \"content\": [{\"type\": \"text\", \"text\": f\"{args['a']} + {args['b']} = {result}\"}]\n    }\n\n@tool(\"multiply\", \"Multiply two numbers\", {\"a\": float, \"b\": float})\nasync def multiply_numbers(args: dict[str, Any]) -> dict[str, Any]:\n    result = args[\"a\"] * args[\"b\"]\n    return {\n        \"content\": [{\"type\": \"text\", \"text\": f\"{args['a']} \u00d7 {args['b']} = {result}\"}]\n    }\n\n@tool(\"divide\", \"Divide one number by another\", {\"a\": float, \"b\": float})\nasync def divide_numbers(args: dict[str, Any]) -> dict[str, Any]:\n    if args[\"b\"] == 0:\n        return {\n            \"content\": [{\"type\": \"text\", \"text\": \"Error: Division by zero\"}],\n            \"is_error\": True\n        }\n    result = args[\"a\"] / args[\"b\"]\n    return {\n        \"content\": [{\"type\": \"text\", \"text\": f\"{args['a']} \u00f7 {args['b']} = {result}\"}]\n    }\n\nasync def main():\n    # Create SDK MCP server\n    calculator = create_sdk_mcp_server(\n        name=\"calculator\",\n        version=\"1.0.0\",\n        tools=[add_numbers, multiply_numbers, divide_numbers]\n    )\n\n    # Configure Claude to use the server\n    options = ClaudeAgentOptions(\n        mcp_servers={\"calc\": calculator},\n        allowed_tools=[\n            \"mcp__calc__add\",\n            \"mcp__calc__multiply\",\n            \"mcp__calc__divide\"\n        ]\n    )\n\n    async with ClaudeSDKClient(options=options) as client:\n        await client.query(\"Calculate (12 + 8) * 3 - 10\")\n\n        async for message in client.receive_response():\n            if isinstance(message, AssistantMessage):\n                for block in message.content:\n                    if isinstance(block, TextBlock):\n                        print(f\"Claude: {block.text}\")\n                    elif isinstance(block, ToolUseBlock):\n                        print(f\"Using tool: {block.name} with {block.input}\")\n\nasyncio.run(main)\n```\n\n--------------------------------\n\n### Configure Claude Agent Query with Tools\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis Python snippet demonstrates how to configure the `query` function to allow Claude to use specific tools and set a permission mode. The `allowed_tools` parameter restricts which tools Claude can access, and `permission_mode='acceptEdits'` allows auto-acceptance of file edits.\n\n```python\noptions = ClaudeAgentOptions(\n    allowed_tools=[\"Read\", \"Write\", \"Bash\"],\n    permission_mode='acceptEdits'  # auto-accept file edits\n)\n\nasync for message in query(\n    prompt=\"Create a hello.py file\",\n    options=options\n):\n    # Process tool use and results\n    pass\n```\n\n--------------------------------\n\n### Create and Use a Custom Tool with ClaudeSDKClient\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis advanced Python example demonstrates how to define a custom tool using the `@tool` decorator, create an in-process SDK MCP server from it, and integrate it with `ClaudeSDKClient`. The client then queries Claude, enabling it to invoke the custom 'greet' tool and process the response.\n\n```python\nfrom claude_agent_sdk import tool, create_sdk_mcp_server, ClaudeAgentOptions, ClaudeSDKClient\n\n# Define a tool using the @tool decorator\n@tool(\"greet\", \"Greet a user\", {\"name\": str})\nasync def greet_user(args):\n    return {\n        \"content\": [\n            {\"type\": \"text\", \"text\": f\"Hello, {args['name']}!\"}\n        ]\n    }\n\n# Create an SDK MCP server\nserver = create_sdk_mcp_server(\n    name=\"my-tools\",\n    version=\"1.0.0\",\n    tools=[greet_user]\n)\n\n# Use it with Claude\noptions = ClaudeAgentOptions(\n    mcp_servers={\"tools\": server},\n    allowed_tools=[\"mcp__tools__greet\"]\n)\n\nasync with ClaudeSDKClient(options=options) as client:\n    await client.query(\"Greet Alice\")\n\n    # Extract and print response\n    async for msg in client.receive_response():\n        print(msg)\n```\n\n--------------------------------\n\n### Basic Claude Agent Query Usage\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis Python example shows how to use the `query` function for simple text-based interactions with Claude, including how to process the `AssistantMessage` and `TextBlock` types. It also demonstrates configuring the query with `ClaudeAgentOptions` to set a system prompt and maximum turns.\n\n```python\nfrom claude_agent_sdk import query, ClaudeAgentOptions, AssistantMessage, TextBlock\n\n# Simple query\nasync for message in query(prompt=\"Hello Claude\"):\n    if isinstance(message, AssistantMessage):\n        for block in message.content:\n            if isinstance(block, TextBlock):\n                print(block.text)\n\n# With options\noptions = ClaudeAgentOptions(\n    system_prompt=\"You are a helpful assistant\",\n    max_turns=1\n)\n\nasync for message in query(prompt=\"Tell me a joke\", options=options):\n    print(message)\n```\n\n--------------------------------\n\n### Interrupt Long-Running Claude Agent SDK Tasks (Python)\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nDemonstrates how to programmatically interrupt an ongoing operation initiated with the Claude Agent SDK in Python. It sets up a background consumer, waits for a period, sends an interrupt signal, and then issues a new query to redirect Claude's attention.\n\n```python\nimport asyncio\nfrom claude_agent_sdk import ClaudeSDKClient\n\nasync def main():\n    async with ClaudeSDKClient() as client:\n        # Start a long-running task\n        await client.query(\"Count from 1 to 100 slowly\")\n\n        # Consume messages in background to enable interrupt processing\n        async def consume_messages():\n            messages = []\n            async for msg in client.receive_response():\n                messages.append(msg)\n            return messages\n\n        consume_task = asyncio.create_task(consume_messages())\n\n        # Wait 2 seconds then interrupt\n        await asyncio.sleep(2)\n        print(\"Sending interrupt...\")\n        await client.interrupt()\n\n        # Wait for interrupt to process\n        await consume_task\n\n        # Send new instruction\n        await client.query(\"Never mind, just tell me a quick joke\")\n        async for msg in client.receive_response():\n            if isinstance(msg, AssistantMessage):\n                for block in msg.content:\n                    if isinstance(block, TextBlock):\n                        print(f\"Claude: {block.text}\")\n\nasyncio.run(main)\n```\n\n--------------------------------\n\n### Migrate from External to SDK In-Process MCP Servers\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis Python snippet illustrates the transition from configuring an external MCP server (running as a separate process) to using an in-process SDK MCP server. The 'BEFORE' section shows the configuration for an external server, while 'AFTER' demonstrates how to define tool functions and create an SDK MCP server directly within the application.\n\n```python\n# BEFORE: External MCP server (separate process)\noptions = ClaudeAgentOptions(\n    mcp_servers={\n        \"calculator\": {\n            \"type\": \"stdio\",\n            \"command\": \"python\",\n            \"args\": [\"-m\", \"calculator_server\"]\n        }\n    }\n)\n\n# AFTER: SDK MCP server (in-process)\nfrom my_tools import add, subtract  # Your tool functions\n\ncalculator = create_sdk_mcp_server(\n    name=\"calculator\",\n    tools=[add, subtract]\n)\n\noptions = ClaudeAgentOptions(\n    mcp_servers={\"calculator\": calculator}\n)\n```\n\n--------------------------------\n\n### Implement Custom Transport for Claude Agent SDK Python\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis snippet demonstrates how to extend the Claude Agent SDK in Python by implementing a custom `Transport` layer. It shows the structure for overriding methods like `connect`, `write`, `read_messages`, `close`, `is_ready`, and `end_input` to integrate with specialized communication protocols or environments. The custom transport is then used when calling the `query` function.\n\n```python\nfrom claude_agent_sdk import Transport, query\nfrom typing import AsyncIterator\n\nclass CustomTransport(Transport):\n    \"\"\"Custom transport implementation.\"\"\"\n\n    async def connect(self) -> None:\n        \"\"\"Establish connection to Claude Code.\"\"\"\n        # Custom connection logic\n        pass\n\n    async def write(self, data: str) -> None:\n        \"\"\"Send data to Claude Code.\"\"\"\n        # Custom write logic\n        pass\n\n    def read_messages(self) -> AsyncIterator[dict]:\n        \"\"\"Read messages from Claude Code.\"\"\"\n        # Custom read logic\n        async def _read():\n            while True:\n                # Yield parsed messages\n                yield {}\n        return _read()\n\n    async def close(self) -> None:\n        \"\"\"Clean up connection.\"\"\"\n        pass\n\n    def is_ready(self) -> bool:\n        \"\"\"Check if transport is ready.\"\"\"\n        return True\n\n    async def end_input(self) -> None:\n        \"\"\"Close input stream.\"\"\"\n        pass\n\nasync def main():\n    transport = CustomTransport()\n    async for message in query(prompt=\"Hello\", transport=transport):\n        print(message)\n\nimport anyio\nanyio.run(main)\n```\n\n--------------------------------\n\n### Implement PostToolUse Hook for Python to Stop Execution on Errors\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis Python hook, `stop_on_error_hook`, demonstrates how to control the agent's execution flow based on tool output. If the tool's response contains a 'critical' error, the hook returns `continue_=False` along with a `stopReason` and `systemMessage` to halt further processing. This allows for immediate termination of the agent's actions under specific critical conditions.\n\n```python\nfrom claude_agent_sdk import HookInput, HookContext, HookJSONOutput\n\nasync def stop_on_error_hook(\n    input_data: HookInput,\n    tool_use_id: str | None,\n    context: HookContext\n) -> HookJSONOutput:\n    \"\"\"Stop execution if critical error detected.\"\"\"\n    tool_response = input_data.get(\"tool_response\", \"\")\n\n    if \"critical\" in str(tool_response).lower():\n        return {\n            \"continue_\": False,\n            \"stopReason\": \"Critical error detected - halting for safety\",\n            \"systemMessage\": \"\ud83d\uded1 Execution stopped due to critical error\"\n        }\n\n    return {\"continue_\": True}\n\n# Use in ClaudeAgentOptions:\n# hooks={\"PostToolUse\": [HookMatcher(matcher=\"Bash\", hooks=[stop_on_error_hook])]}\n```\n\n--------------------------------\n\n### Build Claude Agent SDK Python Wheels Locally (Bash)\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThese Bash commands illustrate how to build Python wheels for the Claude Agent SDK locally, including optional parameters for versioning and CLI management. The process involves installing build dependencies, downloading and bundling the Claude Code CLI into the wheel, and performing checks with twine.\n\n```bash\n# Install build dependencies\npip install build twine\n```\n\n```bash\n# Build wheel with bundled CLI\npython scripts/build_wheel.py\n```\n\n```bash\n# Build with specific version\npython scripts/build_wheel.py --version 0.1.4\n```\n\n```bash\n# Build with specific CLI version\npython scripts/build_wheel.py --cli-version 2.0.0\n```\n\n```bash\n# Clean bundled CLI after building\npython scripts/build_wheel.py --clean\n```\n\n```bash\n# Skip CLI download (use existing)\npython scripts/build_wheel.py --skip-download\n```\n\n--------------------------------\n\n### Set Working Directory for Claude Agent\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis Python example illustrates how to specify the working directory for Claude Agent using the `cwd` parameter within `ClaudeAgentOptions`. This allows Claude to operate within a specific project context.\n\n```python\nfrom pathlib import Path\n\noptions = ClaudeAgentOptions(\n    cwd=\"/path/to/project\"  # or Path(\"/path/to/project\")\n)\n```\n\n--------------------------------\n\n### Perform One-Shot Queries with Claude Agent SDK in Python\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis snippet demonstrates how to use the `query()` function for simple, stateless interactions with Claude. It shows a basic query for a math problem and a more complex one with `ClaudeAgentOptions` to create a file, including tool permissions, system prompt, and working directory. The function returns an asynchronous generator of messages, which are then processed to display Claude's responses and the query cost.\n\n```python\nimport anyio\nfrom claude_agent_sdk import (\n    query,\n    ClaudeAgentOptions,\n    AssistantMessage,\n    TextBlock,\n    ResultMessage\n)\n\nasync def main():\n    # Basic query\n    async for message in query(prompt=\"What is 2 + 2?\"):\n        if isinstance(message, AssistantMessage):\n            for block in message.content:\n                if isinstance(block, TextBlock):\n                    print(f\"Claude: {block.text}\")\n        elif isinstance(message, ResultMessage):\n            print(f\"Cost: ${message.total_cost_usd:.4f}\")\n\n    # Query with options\n    options = ClaudeAgentOptions(\n        allowed_tools=[\"Read\", \"Write\", \"Bash\"],\n        permission_mode=\"acceptEdits\",  # Auto-accept file edits\n        system_prompt=\"You are a helpful coding assistant.\",\n        max_turns=5,\n        cwd=\"/path/to/project\"\n    )\n\n    async for message in query(\n        prompt=\"Create a hello.py file that prints 'Hello, World!'\",\n        options=options\n    ):\n        if isinstance(message, AssistantMessage):\n            for block in message.content:\n                if isinstance(block, TextBlock):\n                    print(block.text)\n\nanyio.run(main)\n```\n\n--------------------------------\n\n### Implement Custom Tool Permission Callback for Python\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThe `can_use_tool` asynchronous function in Python provides a flexible way to programmatically control which tools the agent can use and how. It allows denying specific tool actions (e.g., writing to important files) or modifying tool inputs (e.g., adding safety flags to 'Bash' commands). This callback ensures that the agent adheres to predefined safety and operational policies before executing any tool.\n\n```python\nimport asyncio\nfrom claude_agent_sdk import (\n    ClaudeAgentOptions,\n    ClaudeSDKClient,\n    ToolPermissionContext,\n    PermissionResultAllow,\n    PermissionResultDeny\n)\n\nasync def can_use_tool(\n    tool_name: str,\n    tool_input: dict,\n    context: ToolPermissionContext\n):\n    \"\"\"Custom tool permission logic.\"\"\"\n\n    # Block writes to important files\n    if tool_name == \"Write\":\n        file_path = tool_input.get(\"file_path\", \"\")\n        if \"important\" in file_path.lower() or \"config\" in file_path.lower():\n            return PermissionResultDeny(\n                behavior=\"deny\",\n                message=\"Cannot write to important/config files\",\n                interrupt=False\n            )\n\n    # Modify bash commands to add safety flags\n    if tool_name == \"Bash\":\n        command = tool_input.get(\"command\", \"\")\n        if command.startswith(\"rm\"):\n            # Add interactive flag for safety\n            modified_input = {**tool_input, \"command\": f\"{command} -i\"}\n            return PermissionResultAllow(\n                behavior=\"allow\",\n                updated_input=modified_input\n            )\n\n    # Allow everything else\n    return PermissionResultAllow(behavior=\"allow\")\n\nasync def main():\n    options = ClaudeAgentOptions(\n        allowed_tools=[\"Read\", \"Write\", \"Bash\"],\n        can_use_tool=can_use_tool\n    )\n\n    async with ClaudeSDKClient(options=options) as client:\n        await client.query(\"Write data to important_config.json\")\n        async for msg in client.receive_response():\n            # Will be denied by callback\n            pass\n\nasyncio.run(main)\n```\n\n--------------------------------\n\n### Handle Claude Agent SDK Errors (Python)\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis Python example demonstrates how to implement robust error handling when interacting with the Claude Agent SDK. It shows the use of a `try-except` block to catch specific error types such as `CLINotFoundError` (for missing CLI), `ProcessError` (for process failures), and `CLIJSONDecodeError` (for JSON parsing issues), allowing for graceful recovery and informative user feedback.\n\n```python\nfrom claude_agent_sdk import (\n    ClaudeSDKError,      # Base error\n    CLINotFoundError,    # Claude Code not installed\n    CLIConnectionError,  # Connection issues\n    ProcessError,        # Process failed\n    CLIJSONDecodeError,  # JSON parsing issues\n)\n\ntry:\n    async for message in query(prompt=\"Hello\"):\n        pass\nexcept CLINotFoundError:\n    print(\"Please install Claude Code\")\nexcept ProcessError as e:\n    print(f\"Process failed with exit code: {e.exit_code}\")\nexcept CLIJSONDecodeError as e:\n    print(f\"Failed to parse response: {e}\")\n```\n\n--------------------------------\n\n### Handle Claude Agent SDK Errors in Python\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis example illustrates how to implement comprehensive error handling for the Claude Agent SDK, catching specific exceptions like `CLINotFoundError`, `CLIConnectionError`, `ProcessError`, `CLIJSONDecodeError`, and `MessageParseError`. It shows how to provide user-friendly messages and debug information based on the error type.\n\n```python\nimport asyncio\nfrom claude_agent_sdk import (\n    query,\n    ClaudeSDKError,\n    CLINotFoundError,\n    CLIConnectionError,\n    ProcessError,\n    CLIJSONDecodeError,\n    MessageParseError\n)\n\nasync def main():\n    try:\n        async for message in query(prompt=\"Hello Claude\"):\n            print(message)\n\n    except CLINotFoundError:\n        print(\"Error: Claude Code is not installed\")\n        print(\"Install with: npm install -g @anthropic-ai/claude-code\")\n\n    except CLIConnectionError as e:\n        print(f\"Connection error: {e}\")\n        print(\"Check that Claude Code is properly configured\")\n\n    except ProcessError as e:\n        print(f\"Process failed with exit code {e.exit_code}\")\n        if e.stderr:\n            print(f\"Error output: {e.stderr}\")\n\n    except CLIJSONDecodeError as e:\n        print(f\"Failed to parse JSON response: {e}\")\n        print(f\"Raw line: {e.line}\")\n\n    except MessageParseError as e:\n        print(f\"Failed to parse message: {e}\")\n        if e.data:\n            print(f\"Problem data: {e.data}\")\n\n    except ClaudeSDKError as e:\n        # Base exception for all SDK errors\n        print(f\"SDK error: {e}\")\n\nasyncio.run(main)\n```\n\n--------------------------------\n\n### Implement PreToolUse Hook for Bash Command Validation with Claude Agent SDK (Python)\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis example demonstrates how to use `PreToolUse` hooks in the Claude Agent SDK to validate and potentially block tool calls. The `check_bash_command` asynchronous function inspects incoming `Bash` tool calls for dangerous patterns like 'rm -rf' or 'sudo' and denies them. The `main` function shows how to configure the agent with this hook and test its functionality by attempting both blocked and allowed commands.\n\n```python\nimport asyncio\nfrom claude_agent_sdk import (\n    ClaudeAgentOptions,\n    ClaudeSDKClient,\n    HookMatcher,\n    HookInput,\n    HookContext,\n    HookJSONOutput,\n    AssistantMessage,\n    TextBlock\n)\n\nasync def check_bash_command(\n    input_data: HookInput,\n    tool_use_id: str | None,\n    context: HookContext\n) -> HookJSONOutput:\n    \"\"\"Block dangerous bash commands.\"\"\"\n    tool_name = input_data[\"tool_name\"]\n    tool_input = input_data[\"tool_input\"]\n\n    if tool_name != \"Bash\":\n        return {}\n\n    command = tool_input.get(\"command\", \"\")\n    dangerous_patterns = [\"rm -rf\", \"sudo\", \"foo.sh\"]\n\n    for pattern in dangerous_patterns:\n        if pattern in command:\n            return {\n                \"hookSpecificOutput\": {\n                    \"hookEventName\": \"PreToolUse\",\n                    \"permissionDecision\": \"deny\",\n                    \"permissionDecisionReason\": f\"Blocked dangerous pattern: {pattern}\"\n                }\n            }\n\n    return {}\n\nasync def main():\n    options = ClaudeAgentOptions(\n        allowed_tools=[\"Bash\"],\n        hooks={\n            \"PreToolUse\": [\n                HookMatcher(matcher=\"Bash\", hooks=[check_bash_command])\n            ]\n        }\n    )\n\n    async with ClaudeSDKClient(options=options) as client:\n        # This will be blocked by the hook\n        await client.query(\"Run the bash command: ./foo.sh --help\")\n        async for msg in client.receive_response():\n            if isinstance(msg, AssistantMessage):\n                for block in msg.content:\n                    if isinstance(block, TextBlock):\n                        print(f\"Claude: {block.text}\")\n\n        # This will be allowed\n        await client.query(\"Run the bash command: echo 'Hello World'\")\n        async for msg in client.receive_response():\n            if isinstance(msg, AssistantMessage):\n                for block in msg.content:\n                    if isinstance(block, TextBlock):\n                        print(f\"Claude: {block.text}\")\n\nasyncio.run(main)\n```\n\n--------------------------------\n\n### Run Initial Development Setup Script (Bash)\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis Bash command initiates the development environment setup for the project. Executing this script installs necessary Git hooks, such as a pre-push hook, to ensure that lint checks are automatically performed before code is pushed to the repository, thereby aligning with the Continuous Integration (CI) workflow.\n\n```bash\n./scripts/initial-setup.sh\n```\n\n--------------------------------\n\n### Implement PostToolUse Hook for Python to Review Tool Output\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis Python hook, `review_tool_output`, is designed to run after a tool's execution. It inspects the tool's response, specifically looking for errors, and can inject additional context or system messages back to the agent if issues are detected. The hook integrates with the `ClaudeAgentOptions` to specify its application to a particular tool (e.g., 'Bash').\n\n```python\nimport asyncio\nfrom claude_agent_sdk import (\n    ClaudeAgentOptions,\n    ClaudeSDKClient,\n    HookMatcher,\n    HookInput,\n    HookContext,\n    HookJSONOutput\n)\n\nasync def review_tool_output(\n    input_data: HookInput,\n    tool_use_id: str | None,\n    context: HookContext\n) -> HookJSONOutput:\n    \"\"\"Provide feedback on tool execution results.\"\"\"\n    tool_response = input_data.get(\"tool_response\", \"\")\n\n    # If tool produced an error, add helpful context\n    if \"error\" in str(tool_response).lower():\n        return {\n            \"systemMessage\": \"\u26a0\ufe0f The command produced an error\",\n            \"reason\": \"Tool execution failed\",\n            \"hookSpecificOutput\": {\n                \"hookEventName\": \"PostToolUse\",\n                \"additionalContext\": \"Consider trying a different approach.\"\n            }\n        }\n\n    return {}\n\nasync def main():\n    options = ClaudeAgentOptions(\n        allowed_tools=[\"Bash\"],\n        hooks={\n            \"PostToolUse\": [\n                HookMatcher(matcher=\"Bash\", hooks=[review_tool_output])\n            ]\n        }\n    )\n\n    async with ClaudeSDKClient(options=options) as client:\n        await client.query(\"Run this command: ls /nonexistent_directory\")\n        async for msg in client.receive_response():\n            # Process messages with added context from hook\n            pass\n\nasyncio.run(main)\n```\n\n--------------------------------\n\n### Process Claude Agent SDK Python Message Types and Content Blocks\n\nSource: https://context7.com/anthropics/claude-agent-sdk-python/llms.txt\n\nThis snippet demonstrates how to receive and interpret different message types (UserMessage, AssistantMessage, SystemMessage, ResultMessage, StreamEvent) and their associated content blocks (TextBlock, ThinkingBlock, ToolUseBlock, ToolResultBlock) from the Claude Agent SDK using Python. It shows how to differentiate between messages and parse their contents for display or further processing.\n\n```python\nfrom claude_agent_sdk import (\n    query,\n    UserMessage,\n    AssistantMessage,\n    SystemMessage,\n    ResultMessage,\n    StreamEvent,\n    TextBlock,\n    ThinkingBlock,\n    ToolUseBlock,\n    ToolResultBlock\n)\n\nasync def main():\n    async for message in query(prompt=\"Analyze this file and fix any issues\"):\n        # User messages contain prompts and tool results\n        if isinstance(message, UserMessage):\n            for block in message.content:\n                if isinstance(block, TextBlock):\n                    print(f\"User text: {block.text}\")\n                elif isinstance(block, ToolResultBlock):\n                    print(f\"Tool result for {block.tool_use_id}: {block.content}\")\n\n        # Assistant messages contain Claude's responses and tool uses\n        elif isinstance(message, AssistantMessage):\n            print(f\"Model: {message.model}\")\n            print(f\"Stop reason: {message.stop_reason}\")\n            for block in message.content:\n                if isinstance(block, TextBlock):\n                    print(f\"Claude: {block.text}\")\n                elif isinstance(block, ThinkingBlock):\n                    print(f\"Claude's thinking: {block.thinking}\")\n                elif isinstance(block, ToolUseBlock):\n                    print(f\"Tool use: {block.name}\")\n                    print(f\"Tool ID: {block.id}\")\n                    print(f\"Tool input: {block.input}\")\n\n        # System messages from Claude Code\n        elif isinstance(message, SystemMessage):\n            print(f\"System: {message.metadata}\")\n\n        # Result message at end of conversation\n        elif isinstance(message, ResultMessage):\n            print(f\"Total cost: ${message.total_cost_usd:.4f}\")\n            print(f\"Input tokens: {message.usage.input_tokens}\")\n            print(f\"Output tokens: {message.usage.output_tokens}\")\n            print(f\"Stop reason: {message.stop_reason}\")\n\n        # Partial message updates during streaming\n        elif isinstance(message, StreamEvent):\n            print(f\"Streaming content: {message.content}\")\n\nimport anyio\nanyio.run(main)\n```\n\n--------------------------------\n\n### Install Claude Agent SDK for Python\n\nSource: https://github.com/anthropics/claude-agent-sdk-python/blob/main/README.md\n\nThis command installs the Claude Agent SDK for Python using pip. It requires Python 3.10+ and automatically bundles the Claude Code CLI, which is used by default.\n\n```bash\npip install claude-agent-sdk\n```\n\n=== COMPLETE CONTENT === This response contains all available snippets from this library. No additional content exists. Do not make further requests.",
    "chunks": null,
    "metadata": {}
  },
  "cached_at": 1765393188.0808609,
  "params": {
    "owner": "anthropics",
    "repo": "claude-agent-sdk-python",
    "version": "latest",
    "topic": "orchestration multi-agent coordination",
    "tokens": 8000,
    "format": "txt"
  }
}