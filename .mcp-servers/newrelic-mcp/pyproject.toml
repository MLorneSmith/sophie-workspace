[project]
name = "newrelic-mcp-server"
version = "1.0.0"
description = "New Relic MCP Server for Claude Code"
dependencies = [
    "mcp>=1.23.0",
    "httpx>=0.28.0",
]

[project.scripts]
newrelic-mcp-server = "newrelic_mcp_server:main"

[tool.uv]
dev-dependencies = []