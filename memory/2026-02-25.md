# 2026-02-25 - Wednesday

## [21:00] Phase 3 Agent Layer Progress

**Completed tasks:**
- **#485** — Agent Suggestions UI (PR #2185)
  - Created `AgentSuggestionsPanel.tsx` component
  - Features: grouped by agent, accept/reject per item, bulk actions, status badges
  - GLM-5 completed after GPT-5.2 failed 3x with thinking loops
  - CodeRabbit feedback addressed (a11y, error handling, i18n)
  - PR ready for Mike's review

- **#505** — TipTap-compatible agent output schemas
  - Created `packages/mastra/src/types/agent-output.ts`
  - Zod schemas for all 4 agents (Partner, Validator, Whisperer, Editor)
  - Bundled into PR #2185

- **#507** — Closed as redundant (AgentSuggestionsPanel already handles it)

- **#499** — Closed (was stale — already done via PR #2179)

**PRs awaiting Mike's review:**
- PR #2185 — Agent Suggestions UI
- PR #2184 — ast-grep output tracing

## [21:30] Credit Burn Crisis - Root Cause Analysis

**Problem:** Both ChatGPT Pro and Claude Max hit usage limits this week.

**Root causes identified:**
1. **Sub-agent Watchdog** — 288 Claude calls/day (every 5 min)
2. **MC ↔ Todoist Sync** — 48 GPT-5 calls/day (every 30 min)
3. **Nightly coding sessions** — ~100 GPT-5 calls/night combined
4. **Morning Brief** — ~20 GPT-5 calls/day
5. Multiple other cron jobs using LLMs for deterministic tasks

**Key insight:** LLMs were being used as glorified `if/then/else` statements for tasks that could be deterministic Python scripts.

## [22:00-23:30] Credit Savings Initiative - 7 Jobs Converted

**Converted to deterministic Python:**

| Job | Before | After | Saved |
|-----|--------|-------|-------|
| Sub-agent Watchdog | 288 Claude/day | 0 | 288/day |
| MC ↔ Todoist Sync | 48 GPT-5/day | 0 | 48/day |
| qmd Index | 5 GPT-5/day | 0 | 5/day |
| Feed Refresh | 5 GPT-5/day | 0 | 5/day |
| Health Check | 6 GLM-5/day | 0 | 6/day |
| Daily Backup | 20 GLM-5/day | 0 | 20/day |
| Presentation Pipeline | 20 GLM-5/day | 0 | 20/day |
| **Total** | ~392/day | 0 | **~392/day** |

**Impact:** ~80% reduction in LLM calls (512 → ~120/day)

**Scripts created:**
- `~/clawd/scripts/subagent-watchdog-deterministic.py`
- `~/clawd/scripts/sync-mc-todoist-wrapper.py`
- `~/clawd/scripts/qmd-index-refresh.py`
- `~/clawd/scripts/feed-monitor-refresh.py`
- `~/clawd/scripts/openclaw-healthcheck.py`
- `~/clawd/scripts/backup-openclaw.py`
- `~/clawd/scripts/nightly-presentation-pipeline.py`

## [22:30] Model Routing Fix

**Issue:** ChatGPT Pro hit usage limit (resets March 2nd), causing:
- GPT-5.2 calls failing immediately
- Fallback to GLM-5 adding 88-second latency
- Discord listener timeouts (>30s)

**Fix:** Switched primary model to GLM-5 until ChatGPT Pro resets.

**Config change:** `~/.openclaw/openclaw.json` — primary: `zai/glm-5`, fallback: `openrouter/auto`

## [23:00] Health Check Integration with Mission Control

**Built:**
1. Added `SystemHealthCheck` model to Prisma schema
2. Created `/api/v1/system-health` endpoint (GET + POST)
3. Updated health check script to POST results to MC
4. Cockpit page already has "System Health" section — now displays live data

**Checks:** Gateway, ccproxy, disk space, memory, MC API, cron jobs

## [23:30] GPT-5.2 vs GLM-5 for Coding Sub-Agents

**Finding:** GPT-5.2 via ccproxy gets stuck in "thinking" loops without executing tools. GLM-5 executes cleanly.

**Evidence:**
- Task #485: GPT-5.2 looped on same git/find commands 10+ times
- GLM-5: Explored → understood → created files → committed → pushed → created PR

**Decision:** Use `zai/glm-5` for coding sub-agents. Keep GPT-5.2 for interactive/local use only.

**Updated:** `~/clawd/.ai/agents/coder.yaml` — model: `zai/glm-5`

## [23:45] Telegram Command Limit Error

**Issue:** `BOT_COMMANDS_TOO_MUCH` — Telegram has 100 command limit per bot.

**Cause:** OpenClaw was auto-registering commands for all 30+ skills.

**Fix:** Set `nativeSkills: false` in OpenClaw config.

## [11:30] Backup Job Renaming

Renamed for clarity:
- "Daily OpenClaw backup" → **"OpenClaw Local Backup (daily tar.gz)"**
- "workspace-backup" → **"Workspace Git Backup (daily push to GitHub)"**

Rationale: Different disaster recovery purposes (local archive vs remote git history).

## [11:45] Deleted Unused Cron Job

Removed "Weekly Dinner Menu Suggestions" — not needed.

---

## Lessons Learned

1. **LLMs are expensive `if/then/else`** — Don't use LLMs for deterministic tasks
2. **GLM-5 > GPT-5.2 for sub-agents** — Fewer thinking loops, more execution
3. **Defense in depth for credits** — Multiple safeguards needed (watchdog, timeout, rate limits)
4. **Fixed HTML templates** — Morning brief should use templates, not LLM-generated HTML
5. **Rate limits compound** — ChatGPT Pro + Claude limits hit simultaneously = crisis

## [13:30-14:10] File Organization & Context Optimization

**Container cleanup:**
- Moved 4 agent workspaces to `~/agents/` (coder, research, devops, pipeline)
- Deleted 29 unused IDE dot-directories (`.cursor`, `.copilot`, etc.)
- Moved 37 image files to proper subdirectory
- Moved Google secrets to `~/.clawdbot/`
- Removed `~/gogcli/` (source, binary still at `/usr/local/bin/gog`)
- Merged `~/docs/` into `~/clawd/docs/`
- Moved `~/roadmap/` to `~/projects/roadmap/`

**Context optimization:**
- **AGENTS.md:** 12KB → 2.7KB (78% reduction)
  - Added Boot Sequence at top
  - Added Write Discipline section
  - Added Handover Protocol
  - Trimmed verbose sections
- **MEMORY.md:** 19KB → 3KB (84% reduction)
  - Keep: Critical warnings, priorities, projects, model dispatch
  - Archive: Setup details → `memory/archive/`

**Sub-agent cleanup:**
- Removed 24 duplicate boilerplate files from 4 agent workspaces
- Each agent now has only AGENTS.md + skills + memory (no SOUL, USER, IDENTITY, etc.)

**New cron job:** Memory Gardening (weekly, Sunday 8am) — archives stale MEMORY.md content

**Home directory:** 40+ items → 9 items

---

## HANDOVER — Session End

**What was discussed:**
1. File organization of container (`~/`)
2. OpenClaw vs clawd directory structure
3. Agent workspace consolidation
4. Context window optimization (AGENTS.md, MEMORY.md)
5. qmd (local hybrid search) and how it fits into memory architecture
6. EdgeHDF5 Reddit thread (HDF5-backed memory) — decided not to switch

**What was decided:**
- Agent workspaces → `~/agents/`
- Roadmap → `~/projects/roadmap/`
- AGENTS.md restructured with Boot Sequence, Write Discipline, Handover Protocol
- MEMORY.md trimmed to essentials, historical details archived
- Keep qmd for semantic search, not switching to EdgeHDF5

**Pending tasks:**
- None — file organization complete

**Next steps:**
- Session will use new AGENTS.md boot sequence on next start
- Monitor if 3KB MEMORY.md is sufficient or needs expansion

---

## [18:01] CodeRabbit Review - PR #2187

**PR:** feat: Add before/after comparison view for agent runs

**Feedback processed:**
- ✅ Most issues addressed in commits 37fdae9 and 924d61b
- 🔧 Fixed remaining issue: exact-match branch missing `detail` comparison
  - Commit: 34aa42c1e
  - The exact-match branch only compared `priority` and `status`, missing changes to `detail`
  - Fuzzy-match branch correctly compared detail, creating inconsistency

**PR ready for Mike's review.**

---

## Blockers

- **ChatGPT Pro rate limited** until March 2nd — using GLM-5 as primary
- **PR #2185** awaiting Mike's review
- **PR #2184** awaiting Mike's review
- **PR #2187** awaiting Mike's review (comparison view)

## State Changes

- Primary model: `ccproxy/gpt-5.2` → `zai/glm-5` (until March 2)
- Coder agent model: `ccproxy/gpt-5.2` → `zai/glm-5`
- 7 cron jobs converted from LLM → deterministic Python
- Health check results now stored in MC database
- Presentation pipeline results now stored in MC database
- Agent workspaces moved to `~/agents/`
- AGENTS.md: 12KB → 2.7KB
- MEMORY.md: 19KB → 3KB
- Archive created: `memory/archive/`

## 19:15 — PR #2187 CodeRabbit Round 4 Fixes

CodeRabbit posted a new review with 5 issues. All addressed in commit fc970f806:

- 🔴 **Critical:** `failedRunIds` not cleared on successful fetch retry — fixed by adding cleanup in success branch
- 🟡 **Minor:** Exact-match consumed items skip fuzzy matching — fixed by falling through to fuzzy match
- 🟡 **Minor:** Transient state flicker between state updates — fixed by merging `setComparisonState` calls
- 🟡 **Minor:** `changeReason` bypasses i18n — added TODO comment for follow-up work
- 🟡 **Minor:** Stats badges missing text labels — added Trans components for accessibility

All type checks passed. Pushed to fork. Awaiting CodeRabbit re-review.

---

## 23:11 — PR #2187 CodeRabbit Round 5 Fixes (Commit abb95b07a)

CodeRabbit posted Round 5 with 5 actionable + 6 nitpick comments. All addressed:

**Actionable (already fixed in previous rounds):**
- ✅ Fuzzy match fallback — already implemented via `findBestFuzzyMatch()`
- ✅ State update merge — already done with combined `setComparisonState` call
- ✅ Retry recovery — `failedRunIds` already cleared on success
- ✅ Stats badges accessibility — already using Trans with values

**Nitpick fixes (commit abb95b07a):**
- ✅ Removed unused `currentBySummary` map (dead code)
- ✅ Hoisted `DIFF_TYPE_CONFIG` to module scope (perf)
- ✅ Changed `selectedCompareRunIds` to `Partial<Record<...>>` (type safety)
- ✅ Added TODO for side-by-side view toggle
- ✅ Enhanced ref invariant comment explaining dual-track pattern

All type checks passed. Already pushed to fork. Awaiting CodeRabbit re-review.
