# 2026-02-19

## [16:00] Phase 1 Foundation — Workflow Data Model Sprint

Mike initiated a focused sprint on **Objective 2 (Build a Product Customers Love) → Initiative #38 (Phase 1 — The Foundation)**.

### Completed Tasks
- **#532-536** — Marked done (PR #2102 merged by Mike with small changes)
- **#538** — Argument Map (Pyramid Principle) UI → PR #2108 → merged ✅
- **#544** — Create presentations record on workflow start → PR #2109 → merged ✅
- **#545** — Wire Profile step → audience_profiles → PR #2110 (open, ready for review)
- **#546** — Wire Assemble step → assemble_outputs → branch pushed, awaiting #545 merge to PR
- **#547** — Wire Outline step → outline_contents → branch pushed, awaiting chain merge

### New Tasks Created (#544-549)
Broke "Wire workflow to new presentations data model" into 6 tasks:
- #544 presentations record, #545 profile, #546 assemble, #547 outline, #548 storyboard, #549 generate
- All assigned to Sophie, initiative #38, phase P1D

### Key Design Decisions (from Mike)
1. **Argument Map replaces the free-text "answer" field** — it IS the structured answer in SCQA
2. **Flow order:** Presentation Type → Title → Audience → Situation → Complication → Question Type → Argument Map
3. **Clean card-based tree** style, 3 levels only: claim → support → evidence
4. **No backwards compatibility needed** — cut over to new `presentations` table, stop writing to `building_blocks_submissions`
5. **Break large tasks into smaller PRs** — one per workflow step

## [16:50] CF Access Credentials Location
The `CF_ACCESS_CLIENT_ID` / `CF_ACCESS_CLIENT_SECRET` for Mission Control API are in:
`~/clawd/slideheroes-internal-tools/app/.env.local` (NOT `~/.clawdbot/.env`)
Scripts like `log-activity.sh` already reference this path. I was looking in the wrong file.

## [17:00] Sub-Agent Pre-Commit Fix
**Lesson learned:** Sub-agents were only running `pnpm --filter web typecheck` + `lint` but CI also checks `pnpm biome format . --diagnostic-level=error`. PRs were failing format checks.

**Fix:** Added to TOOLS.md — sub-agents must run `pnpm format:fix && pnpm lint:fix && pnpm typecheck` before committing.

## [17:45] GPT-5.2 Codex Rate Limit
After 4 successful sub-agent runs (~280k tokens total) plus a stuck 49-min session, Codex hit rate_limit cooldown. GLM also failed (timed out on #547). Opus handled #547 in under 2 minutes.

**Lesson:** For coding tasks, Opus is more reliable than GLM as a Codex fallback. GLM struggles with complex repo navigation.

## [18:10] Initiative #38 Status
Remaining backlog after today's sprint:
- **#548** — Storyboard → storyboard_contents (not started)
- **#549** — Generate → generate_outputs (not started)
- **#519** — Migrate audience field to structured Audience Brief (rescoped — may be covered by #545)
- **#327** — Storyboard Build
- **#329** — Presentation Export (PPTX/PDF)
- **#333** — Debug PPT generation feature
- **#331** — Workflow Bug Fixes

## [18:18] Sonnet 4.6 Added to Config
Added `anthropic/claude-sonnet-4-6` (alias: sonnet) to allowed models for sub-agents. Mike suggested using Sonnet for coding tasks when Codex is rate-limited — cheaper than Opus for focused implementation work.

**Updated model dispatch strategy:**
- **Codex (GPT-5.2)** — primary coding agent
- **Sonnet 4.6** — coding fallback when Codex rate-limited (fast + cheap)
- **Opus 4.6** — complex reasoning, conversation, last resort coding
- **GLM 4.7** — bulk/research only (unreliable for coding, timed out on #547)

## [20:15] GPT-5.3 Codex First Real Task — Success
#333 (PPTX export) completed successfully on `openai-codex/gpt-5.3-codex`. Took ~10 min total runtime. Model works well.

## [20:00] Task #333 Rescoped + #329 Merged In
Rescoped #333 from "Debug PPT generation" to "Consolidate export pipeline + wire into Generate step." #329 (Presentation Export PPTX/PDF) marked done as it was redundant. PDF export can be a separate task later.

**Generate step now has:** export button, slide count preview, loading states, browser download, slide number footers, bullet point formatting.

## [19:50] Phase 1 Foundation — Nearly Complete
After today's sprint, only 2 tasks remain in Initiative #38:
- **#327** [high] — Storyboard Build (flesh out editor)
- **#331** [medium] — Workflow Bug Fixes

## [18:50] Sonnet 4.6 Unreliable for Sub-Agent Coding Tasks
Sonnet 4.6 got stuck at 31 minutes on #548 (storyboard persistence) — had to kill it. Opus handled the same task in 2 minutes.

**Lesson:** For coding sub-agents, the reliable fallback chain is: **Codex → Opus → (skip Sonnet/GLM)**. Both Sonnet and GLM have gotten stuck on repo navigation tasks. Opus is fast and decisive for these focused tasks — it checks what exists first and only adds what's missing.

## [19:29] OpenAI Codex Config Updated by Configure Wizard
Running `openclaw configure --section model` to re-auth Codex had a side effect — it updated the model config:

**Removed:** `openai-codex/gpt-5.2`, `zai/glm-4.7`
**Added:** `openai-codex/gpt-5.3-codex`, `openai-codex/gpt-5.3-codex-spark`, `zai/glm-5`

Both GPT-5.2 and GPT-5.3-codex are confirmed working after re-auth. Need to update TOOLS.md and model-dispatch.json with new model IDs.

**Lesson:** `openclaw configure` rewrites the models section to latest defaults — be aware it may change model IDs. Always check config after running it.

## [18:52] Task #548 Complete
All persistence tasks done except #549 (Generate). Sprint status:
- ✅ #538, #544, #545 — merged into dev
- ✅ #546 — PR #2112 open
- ✅ #547, #548 — branches pushed, awaiting chain merge
- ✅ #549 — done, branch pushed

## [21:30] PR Chain Progress
PRs #2112-#2115 opened for tasks #546-#549. PR #2115 (#549 Generate persistence) is the last in the chain, awaiting merge. All earlier PRs (#2108-#2114) merged.

## [19:00] Full Persistence Chain Complete
All 6 workflow persistence tasks (#544-549) are done. The entire workflow now writes to the clean new data model:
- Profile → `audience_profiles`
- Assemble → `assemble_outputs`
- Outline → `outline_contents`
- Storyboard → `storyboard_contents`
- Generate → `generate_outputs`
Each step advances `presentations.current_step` and `completed_steps`.

**Merge chain:** #544 merged → #545 merged → #546 (PR #2112 open) → #547 → #548 → #549 (sequential PRs needed)

## [19:00] Codex OAuth Token Expired
GPT-5.2 Codex rate limit cleared but OAuth token expired. Needs `codex login --device-auth` to re-authenticate. Not a rate limit issue anymore.
