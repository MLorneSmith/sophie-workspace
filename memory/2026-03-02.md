## 19:59 — CodeRabbit Reviews on PR #2222 and PR #2221

**PR #2222** (`feat(canvas): add template selection UI to Generate step`) — 10 actionable + 1 nitpick:
- 🔴 Critical: Normalize swatch colors before inline style (missing `#` prefix on hex values)
- 🔴 Critical: Fix preview gradient color formatting and fallback CSS in template-picker
- 🟠 Major: Constrain palette entries to valid hex colors in schema
- 🟠 Major: Validate `templateId` against allowed IDs in save action
- 🟠 Major: Initialize `selectedTemplateId` from validated TemplateId
- 🟠 Major: Guard against out-of-order template saves on rapid selection
- 🟠 Major: Avoid unchecked cast to TemplateId in selection handler
- 🟠 Major: Replace hard-coded copy with Trans for i18n
- 🟠 Major: Export powerpoint action issue (analysis chain)
- 🟡 Minor: Normalize template_id before passing to client component
- Nitpick: Add `import "server-only"` to server action

**PR #2221** (`feat: add Library section to left navigation`) — 6 actionable + 1 nitpick:
- 🟠 Major: Render template thumbnail/preview assets from config (currently using synthetic gradients)
- 🟠 Major: Company Profiles tab not implemented (always empty)
- 🟡 Minor: Localize profile labels and date formatting
- 🟡 Minor: Profile component issue (analysis chain)
- 🟡 Minor: Move template labels/actions to translation keys
- 🟡 Minor: Templates config issue (analysis chain)
- Nitpick: Handle Supabase auth/query errors explicitly

Both queued for Neo to address in next pickup cycle.

---

## 12:31 — CodeRabbit Review on PR #2211 (Template Configs)

CodeRabbit posted multiple reviews on PR #2211 (`feat: define 5 curated template configurations`). Key issues (all repeated across review passes):

1. **Type guard too permissive** — `isCuratedTemplateConfig` doesn't validate typography fields or density enum
2. **`getDefaultTemplate()` can return undefined** — needs guard/throw if CURATED_TEMPLATES empty
3. **Hard-coded English strings** — should use i18n keys for template `name`/`description`
4. **State leak (outside-diff)** — `generateFromStoryboard()` mutates instance state without cleanup

Queued for Neo to address.

---

## 13:00 — AI Gateway Evaluation Kicked Off

Mike wants to evaluate replacing Portkey AI Gateway with open-source alternatives. Spawned Kvoth to research:
- Portkey capabilities audit (features, pricing, lock-in)
- Three alternatives: Bifrost, LiteLLM, Helicone
- Additional alternatives to consider
- Comparison matrix

## 13:10 — Sub-Agent File Persistence Fix

**Issue:** Kvoth wrote `ai-gateway-evaluation.md` but file didn't persist — agent sandbox `workspaceAccess: "ro"` prevented writes to `~/clawd/`.

**Fixes applied:**
1. Config: `workspaceAccess: "ro" → "rw"` (gateway restart)
2. Kvoth's AGENTS.md: deliverables path → `~/clawd/artifacts/kvoth/YYYY-MM-DD/`
3. Updated ALL agent AGENTS.md files with `~/clawd/artifacts/{agent}/YYYY-MM-DD/` pattern (Neo was already done, updated Hemingway, Michelangelo, Viral, Scrooge)

## 13:25 — AI Gateway Research Complete + Deep Dives

**Round 1 findings:** LiteLLM recommended (37k stars, 100+ providers). Bifrost dismissed as "small community."

**Round 2 deep dive** (Mike's questions):
- Vercel AI Gateway NOT included in Pro plan (separate credit billing)
- Bifrost community concern valid but not disqualifying
- Portkey OSS loses analytics, prompt mgmt, cost tracking

**Round 3** (prompt mgmt + hosting):
- **Langfuse** identified as best OSS prompt management (17k stars, native LiteLLM integration)
- Bifrost hosting: ~$0 extra (64MB RAM, colocates on EC2)
- LiteLLM hosting: $30-60/mo (needs 2-4GB RAM + PostgreSQL)

## 13:33 — DECISION: Bifrost + Langfuse Cloud

**Mike chose Bifrost + Langfuse Cloud** based on:
- Performance: 11µs overhead (5,000x faster than LiteLLM's 60ms)
- Cost: ~$15-30/mo vs Portkey's $49-199/mo + 5.5% fee
- Resources: 64MB RAM vs LiteLLM's 2-4GB
- Provider support: 21 providers (not 15 as Kvoth initially reported) — covers OpenAI, Anthropic, and more
- Langfuse Cloud free tier for prompt management + observability

## 13:41 — DECISION: Cloudflare Tunnel for Vercel → EC2 Connectivity

Vercel → Bifrost connectivity: **Cloudflare Tunnel + CF Access service token**
- ~10ms added latency (negligible vs 1-3s LLM calls)
- Zero exposed ports on EC2
- Already use Cloudflare for other services
- Tailscale ruled out (Vercel Functions can't join mesh)

## 13:44 — Spec Created: Bifrost + Langfuse Migration (#2212)

Rabbit Plan spec created and approved. GitHub issues:
- **#2212** — Spec (parent)
- **#2213** — F1: Bifrost Deployment + CF Tunnel (2-3 days)
- **#2214** — F2: Gateway Client Refactor (3-4 days)
- **#2215** — F3: Langfuse Prompt Migration (2-3 days, parallel with F1)
- **#2216** — F4: Integration & Cleanup (2-3 days, depends on F1-F3)

All labeled `plan-me`. CodeRabbit posted coding plans on #2213, #2214, #2215.

## 14:00 — CodeRabbit Plans Received

CodeRabbit posted coding plans on 3 of 4 Bifrost issues (#2213, #2214, #2215). #2216 still waiting.

## 14:15 — Task Board Consolidation Decision Recalled

Mike asked about the content Kanban decision. Confirmed from architecture doc (2026-02-28):
- **Decision:** Single MC board with `contentType`/`contentPhase` fields (kill separate Content board)
- **No coding task existed** to implement this

**Created MC #684:** "Consolidate Content Board into Main Task Board (single Kanban)"
- Migrate 31 tasks from Board 6 → Board 1
- Populate contentType/contentPhase fields (all currently null)
- Build content-specific Kanban view (columns by contentPhase)
- Archive Board 6

## 14:30 — Neo Loop Pipeline Bugs Diagnosed & Fixed

**Three bugs found preventing Neo from picking up tasks:**

1. **D-Bus missing in cron** — `systemd-run --user` failed with "No medium found" because cron doesn't set `DBUS_SESSION_BUS_ADDRESS`. Fixed in `process-spawn-queue.py`.
2. **Wrong label name** — script used `in-progress`, repo has `status:in-progress`. Fixed in `neo-issue-pickup.py`.
3. **Stale active-runs locks** — failed spawns left locks that blocked all retries. Cleared manually + added auto-cleanup.

**After fix:** Neo successfully processed #2215 → PR #2217 (Langfuse integration, +328/-1 lines, all CI green, MERGED).

## 14:45 — Neo Loop Process Improvements (5 fixes)

1. ✅ `cleanup-stale-locks.py` — cron every 15 min, clears locks >45 min old
2. ✅ `process-spawn-queue.py` — added */10 cron to drain queue (prevents tasks stuck after concurrent triggers)
3. ✅ Success validation — PR-existence check when exit code is non-zero (acpx exit ≠ actual outcome)
4. ✅ `neo-healthcheck.py` — validates D-Bus, labels, locks, queue, acpx availability
5. ✅ LEARNINGS.md updated with 5 new rules

## 15:00 — Neo Pipeline Progress

- **#2215 → PR #2217 MERGED** ✅ — Langfuse Cloud Prompt Migration (+328/-1, 6 files, all CI green)
- **#2214 → PR #2219 OPEN** — Gateway Client Refactor (Portkey → Bifrost). Completed at 15:11.
- **#2213 → PR #2220 OPEN** — Bifrost Deployment (Docker, CF Tunnel, provider configs, verification scripts). Completed at 17:41.

**Bifrost migration: 3/4 features done.** F4 (#2216, Integration & Cleanup) waiting on CodeRabbit plan. Two PRs ready for Mike's review (#2219, #2220).

Queue is empty — need to re-trigger #2213 pickup on next cron cycle.

## 15:29 — Created MC #685 + GitHub internal-tools#23

Agent Pipeline Status on /agents page — Neo first, extensible to all agents.

## 17:35 — Re-queued #2213 (Bifrost Deployment)

#2213 was stuck in limbo: `status:in-progress` label but no active work, pickup script ignoring it. Root cause: failed spawn (pre-D-Bus fix) removed `plan-me` but never restored it on failure. Manual fix: reset label to `plan-me`, cleared from processed list, re-triggered pickup. Neo now working on it.

**Systemic gap identified and FIXED:** `process-spawn-queue.py` now auto-resets on failure:
- Resets label `status:in-progress` → `plan-me`
- Removes issue from processed list
- Tracks retry count in cooldown.json (max 3 attempts)
- After 3 failures: sets `status:blocked` + escalation to #neo Discord
- No more limbo — fully automatic retry loop

## 15:17 — DECISION: Neo Job Queue in Mission Control (Option B)

Mike wants Neo's job queue visible in MC. Two options discussed:
- **Option A:** Full API endpoint + dashboard page (more work)
- **Option B:** Wrap `rabbit-status.py` output as JSON API + status card (1-2 days, recommended)

**Mike chose Option B** — wrap `rabbit-status.py` as JSON API + expandable status card on `/agents` page.

Created:
- **GitHub slideheroes-internal-tools#23** — Agent Pipeline Status on /agents page
- **MC #685** — tracking task

Design: `/api/agents/neo/pipeline` endpoint reads state files directly, AgentCard gets expandable Pipeline Status section. Extensible to all agents later.

## 18:25 — CodeRabbit Missing on internal-tools Repo

Mike noticed #22 and #23 on `slideheroes-internal-tools` had no CodeRabbit plans after several hours. Root cause: `.coderabbit.yaml` had PR review config but was **missing `issue_enrichment.planning` section** — the `plan-me` label trigger for auto-planning wasn't configured.

**Fix:** Added `issue_enrichment.planning` config (matching `2025slideheroes`), committed directly, re-toggled labels to trigger. Plans should appear within minutes.

**Learning:** When adding `plan-me` workflow to a new repo, always verify `.coderabbit.yaml` has the `issue_enrichment.planning` section, not just review config.

**Update 18:38:** Config alone wasn't enough — CodeRabbit App was installed but label auto-trigger wasn't firing. Manual `@coderabbitai plan` comment worked. Plans now generating for both #22 and #23.

## 18:46 — Neo Multi-Repo Pickup Implemented

Neo's pickup script was hardcoded to `slideheroes/2025slideheroes` only. Extended to support multiple repos:

**Changes:**
- Added `WATCHED_REPOS` config in `common.py` — list of repo configs with workflow type (fork vs direct), local dir, base branch, repo IDs
- Rewrote `neo-issue-pickup.py` to iterate over all watched repos
- Different task prompts for fork workflow (GraphQL PR creation) vs direct workflow (`gh pr create`)
- Repo-scoped dedup keys (e.g., `slideheroes-internal-tools:23`) to avoid ID collisions
- Updated `process-spawn-queue.py` to infer repo from label for failure retry
- Created `status:in-progress` and `status:blocked` labels on internal-tools repo

**Test:** Pickup ran successfully, queued both #23 (Pipeline Status) and #22 (Content Board) from internal-tools. Neo spawned on #23.

## 18:50 — PRs #2219 + #2220 Merged, #2216 Unblocked

Mike merged F1 (Bifrost Deployment, #2220) and F2 (Gateway Refactor, #2219). All three prereqs for F4 now merged.

**#2216 (F4: Integration & Cleanup)** had failed CodeRabbit planning ("ticket too vague"). Rewrote the issue body with specific tasks: Portkey dependency removal, grep commands, exact files, CI checks. Re-triggered `@coderabbitai plan` — plan generated (3 CR comments now).

## 18:54 — Neo Completed internal-tools #23 → PR #24

Neo built Agent Pipeline Status page for internal-tools. PR #24 open.

## 19:07 — Neo Also Picked Up #2200

Neo processed issue #2200 (Library section in left nav) from the 2025slideheroes backlog. Multi-repo pickup working — queue draining correctly.

**Current Neo status:** Idle, queue empty. #2216 has a CodeRabbit plan now — should get picked up on next cron cycle (≤10 min).

## 19:30 — Neo Spawned on #2199 (Template Selection UI)

Re-queued #2199 and Neo picked it up. Working on template selection UI for Generate step.

## 19:47 — MC ↔ GitHub Template Task Audit

Three MC tasks were out of sync with GitHub:
- **MC #629** (Step 2): backlog → **done** (PR #2206 merged, GH #2205 closed)
- **MC #631** (Step 4): backlog → **in_progress** (Neo working on GH #2199)
- **MC #632** (Step 5): backlog → **mike_review** (PR #2221 open for review)

---

### CodeRabbit Review on PR #2211 (2:46 PM)
**PR:** feat: define 5 curated presentation template configurations
**Latest review** (3rd pass, commit 2961d93): 2 new actionable + 1 duplicate

**New actionable items:**
1. **`server-only` import** (Major refactor) — Add `import "server-only"` as first line of `pptx-generator.ts` to prevent client bundling
2. **Truthy check on template override** (Minor) — Change `if (template)` to `if (template !== undefined)` at line ~251 to avoid skipping valid-but-empty inputs

**Duplicate (still open):**
3. **Type guard hardening** — `isCuratedTemplateConfig` still too permissive; needs full validation of density enum, typography fields, and color fields

**Previous review items (from earlier pass):**
- Template state leak across calls (outside-diff, Major) — `generateFromStoryboard()` mutates instance fields
- Export `Position` interface (nitpick)
- `getDefaultTemplate` can return undefined (actionable)
- Use i18n keys instead of plaintext name/description (actionable)
- Extract test constants for maintainability (nitpick)

Status: Awaiting Neo's next push to address these.

## 3:16 PM — CodeRabbit Review Round 3 on PR #2211 (cron)

PR: feat: define 5 curated presentation template configurations
Branch: sophie/alpha/templates-step3-v2 → dev

**Round 3 (18:46 UTC) — 2 new comments:**

1. **🟠 Major — Add `server-only` import** (`pptx-generator.ts:13`)
   - Module is server-exclusive (Node buffers, pptx generation). Add `import "server-only"` as first import to prevent client bundling.
   - Simple one-liner fix.

2. **🟡 Minor — Fix truthy check on template override** (`pptx-generator.ts:251`)
   - `if (template)` should be `if (template !== undefined)` to handle empty-string edge case.
   - Simple one-liner fix.

**Prior rounds (still open):**
- Round 1: Type guard hardening (`isCuratedTemplateConfig`), `getDefaultTemplate` undefined safety, i18n keys suggestion
- These were partially addressed in round 2 push

Status: Both new items are minor fixes, worth addressing. Logged for Mike's awareness.

## CodeRabbit Review — PR #2220 (Bifrost Gateway Deployment) — 5:48 PM

CodeRabbit posted a review with **11 actionable comments** on PR #2220 (feat: Bifrost Gateway Deployment on EC2 with Cloudflare Tunnel).

**Key issues flagged:**
1. **config.json** — CORS wildcard origin too permissive
2. **docker-compose.tunnel.yml** — Overlapping volume mounts (file mount masked by directory volume)
3. **docker-compose.yml** — Config file not mounted into container
4. **README.md** — Script filename mismatch (verifyExternal.sh vs verify-external.sh)
5. **verify-external.sh** — `info` function called before defined (fails under `set -e`)
6. **verify-external.sh** — Broken single-quote JSON in curl `-d` payloads (2 occurrences)
7. **verify-external.sh** — Health check false-positives (greps "status" which matches error responses)
8. **verify-external.sh** — Unused/wrong CURL_OPTS with incorrect CF header name
9. **verify-local.sh** — Provider check failures don't exit non-zero
10. **verify-local.sh** — Same single-quote JSON breakage as external script
11. **verify-local.sh** — Container name check matches substrings (bifrost-tunnel)

All are legitimate issues. This PR is Neo's work — fixes should go through Neo.

## 17:48 — CodeRabbit Reviews (Cron)

**PR #2220** (Bifrost Gateway Deployment on EC2): 11 new comments from CodeRabbit
- 🔴 4 Critical: broken JSON quoting in verify scripts, `info` called before defined, docker volume overlap hiding config.yml, docker-compose issue
- 🟠 4 Major: container name check too broad, provider failures don't fail exit code, health-check false-pass, wildcard CORS on exposed gateway
- 🟡 1 Minor: README script filename mismatch
- Plus 2 more comments

**PR #2219** (Gateway Client Refactor): 1 new comment
- 🟡 Minor: missing validation for CF Access credentials when Bifrost configured

Both PRs are Sophie's fork → upstream dev. Need Neo to address these review comments.

## 18:51 — CodeRabbit Plan Check (Issue #2216)
- Cron fired for issue #2216 "Integration Testing & Portkey Cleanup"
- CodeRabbit initially said ticket lacked detail (19:04 UTC)
- SophieLegerPA re-triggered with `@coderabbitai plan` at 23:51 UTC
- CodeRabbit responded "plan generation in progress" — plan hasn't landed yet
- Will need to check back later for the actual plan

## 19:00 — CodeRabbit Coding Plan: Issue #2216

CodeRabbit posted a coding plan on **Issue #2216: [Feature] Integration Testing & Portkey Cleanup**.

**Plan Summary:** Documentation & config cleanup to finish the Portkey → Bifrost/Langfuse migration. All source code changes were already completed in prior PRs — this is purely docs/config/templates.

**3 Phases:**
1. **Configuration Cleanup** — Remove Portkey env vars from `.env.development`, add Bifrost/Langfuse placeholders, verify `.env.example` files
2. **Documentation Updates** — Rewrite ai-gateway README, update root README, update local dev docs, delete duplicate `README copy.md`, update GitHub issue templates (replace "Portkey" placeholders), add integration docs to Bifrost README
3. **Verification & CI** — grep for remaining Portkey references, run typecheck/lint/format, verify lockfile is clean

**Design Choices:**
- Update `.env.development` (not leave unchanged) since it's committed and serves as dev baseline
- Replace Portkey placeholder examples in issue templates for consistency

This is a straightforward cleanup task — no code logic changes, just docs and config alignment. Good candidate for Neo.

## 19:00 — CodeRabbit Coding Plan: Issue #2216

CodeRabbit posted a coding plan on issue #2216 "[Feature] Integration Testing & Portkey Cleanup". Plan is docs/config cleanup only (source code already done). 3 phases, ready for Neo pickup.

## 19:12 — CodeRabbit Review on PR #2221 (Library Navigation)

New CodeRabbit review on PR #2221 "feat: add Library section to left navigation (Templates + Profiles)":
- **6 actionable comments** + 1 nitpick
- **2 Major issues:**
  1. Template thumbnails not rendered — cards use synthetic gradients instead of `thumbnailUrl`/`previewUrl` from config
  2. Company Profiles tab is empty/unimplemented — always shows empty state, never queries saved company briefs
- **4 Minor issues:**
  1. Hard-coded UI labels + forced `en-US` date formatting in profiles component
  2. Another i18n issue in profiles component
  3. Template labels/actions not using translation keys (`Trans` component)
  4. Issue in `templates.config.ts` (details truncated)
- **1 Nitpick:** Missing error handling for Supabase auth/query in library page

Logged for Mike's awareness. These are on Sophie's fork PR — Neo can address if Mike approves.

## 7:12 PM — CodeRabbit Review on PR #2221

PR: feat: add Library section to left navigation (Templates + Profiles)
Review: 6 actionable + 1 nitpick across 4 files.
Key issues: missing i18n, accessibility (SheetTrigger wrapping Card), missing company_briefs query for Company Profiles tab, schema validator mismatch with relative paths, silent error swallowing.

## 7:12 PM — CodeRabbit Review on PR #2221 (Library Navigation)

CodeRabbit reviewed PR #2221 "feat: add Library section to left navigation (Templates + Profiles)" with **6 actionable comments + 1 nitpick** across 4 files:

### 🟠 Major (2):
1. **library-templates.tsx:39** — Template cards use synthetic gradients instead of actual thumbnail/preview assets from config. Should render `template.thumbnailUrl` with img/Image, fallback to gradient.
2. **library/page.tsx:38** — Company Profiles tab is always empty — page only queries `audience_profiles`, never fetches `company_briefs`.

### 🟡 Minor (4):
1. **library-profiles.tsx:37** — Hard-coded UI text and fixed `"en-US"` locale in `formatDate`. Should use i18n `Trans` component and dynamic locale.
2. **library-profiles.tsx:46** — SheetTrigger wraps non-semantic Card component — accessibility issue for screen readers. Should use Button or add `role="button"`.
3. **library-templates.tsx:72** — Hard-coded "Colors:" and "Use this template" strings should use Trans/i18n keys.
4. **templates.config.ts:24** — `TemplateSchema` validators use `z.string().url()` but `CURATED_TEMPLATES` uses relative paths — schema mismatch.

### 🧹 Nitpick (1):
- **library/page.tsx:26-38** — Supabase auth/query errors silently treated as empty data. Should add try/catch with structured logging.


## 7:52 PM — CodeRabbit Review on PR #2221

**PR:** #2221 "feat: add Library section to left navigation (Templates + Profiles)"
**Review:** 6 actionable comments + 1 nitpick across 4 files

### Actionable Comments:
1. **library-profiles.tsx** — Hard-coded "en-US" locale in formatDate + hard-coded UI labels → use i18n Trans component
2. **library-profiles.tsx** — SheetTrigger wraps non-semantic Card → accessibility issue, should use Button
3. **library-templates.tsx** — Synthetic gradient placeholder instead of actual template thumbnail/preview images
4. **library-templates.tsx** — Hard-coded strings ("Colors:", "Use this template") → use Trans i18n
5. **library/page.tsx** — Company Profiles tab never populated (only queries audience_profiles, not company_briefs)
6. **templates.config.ts** — TemplateSchema uses z.string().url() but templates use relative paths

### Nitpick:
7. **library/page.tsx** — Silent error handling on Supabase auth/query failures → add try/catch with logger


## 7:59 PM — CodeRabbit Review on PR #2222

**PR:** #2222 "feat(canvas): add template selection UI to Generate step"
**Review:** 10 actionable comments + 1 nitpick across 6 files

### Categorized:

### 🔴 Validation/Security (3):
1. **save-template.action.ts:8-11** — SaveTemplateSchema allows any string for templateId. Should validate against allowed template IDs (z.enum or z.union).
2. **generate/page.tsx:56** — Raw DB value `template_id` passed to client without validation. Should normalize/validate server-side before passing as prop.
3. **export-powerpoint.action.ts:17** — Zod schema allows any string for templateId. Should validate against template registry.

### 🟠 Data Integrity (2):
4. **generate-step.tsx:69-71** — `selectedTemplateId` initialized as untrusted string. Should validate/normalize to TemplateId type with fallback.
5. **generate-step.tsx:82-91** — `saveTemplateAction` fires per click with no debounce. Rapid clicks cause out-of-order saves. Should debounce/serialize.

### 🟡 UI/Display (2):
6. **template-color-swatches.tsx:27-29** — Raw hex values missing '#' prefix applied to style/title. Should normalize colors.
7. **template-picker.tsx:85-88** — Gradient built from raw color tokens missing '#'. Also falls back to CSS class "bg-muted" which is invalid for inline CSS.

### 🟡 Type Safety (1):
8. **template-picker.tsx:39-42** — `handleValueChange` casts string to TemplateId without validation. Should check against allowed IDs.

### 🟡 i18n (1):
9. **template-picker.tsx:48-51** — Hard-coded heading/description text. Should use Trans component.

### 🟢 Schema (1):
10. **presentation-template.schema.ts:49-50** — Colors array allows any string. Should validate hex format with regex.

### 🧹 Nitpick (1):
11. **save-template.action.ts:1-6** — Missing `import "server-only"` marker.

## 19:59 — CodeRabbit Reviews on PRs #2221 & #2222

**PR #2222** (Template Selection UI for Generate step) — 10 actionable + 1 nitpick:
- Hex color validation: colors schema should validate hex strings, not accept any string
- Template ID validation: `SaveTemplateSchema` should use enum/literal for allowed IDs, not `z.string().min(1)`
- Type safety: `selectedTemplateId` state should be typed as `TemplateId`, not raw string
- Debounce saves: rapid template clicks can fire out-of-order saves — needs debounce/serialization
- Server-side validation: normalize `template_id` from DB before passing to client component
- Export action: validate templateId against registry in export-powerpoint action
- Color swatch normalization: prepend `#` to raw hex values like `1a1a2e`
- i18n: hardcoded heading/description in TemplatePicker should use `<Trans>`
- Template picker validation: `handleValueChange` should validate before casting to `TemplateId`
- Gradient colors: template preview gradient uses raw hex tokens missing `#` prefix
- Nitpick: add `import "server-only"` to save-template action

**PR #2221** (Library section — Templates + Profiles) — 6 actionable + 1 nitpick:
- i18n: hardcoded labels in library-profiles and library-templates need `<Trans>`
- Locale: `formatDate` hardcodes "en-US" — should use app locale
- Accessibility: SheetTrigger wrapping Card is non-semantic — needs button role
- Template images: cards show synthetic gradient instead of `thumbnailUrl`/`previewUrl`
- Company Profiles tab: never populated — page only queries `audience_profiles`, not `company_briefs`
- Schema mismatch: `TemplateSchema` uses `z.string().url()` but templates use relative paths
- Nitpick: wrap Supabase auth/query in try/catch with structured error logging

No immediate action needed — these are queued for Neo to address when Mike approves the PRs or asks for fixes.

### 7:59 PM — CodeRabbit Reviews (Round 2)

**PR #2222 (Template Selection UI)** — 10 comments (2 critical, 6 major, 2 minor):
- 🔴 Critical: Normalize swatch colors before applying inline style (template-color-swatches.tsx:29)
- 🔴 Critical: Fix preview gradient color formatting and fallback CSS (template-picker.tsx:88)
- 🟠 Major: Constrain palette entries to valid hex colors (presentation-template.schema.ts:50)
- 🟠 Major: Validate templateId against allowed IDs (save-template.action.ts:11)
- 🟠 Major: Initialize selectedTemplateId from validated TemplateId (generate-step.tsx:71)
- 🟠 Major: Guard against out-of-order template saves on rapid selection (generate-step.tsx:91)
- 🟠 Major: Unchecked cast to TemplateId in selection handler (template-picker.tsx:42)
- 🟠 Major: Replace hard-coded copy with Trans for i18n (template-picker.tsx:51)
- 🟠 Major: export-powerpoint.action.ts issue (details collapsed)
- 🟡 Minor: Normalize template_id before passing to client (page.tsx:56)

**PR #2221 (Library Navigation)** — 6 comments (2 major, 4 minor):
- 🟠 Major: Render template thumbnail/preview assets from config (library-templates.tsx:39)
- 🟠 Major: Company Profiles tab not implemented yet (library/page.tsx:38)
- 🟡 Minor: Localize profile labels and date formatting (library-profiles.tsx:37)
- 🟡 Minor: Move template labels/actions to translation keys (library-templates.tsx:72)
- 🟡 Minor: Two details-collapsed items in library-profiles.tsx and templates.config.ts

Queued for Neo when Mike approves fixes.

## CodeRabbit Reviews — 7:59 PM

### PR #2222 — Template Selection UI (10 comments)
- 🔴 **2 Critical**: Color formatting — swatch colors missing `#` prefix in inline styles; gradient colors also missing `#` prefix + fallback CSS issues
- 🟠 **6 Major**: Hex color validation in schema; templateId validation in save action; validated TemplateId initialization; race condition on rapid template selection; export action analysis chain; unchecked cast to TemplateId; hard-coded copy needs i18n
- 🟡 **1 Minor**: Normalize template_id before client pass-through
- **Status**: Open, not yet merged

### PR #2221 — Library Navigation (6 comments)
- 🟠 **2 Major**: Template thumbnailUrl/previewUrl unused (synthetic gradients instead); Company Profiles tab unimplemented (always empty)
- 🟡 **3 Minor**: Hard-coded profile labels/date formatting; library-profiles analysis chain; template labels need i18n keys
- 🟡 **1 Minor**: templates.config.ts analysis chain
- **Status**: Open, not yet merged
- **Pre-merge failed**: Docstring coverage 20% (needs 80%)

### PR #2222 — CodeRabbit Review #2 (7:59 PM)
- **PR**: feat(canvas): add template selection UI to Generate step
- **Review**: 10 actionable comments + 1 nitpick across 9 files
- **🔴 Critical (2)**:
  - `template-color-swatches.tsx`: Raw hex values without `#` prefix break inline CSS
  - `template-picker.tsx`: Gradient preview uses raw values like `1a1a2e` (no `#`) and falls back to CSS class token in inline style
- **🟠 Major (5)**:
  - `presentation-template.schema.ts`: Colors schema accepts any string, should validate hex format
  - `save-template.action.ts`: templateId accepts any string, should validate against allowed IDs
  - `generate-step.tsx`: selectedTemplateId initialized from untrusted string, needs validation
  - `generate-step.tsx`: No debounce on save — rapid clicks cause out-of-order persisted template_id
  - `export-powerpoint.action.ts`: Zod schema allows any string for templateId
  - `template-picker.tsx`: Unchecked cast `as TemplateId` without validation
  - `template-picker.tsx`: Hard-coded strings need i18n (Trans component)
- **🟡 Minor (1)**: `generate/page.tsx`: Normalize raw DB template_id before passing to client
- **Nitpick (1)**: `save-template.action.ts`: Add `import "server-only"`
- **Theme**: Consistent pattern — hex colors stored without `#` prefix cause CSS issues; templateId validation too loose throughout

## CodeRabbit Reviews — 7:59 PM

**PR #2222** (Template Selection UI for Generate step) — 10 actionable + 1 nitpick:
- 🔴 Critical (2): Raw color values missing `#` prefix in swatches and gradient preview
- 🟠 Major (6): Hex color validation in schema, templateId validation, state initialization, race condition on rapid selection, unchecked `as TemplateId` cast, template_id in export action
- 🟡 Minor (1): Normalize template_id before passing to client
- Nitpick (1): Mark server action as server-only
- i18n (1): Hard-coded copy needs Trans component

**PR #2221** (Library nav section) — 6 actionable + 1 nitpick:
- 🟠 Major (2): Template thumbnails not using config assets (synthetic gradients only), Company Profiles tab not implemented
- 🟡 Minor (3): i18n for profile labels/dates, localize template labels, Supabase error handling
- Nitpick (1): templates.config.ts analysis

Both PRs queued for Neo to address. Most urgent: color formatting bugs in #2222 (Critical).
