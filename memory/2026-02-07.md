# 2026-02-07 — Council v2 Multi-Agent Deliberation

*Truncated 2026-03-01 — full history in git*

## Council v2 Built
Multi-agent deliberation system with true multi-model debates:
- Claude (Opus), GPT-5.2, GLM on separate spawns
- Main session orchestrates — sub-agents cannot spawn sub-agents
- Database: debates, rounds, turns, synthesis
- REST API + webhook integration
- UI: New Debate button, live polling, synthesis display

## Config Discovery
`tools.subagents.tools.allow` and `deny: []` don't override hardcoded restriction. Clawdbot design: "sub-agents cannot spawn sub-agents" — prevents nested fan-out.

## Debates Run
1. SSR vs CSR → Consensus: Next.js App Router with SSR + client islands
2. Freemium vs Free Trial → Consensus: 14-day free trial
3. Mobile app vs Web-only → Consensus: Web-only with PWA roadmap
