# 2026-02-16 — Monday

## [02:57] Initiative Linking — All 113 Orphan Tasks Assigned
- Queried all tasks with no `initiative_id` (excluding done) — found 113 unlinked tasks
- Worked through 6 batches of ~20 with Mike reviewing each batch
- Key decision: All Mastra-related tasks (#473-490) → **Ship the Core Product Experience** (initiative 15), not split across multiple initiatives
- Key decision: All Loops-related tasks → **Build the Customer Data Ecosystem** (initiative 11), not Platform Migration
- Cleaned up 4 exact duplicate tasks: #385/#415, #386/#416, #387/#417, #388/#418 (kept higher-numbered versions)
- All 30 initiatives now have tasks linked

## [02:57] GLM False Billing Cooldown — Recurring Issue
- Hit the ZAI/GLM false billing cooldown **twice** in one session (02:57 and 03:10)
- Both times fixed with `gateway restart`
- This keeps recurring — need a permanent fix (either OpenClaw config or upstream)

## [15:23] Morning Brief Email Not Sent — GPT Sub-Agent Error
- Mike reported no email version of today's morning brief
- **Root cause:** GPT-5.2 sub-agent incorrectly classified `msmith@slideheroes.com` as an "external recipient" and skipped the `gog gmail send` step
- Sent today's email manually
- **Fix:** Updated cron job `2cd6c98b` prompt with:
  - `⚠️ MANDATORY` label on email step
  - Explicit "Do NOT skip" / "Do NOT claim external recipient" instructions
  - Added `timeout 60s` to data script (was also hanging)
- **Lesson:** GPT sub-agents will sometimes refuse safe internal actions out of caution. Critical delivery steps need emphatic, unambiguous instructions in cron prompts.

## [~16:00] Agentic Layer Initiative Created
- Created initiative **"Build the Agentic Layer (Mastra)"** (id: 36) under Objective 2 (Build a Product Customers Love)
- Moved 19 tasks (#438, #473-490) from Core Product Experience → Agentic Layer
- Mike decided: product-focused (Obj 2), not AI Systems (Obj 6)
- Tagged 7 existing docs with "Agentic Layer", added auto-tag rule to `_utils.ts` for filenames containing "agentic", "mastra", "wow4-agent"

## [~17:00] Gap Analysis & New Tasks
- Identified 8 missing tasks, created #493-#500 filling gaps (migration strategy, testing, observability, security, etc.)
- **27 total tasks** now in the Agentic Layer initiative
- Created comprehensive implementation plan: `deliverables/agentic-layer-implementation-plan.md`
- Critical path: #498 (migration) → #474 (Mastra setup) → #476 (Zod schemas) → #483 (agents) → #484 (orchestration) → #485/#496 (UI)
- 5 open decisions for Mike: which 2 agents ship first, pricing model, research data sources, Mastra vs alternatives, results privacy

## [~18:00] Codebase Review of 2025slideheroes
- Explored full monorepo: apps (web, payload CMS, dev-tool, e2e, load tests), packages (ai-gateway, e2b, billing, analytics, supabase, mcp-server, monitoring)
- ai-gateway: OpenAI SDK + Portkey gateway, usage tracking/cost estimation, credit bypass flags
- e2b: sandboxed code execution package
- Existing agent infra in `.agents/skills/`, `.claude/agents/` (alpha, research, zeta), `.ai/` directory
- **No Mastra files exist yet** — greenfield integration
- Review still in progress when session compacted

## [~17:46] WOW #1 Initiative Created + Task Breakdown
- Created initiative **"WOW #1 — Audience Profiling"** (id: 37) under Objective 2
- Moved #477, #479 from Agentic Layer to WOW #1
- Created 10 new tasks (#508-#517): LinkedIn data providers, person/company research engines, Audience Brief UI, context flow design, saved profiles, multi-audience, PostHog metrics, LinkedIn legal, sparse data handling
- Todoist section created: "WOW 1 Audience Profiling" (sectionId: `6g32h5m2fH5gMFPh`) and added to `todoist-mc-mapping.json` as initiative 37

## [~17:50] Codebase Review — WOW #1 Nuances from 2025slideheroes
- **Existing audience code found:** ai-gateway has `audience-suggestions` config/template/prompt, but only generates 4 generic role labels from title. No external research.
- **DB schema:** `building_blocks_submissions.audience` is a single nullable 200-char string. Needs structured Audience Brief.
- **Form flow:** Audience is step 3 of 7 in SCQA form. WOW #1 needs it as step 1.
- **`presentation-context.ts`** injects `{{audience}}` as flat string into all downstream prompts — needs full brief injection.
- **Onboarding collects role + industry** — untapped signal for audience recommendations.
- **No external data integrations** — zero LinkedIn/web enrichment. Entirely greenfield.
- Created 6 additional codebase-informed tasks (#518-#523): form refactor, DB migration, ai-gateway upgrade, onboarding leverage, profiles table, context partial update
- **18 total tasks** in WOW #1 initiative

## [~18:05] Context Engineering SOP
- Mike shared 5-stage context engineering framework: Curate → Compress → Structure → Deliver → Refresh
- **Key insight:** This applies across the entire product, not just WOW #1 — SCQA prompts, storyboard generation, agentic layer, knowledge engine
- Created SOP at `sops/context-engineering.md` with SlideHeroes-specific examples, PR review gate, checklist, token budgets
- Created task #524: "Audit all ai-gateway prompt templates against Context Engineering SOP"
- **MC Docs API doesn't exist yet** — `/api/v1/docs` returns 404. Doc registration deferred.

## Lessons Learned
- **MC API field naming**: `initiativeId` (camelCase) works on PATCH. `initiative_id` (snake_case) silently ignored on PATCH — caused entire first batch of assignments to fail with no error. Always use camelCase for PATCH.
- **Todoist API v1 migration**: `rest/v2/sections` is deprecated, use `api/v1/sections`
- **PostHog→BigQuery sync** has 3 consecutive timeout errors (job `30430f69`) — needs attention
