# 2026-02-15 (Sunday)

## [15:38] Feed Monitor Overhaul

Mike flagged that morning brief only recommends Reddit posts. Root cause analysis:
- 9 Reddit feeds had the only non-zero reputation scores (up to 4.25)
- Scoring formula: `llmScore×4 + contentDepth×2 + feedReputation×3 + recency×1` — reputation weight gave Reddit 1.5-12.75 point advantage
- All blogs/newsletters stuck at 0.0 reputation
- No presentation/consulting feeds at all

**Changes made:**
- Removed all 9 Reddit feeds (1,093 items) — Mike's decision
- Removed 13 dead/broken feeds (404s)
- Removed Kyle Poyar/Growth Unhinged — migrated to beehiiv, no RSS available
- Added: Duarte Blog, SlideModel, Visme (Presentations & Design), Lenny's Newsletter, SaaStr, Stratechery (Product/Strategy)
- Several consulting feeds (McKinsey, BCG, Bain, HBR, First Round, Gamma, Canva) had broken RSS URLs — removed

**Reputation scores — Mike's picks (3.0):** The Signal, Elena's Growth Scoop, Cannonball GTM. Modest boost (1.5-2.0): Lenny's, Stratechery, Duarte. Everything else: 0 — earn their way up.

**Lesson:** Don't assume reputation scores for feeds. Let Mike tell us which ones he values.

## [18:26] Agentic Layer Infrastructure Research

Mike requested research on infrastructure for WOW #4 (Agent Layer). Must-evaluate: Letta, Master AI, Calljmp. First sub-agent stalled (0 tokens processed in 30 min) — had to respawn.

**Lesson:** Sub-agent stalls happen. Check `totalTokens` in session list — if still 0 after 5+ min, respawn. Set tighter timeouts and more focused prompts.

Deliverable: `deliverables/agentic-layer-infrastructure-research.md`. Key findings:
- Letta: clear pricing ($0-$200/mo), TS SDK, stateful memory — strong for memory layer
- Calljmp: TS-first managed agentic backend, $20/$99 + usage
- LangGraph JS: durable execution + HITL + memory, but self-hosted infra
- OpenAI Assistants API: being sunset 1H 2026, shifting to Agents/Responses platform
- Master Agent AI: insufficient developer docs to recommend
- Recommendation: LangGraph JS + Vercel AI SDK + Postgres/pgvector + queue/worker

Awaiting Mike's review.

## [19:13] Mastra AI — Corrected Research Target

Mike clarified "Master AI" was actually **Mastra AI** (mastra.ai) — from the Gatsby team, $13M YC seed, 150K weekly npm downloads. Purpose-built TypeScript agent framework with workflows, memory, HITL, evals, observability. v1 shipped Jan 2026.

**Key capabilities:** Agent definitions, graph-based workflows (`.then()`, `.parallel()`, `.branch()`), suspend/resume for HITL, working memory + semantic recall + message history, 40+ model providers, Next.js native, built-in evals/scorers and tracing.

**Mastra vs Portkey:** Complementary, not competing. Mastra = orchestration layer (what agents do). Portkey = plumbing layer (model gateway, fallbacks, caching, cost tracking). Mastra's built-in model routing likely sufficient for beta; Portkey adds value at scale.

**Deep evaluation completed:** `deliverables/mastra-deep-evaluation.md` — maps all 12 SlideHeroes agents to Mastra's model, TypeScript code sketches, architecture proposal, gaps identified (UI HITL, cost-in-$ accounting, product DB schemas still need custom build).

**Mastra is the leading candidate** for our agentic layer infrastructure. Awaiting Mike's review of the evaluation.

## [20:15] Session Compaction — State Snapshot

Key state captured from pre-compaction summary:

**Blockers (unchanged):**
- Apollo API credits exhausted (~100/mo free plan)
- #465 RB2B needs Mike's account + pixel install
- #467 AC→Loops needs Mike's AC exports/API key
- #339 Sleekplan needs Mike to sign up
- Airbyte EC2 still running (~$1/day waste)

**Overnight completions (verified):** #260 Notion Reorg, #296 Readwise Config, #438 Agentic UI Redesign, #469 Apollo TAM list, #471 Kanban blocked tasks (in mike_review).

**Continuation crons working:** 11pm→1am→2am→4am schedule enabling 3-5 tasks/night vs previous 1.
