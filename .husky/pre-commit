#!/bin/bash
# Optimized Husky pre-commit hook for SlideHeroes

echo "🚀 Starting optimized pre-commit checks..."

# Use lint-staged with limited concurrency to prevent SIGKILL errors
echo "🔄 Running lint-staged with concurrency limit..."
npx lint-staged --concurrent 2

echo "✅ Pre-commit checks completed!"
