#!/bin/bash
# Optimized Husky pre-commit hook for SlideHeroes

echo "🚀 Starting optimized pre-commit checks..."

# Use lint-staged for parallel processing of files
echo "🔄 Running lint-staged for parallel file processing..."
npx lint-staged

echo "✅ Pre-commit checks completed!"
