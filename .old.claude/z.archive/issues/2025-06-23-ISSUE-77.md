# Issue: Async logger support needed in storage-url-generators.ts

**ID**: ISSUE-77
**Created**: 2025-06-23T14:31:54Z
**Updated**: 2025-06-23T14:31:54Z
**Reporter**: MLorneSmith
**Status**: open
**Labels**: bug, needs-investigation, low, logging

## GitHub Issue Content

# Issue: Async logger support needed in storage-url-generators.ts

**ID**: ISSUE-[github_issue_number]
**Created**: 2025-06-23T10:26:00Z
**Reporter**: Claude Debug Assistant
**Severity**: low
**Status**: new
**Type**: bug

## Summary

The storage URL generator implementation (`apps/payload/src/lib/storage-url-generators.ts`) has commented out logger calls with TODO notes indicating that async logger support is needed. This prevents proper logging of URL generation operations, warnings for missing filenames, and error tracking. The tests were expecting console logging but have been updated to not expect any logging since the implementation doesn't actually log.

## Environment

- **Application Version**: 2.11.0
- **Environment**: development/staging/production
- **Node Version**: As per project requirements
- **Last Working**: Never (TODOs indicate this was never implemented)

## Reproduction Steps

1. Open `apps/payload/src/lib/storage-url-generators.ts`
2. Look at lines 119, 127, and 133
3. Observe commented out logger calls with TODO comments
4. Run tests and note that logging expectations were removed

## Expected Behavior

The storage URL generator should:

- Log warnings when filename is missing
- Log generated URLs in development mode for debugging
- Log errors when URL generation fails
- Use the async logger pattern consistent with the rest of the codebase

## Actual Behavior

Logger calls are commented out with TODO notes:

- Line 119: `// TODO: Async logger needed`
- Line 127: `// TODO: Async logger needed`
- Line 133: `// TODO: Async logger needed`

No logging occurs despite the clear intent to log these events.

## Diagnostic Data

### Code Analysis

```typescript
// Line 116-121
if (!filename) {
  // Instead of throwing, log warning and return fallback URL
  // TODO: Async logger needed
  // (await getLogger()).warn(`[URL-GENERATOR] Warning: Filename is missing for ${collection} URL generation. Returning fallback URL.`, { data:  });
  return `/uploads/placeholder-${collection}.png`;
}

// Line 125-129
if (process.env.NODE_ENV === "development") {
  // TODO: Async logger needed
  // (await getLogger()).info(`[URL-GENERATOR] Generated ${collection} URL: ${url}`);
}

// Line 131-137
} catch (_error) {
  // TODO: Async logger needed
  // (await getLogger()).error(`[URL-GENERATOR] Error generating URL for ${collection}/${filename}:`, { arg1: error, arg2:  });

  // Return a fallback URL
  return `/uploads/${filename || "unknown"}`;
}
```

### Test Modifications

The tests in `storage-url-generators.test.ts` were updated to not expect console logging:

- Tests now use `expect(mockConsoleWarn).not.toHaveBeenCalled()`
- Comments added: "Logger calls are commented out in implementation with TODO"

## Related Code

- **Affected Files**:
  - `apps/payload/src/lib/storage-url-generators.ts`
  - `apps/payload/src/lib/storage-url-generators.test.ts`
- **Logger Import**: Already imported as `createServiceLogger` from `@kit/shared/logger`
- **Logger Pattern**: Uses `getLogger()` but needs async support

## Initial Analysis

The implementation already has:

1. Logger service imported and initialized
2. Clear logging points identified
3. Appropriate log levels chosen (warn, info, error)
4. Structured logging format prepared

The issue is that the `createURLGenerator` function is synchronous but needs to await the logger. This requires either:

1. Making the generator function async
2. Initializing the logger differently to avoid async requirement
3. Using a synchronous logging approach

## Suggested Investigation Areas

1. Review how other parts of the codebase handle async logging in synchronous contexts
2. Consider if `createURLGenerator` can be made async without breaking consumers
3. Investigate if the logger can be initialized once and reused synchronously
4. Look at similar patterns in other storage-related files

## Additional Context

This issue was discovered while fixing test failures in issue #76. The tests were expecting console logging but the implementation had the logger calls commented out. Rather than just fixing the tests, this issue tracks the need to implement the actual logging functionality.

The logging would be valuable for:

- Debugging URL generation issues in development
- Tracking missing filename errors in production
- Monitoring URL generation failures
- Understanding usage patterns of different storage types

---

_Generated by Claude Debug Assistant_
_Tools Used: Code analysis, Git commands_

---

**Auto-Generated Local Cache**

- GitHub Issue: #77
- GitHub URL: <https://github.com/MLorneSmith/2025slideheroes/issues/77>
- Cached: 2025-06-23T15:57:59.780Z
- Status: auto-sync cache (read-only)
- Source: GitHub (authoritative)
