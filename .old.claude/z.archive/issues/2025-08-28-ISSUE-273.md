# Issue: [Bug] Authentication Redirect Failure - App Not Redirecting After Successful Login

**ID**: ISSUE-273
**Created**: 2025-08-28T20:48:39Z
**Updated**: 2025-08-28T23:09:17Z
**Reporter**: MLorneSmith
**Status**: open
**Labels**: bug, high-priority, e2e, authentication, navigation

## GitHub Issue Content

## Issue Summary

E2E authentication tests are failing due to application redirect failures after successful authentication. Tests show that users successfully authenticate but the application fails to redirect to the expected pages (/onboarding or /home), causing TimeoutError on page.waitForURL operations. This appears to be an application bug, not a test infrastructure issue.

## Environment

- **Branch**: dev
- **Test Framework**: Playwright 1.53.1
- **Test Environment**: Local E2E (port 3010)
- **Failure Rate**: 100% for redirect-dependent auth tests
- **Last Known Working**: Unknown

## Test Failures

### Failed Tests from junit.xml

1. **"will sign-in with the correct credentials"**
   - File: `apps/e2e/tests/authentication/auth.spec.ts:60`
   - Error: `TimeoutError: page.waitForURL: Timeout 120000ms exceeded`
   - Waiting for: Navigation to `/home` or `/onboarding`

2. **"when logged out, redirects to the correct page after sign in"**
   - File: `apps/e2e/tests/authentication/auth.spec.ts:194`
   - Error: `TimeoutError: page.waitForURL: Timeout 120000ms exceeded`
   - Expected: Redirect to `/onboarding` after sign-in

## Root Cause Analysis

### Evidence of Successful Authentication

- Tests successfully fill in email and password fields
- Sign-in button is clicked successfully
- No authentication errors are reported
- The timeout occurs AFTER authentication, during redirect phase

### The Redirect Problem

```javascript
// From auth.po.ts - where tests fail
await this.page.waitForURL(
  (url) => url.href.includes("/home") || url.href.includes("/onboarding"),
  { timeout: 120000 }
);
```

The application appears to:

1. Successfully authenticate the user
2. NOT trigger the expected redirect to /onboarding or /home
3. Remain on the authentication page or redirect to an unexpected location

## Debugging Steps Taken

1. **Increased Timeouts**: Tests already use 120-second timeout (2 minutes) - still fails
2. **Multiple Test Runs**: Consistent failure pattern across all runs
3. **Infrastructure Verification**: All services healthy, not an infrastructure issue

## Potential Application Issues

### 1. Middleware Redirect Logic

The middleware may not be properly handling post-authentication redirects:

- Session might not be immediately available after auth
- Redirect logic might be checking incorrect conditions
- Race condition between auth completion and redirect trigger

### 2. Client-Side Navigation

The authentication success handler might:

- Not be calling the redirect function
- Have a broken redirect URL construction
- Be waiting for additional conditions that never complete

### 3. Session State Management

- Session might not be properly set after authentication
- Cookie/token might not be immediately available for redirect logic
- Supabase session might need explicit refresh

## Reproduction Steps

1. Start the test environment: `pnpm --filter web dev:test`
2. Run authentication tests: `pnpm --filter web-e2e test:e2e --grep "sign-in"`
3. Observe test hanging after successful authentication
4. Test times out waiting for redirect to /home or /onboarding

## Investigation Areas

### 1. Authentication Success Handler

Check the code that handles successful authentication response:

```typescript
// Look for code like this in auth components
const handleSignIn = async () => {
  const { error } = await supabase.auth.signIn({...});
  if (!error) {
    // THIS REDIRECT MIGHT BE FAILING
    router.push('/home'); // or '/onboarding'
  }
}
```

### 2. Middleware Redirect Rules

Review middleware.ts for post-auth redirect logic:

```typescript
// Check if middleware is properly detecting authenticated state
if (session && isAuthPage) {
  // Should redirect to /home or /onboarding
  return NextResponse.redirect(new URL('/home', request.url));
}
```

### 3. Onboarding Detection Logic

Check how the app determines if user needs onboarding:

```typescript
// This logic might be broken
const needsOnboarding = !user.profile?.onboarded;
const redirectPath = needsOnboarding ? '/onboarding' : '/home';
```

## Proposed Solutions

### Quick Fix (Test Workaround)

```javascript
// Add fallback navigation in tests
try {
  await page.waitForURL(/* expected URL */, { timeout: 10000 });
} catch {
  // Force navigation if redirect fails
  await page.goto('/home');
}
```

### Proper Fix (Application)

1. **Add Redirect Logging**: Add console.logs to trace redirect decision flow
2. **Fix Session Timing**: Ensure session is available immediately after auth
3. **Explicit Redirect**: Use router.push() explicitly after successful auth
4. **Session Refresh**: Add explicit session refresh before redirect check

## Expected Behavior

After successful authentication:

1. User should be redirected to `/onboarding` if new user
2. User should be redirected to `/home` if existing user
3. Redirect should happen within 5 seconds of authentication

## Actual Behavior

After successful authentication:

1. No redirect occurs
2. Page remains on authentication page or unknown location
3. Tests timeout after 120 seconds waiting for expected redirect

## Impact

- **Severity**: High - Core authentication flow is broken
- **User Impact**: Users cannot access the application after logging in
- **Test Impact**: 100% failure rate for authentication-dependent tests
- **CI/CD Impact**: Blocks all deployment pipelines

## Related Issues

- Different from #272 (infrastructure issues) - this is an application bug
- May relate to previous auth issues (#255, #226) but specific to redirect logic
- Not related to test infrastructure improvements (#259-#269)

## Labels

- bug
- high-priority
- authentication
- e2e
- navigation

## Next Steps

1. Debug authentication success handler in browser DevTools
2. Add logging to middleware redirect logic
3. Test authentication flow manually to confirm it's not test-specific
4. Review recent changes to auth or middleware code
5. Check Supabase auth callback configuration

---
**Auto-Generated Local Cache**

- GitHub Issue: #273
- GitHub URL: <https://github.com/MLorneSmith/2025slideheroes/issues/273>
- Cached: 2025-08-28T23:10:17.798Z
