# Issue: E2E Test Matrix: Merge Test Reports job failing with missing playwright-report directory

**ID**: ISSUE-166
**Created**: 2025-07-08T18:31:48Z
**Updated**: 2025-07-08T18:31:48Z
**Reporter**: MLorneSmith
**Status**: open
**Labels**: bug, needs-investigation, ci/cd, medium-priority, e2e-tests

## GitHub Issue Content

# Issue: E2E Test Matrix: Merge Test Reports job failing with missing playwright-report directory

**ID**: ISSUE-[github_issue_number]
**Created**: 2025-07-08T18:36:00Z
**Reporter**: Claude Debug Assistant
**Severity**: medium
**Status**: new
**Type**: error

## Summary

The E2E Test Matrix workflow is failing in the "Merge Test Reports" job because it cannot find the `apps/e2e/playwright-report/` directory to upload as an artifact. This appears to be happening after successfully downloading test results and attempting to merge Playwright reports.

## Environment

- **Application Version**: Latest dev branch (commit bc20618f)
- **Environment**: GitHub Actions CI/CD
- **Workflow**: E2E Test Matrix
- **Affected Job**: Merge Test Reports
- **Node Version**: 20
- **PNPM Version**: 9.12.0
- **Last Working**: Unknown

## Reproduction Steps

1. Create a pull request with changes
2. E2E Test Matrix workflow triggers
3. Chromium and Firefox tests run successfully
4. Merge Test Reports job starts
5. Job fails when trying to upload unified report

## Expected Behavior

The Merge Test Reports job should:

1. Download test results from browser-specific jobs
2. Merge the Playwright blob reports
3. Generate a unified HTML report in `apps/e2e/playwright-report/`
4. Upload the report as an artifact
5. Comment on the PR with test results

## Actual Behavior

The job fails at step 7 (Upload unified report) with:

- Error: "No files were found with the provided path: apps/e2e/playwright-report/"
- The merge process completes but the expected directory is not created

## Diagnostic Data

### Workflow Run Details

- Run ID: 16151222023
- Job ID: 45583068173
- PR: #165

### Error Output

```
! No files were found with the provided path: apps/e2e/playwright-report/. No artifacts will be uploaded.
X Process completed with exit code 1.
```

### Job Steps Status

- ✓ Set up job
- ✓ Run actions/checkout@v4
- ✓ Run pnpm/action-setup@v4
- ✓ Run actions/setup-node@v4
- ✓ Install dependencies
- ✓ Download all test results
- ❌ Merge Playwright reports (exits with code 1)
- ✓ Upload unified report (continues despite missing files)
- ✓ Comment on PR with test results

## Related Code

- **Affected Files**:
  - `.github/workflows/e2e-matrix.yml`
- **Workflow Section**: Merge Test Reports job (lines ~180-250)
- **Commands Executed**:

  ```bash
  cd apps/e2e
  pnpm playwright install --with-deps
  find ../../merged-reports -name "*.zip" -exec unzip -o {} -d ./blob-reports/ \;
  pnpm playwright merge-reports --reporter html ./blob-reports/ || echo "Report merge completed with warnings"
  ```

## Initial Analysis

The issue appears to be that:

1. The `playwright merge-reports` command is failing silently
2. The expected output directory `playwright-report` is not being created
3. The `|| echo` fallback is masking the actual error
4. The workflow continues to the upload step which then fails

Possible causes:

- The blob reports may not be in the expected format
- The merge command may be failing due to missing dependencies
- The output directory path may be incorrect

## Suggested Investigation Areas

1. Check if the blob reports are being downloaded correctly
2. Verify the `playwright merge-reports` command syntax and requirements
3. Ensure the working directory is correct when running merge
4. Consider adding more verbose logging to the merge step
5. Check if the Playwright version supports the merge-reports feature

## Additional Context

This is a non-blocking issue for the PR merge process but prevents proper E2E test result visualization. The individual browser tests (Chromium and Firefox) are passing successfully, so the core functionality is working.

---

_Generated by Claude Debug Assistant_
_Tools Used: GitHub CLI, Log Analysis_

---

**Auto-Generated Local Cache**

- GitHub Issue: #166
- GitHub URL: <https://github.com/MLorneSmith/2025slideheroes/issues/166>
- Cached: 2025-07-08T18:33:14.044Z
