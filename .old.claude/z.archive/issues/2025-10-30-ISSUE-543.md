# E2E Tests Failing: Authentication Redirecting to Verification Page

## Summary

All E2E authentication setup tests are failing because users are being redirected to `/auth/verify` (email verification page) instead of completing direct login. This blocks 175 downstream tests from running.

## Environment

- **Application Version**: 2.13.1
- **Environment**: Docker test environment (LOCAL)
- **Node Version**: 22.21.1 (upgraded from 20.19.5)
- **Next.js Version**: 16.0.0
- **Browser**: Chromium (Playwright)
- **Test Framework**: Playwright
- **First Noticed**: 2025-10-30 (immediately after fixing URLPattern issue with Node.js 22 upgrade)
- **Last Working**: Unknown (possibly related to recent Next.js 16.0.0 upgrade)

## Reproduction Steps

1. Start Docker test environment: `docker-compose -f docker-compose.test.yml up`
2. Run E2E tests: `cd apps/e2e && pnpm test`
3. Observe authentication setup tests failing with redirect to `/auth/verify`

## Expected Behavior

**Test users should login directly without email verification:**

- Test credentials (`test1@slideheroes.com`, `aiesec1992`) should authenticate successfully
- Users should be redirected to target page (e.g., `/home`)
- Authentication state should be stored in test storage
- Downstream tests should execute normally

## Actual Behavior

**Users are redirected to email verification page:**

- Authentication API responds with 200 status
- Application redirects to `/auth/verify` instead of target page
- Verification code input form is displayed
- Tests timeout waiting for successful authentication
- 175 tests are skipped due to failed auth setup

## Test Failures

### Failed Tests (3/4 auth setups)

1. ❌ **authenticate as test user** - `test1@slideheroes.com`
2. ❌ **authenticate as owner user** - `test1@slideheroes.com`  
3. ❌ **authenticate as super-admin user** - `michael@slideheroes.com`
4. ⚠️ **authenticate as test user for billing** (flaky)

### Impact

- **175 tests skipped** - All dependent tests cannot run
- **Test coverage**: 0% (only setup tests executed)

## Diagnostic Data

### Authentication Flow Analysis

```
Phase 1: Supabase auth/v1/token API
✅ Status: 200 (successful authentication)
✅ Timing: ~1400ms
✅ Credentials accepted by Supabase

Phase 2: Navigation after auth
❌ Expected: /home or target page
❌ Actual: /auth/verify
❌ Reason: Application logic redirecting to verification

Current URL: http://localhost:3001/auth/verify
Available Input: verificationCode (text field)
```

### Environment Configuration

```bash
# apps/web/.env.test.locked
NEXT_PUBLIC_AUTH_PASSWORD=true
NEXT_PUBLIC_AUTH_MAGIC_LINK=false
NEXT_PUBLIC_AUTH_OTP=false
```

**Analysis**: Password auth is enabled, OTP/Magic Link disabled, but verification page still appears.

### Application Logs

```
GET /auth/sign-in?next=/auth/verify 200 in 245ms
GET /auth/verify 200 in 45ms
```

**Pattern**: The `next=/auth/verify` parameter suggests the application is explicitly redirecting to verification after successful auth.

### Page State at Failure

```yaml
- heading "Sign in to your account"
- textbox "Email Address" [active]
- textbox "Password"
- button "Sign in with Email"
- button "google logo Sign in with Google"
```

**Analysis**: Still on sign-in page in some test iterations, suggesting navigation never completes.

### Test User Credentials

```typescript
// From apps/web/.env.test.locked
E2E_TEST_USER_EMAIL=test1@slideheroes.com
E2E_TEST_USER_PASSWORD=aiesec1992
E2E_OWNER_EMAIL=test1@slideheroes.com
E2E_ADMIN_EMAIL=michael@slideheroes.com

// All users configured with same password hash in seed data
TEST_PASSWORD_HASH=$2a$10$HnRa4VckSRWnYpgTXkrd4.x.IGVeYdqJ8V3nlwECk8cnDvIWBBjl6
```

## Related Code

### Affected Files

- `apps/e2e/tests/auth.setup.ts` - Authentication setup tests (failing)
- `apps/web/proxy.ts` - Middleware handling auth redirects (recently modified)
- `apps/web/app/auth/sign-in/page.tsx` - Sign-in page
- `apps/web/app/auth/verify/page.tsx` - Verification page
- `apps/web/supabase/seeds/01_main_seed.sql` - Test user seed data

### Recent Changes

```
e3e989537 fix(payload): resolve Next.js 16 build configuration issues
7bd5fa4d7 fix: restore Next.js 16.0.0 and React 19.2.0 versions from upstream
bf45aea51 chore: format package.json files and sync SlideHeroes-only packages
05a98071b chore: sync dependency versions from upstream
17bb45b31 chore: downgrade Next.js 16.0.0 to 15.5.4 and upgrade Payload CMS
ec62b607d chore: upgrade to Next.js 16.0.0
```

**Timeline**:

1. Next.js 16.0.0 upgrade (ec62b607d)
2. Brief downgrade to 15.5.4 (17bb45b31)
3. Restoration to Next.js 16.0.0 (7bd5fa4d7)
4. URLPattern fix with Node.js 22 upgrade (today)

### Suspected Root Causes

1. **Next.js 16 Auth Flow Change**: Middleware or auth flow changes in Next.js 16.0.0
2. **Proxy.ts Modifications**: Recent URLPattern fix may have affected redirect logic
3. **Email Verification Enforcement**: Application now requiring email verification even for seeded test users
4. **Session Cookie Issues**: Auth state not persisting correctly after Docker/Node upgrade

## Error Stack Traces

```
Error: page.waitForResponse: Timeout 20000ms exceeded while waiting for event "response"

Call Log:
- Timeout 45000ms exceeded while waiting on the predicate

  142 |     password: credentials.password,
  143 |   });
> 144 | }).toPass({
      |    ^
  145 |   intervals: testConfig.getRetryIntervals("auth"),
  146 |   timeout: testConfig.getTimeout("long"),
  147 | });
```

## Initial Analysis

### Hypothesis 1: Email Verification Required (Most Likely)

The application may now be enforcing email verification for all users, including seeded test users. The seed data creates users but may not be marking them as `email_confirmed=true`.

**Evidence**:

- Redirect to `/auth/verify` is explicit and consistent
- Supabase auth succeeds (200 response)
- Navigation to verification page happens every time

**Investigation Needed**:

- Check `auth.users.email_confirmed_at` in seed data
- Review auth middleware/proxy.ts for verification enforcement
- Check if Next.js 16 changed default email verification behavior

### Hypothesis 2: Session State Not Persisting

Auth completes but session cookies aren't being stored correctly in Playwright's storage state.

**Evidence**:

- Auth API returns 200
- But subsequent requests still see unauthenticated state

**Investigation Needed**:

- Review Playwright storage state handling
- Check cookie domain/path configuration
- Verify session cookie is set correctly

### Hypothesis 3: Proxy.ts Redirect Logic

The recent URLPattern fix in proxy.ts may have inadvertently changed redirect behavior.

**Evidence**:

- proxy.ts was modified today to fix URLPattern issue
- Logs show `proxy.ts` timing in all requests
- Auth redirects may be handled differently

**Investigation Needed**:

- Review proxy.ts redirect logic for authenticated users
- Check if URLPattern changes affected route matching
- Compare with previous middleware.ts behavior

## Suggested Investigation Areas

1. **Database Seed Data** (Priority 1)
   - Check `apps/web/supabase/seeds/01_main_seed.sql`
   - Verify `email_confirmed_at` is set for test users
   - Ensure test users have proper auth flags

2. **Auth Middleware** (Priority 2)
   - Review `apps/web/proxy.ts` redirect logic
   - Check for email verification enforcement
   - Compare with pre-URLPattern fix version

3. **Test Configuration** (Priority 3)
   - Review `apps/e2e/playwright.config.ts`
   - Check storage state persistence settings
   - Verify auth timeout configurations

4. **Next.js 16 Changes** (Priority 4)
   - Research Next.js 16 auth/session changes
   - Check if default behavior changed for email verification
   - Review migration notes for breaking changes

## Related Issues

### Search Strategy

Will search for:

- `repo:MLorneSmith/2025slideheroes is:issue auth verify`
- `repo:MLorneSmith/2025slideheroes is:issue email verification`
- `repo:MLorneSmith/2025slideheroes is:issue e2e authentication`
- `repo:MLorneSmith/2025slideheroes is:issue Next.js 16`

## Additional Context

### Unified E2E Environment

- Uses single Web Supabase instance (ports 54321/54322)
- Docker test environment with dedicated ports (3001)
- Test users seeded via `01_main_seed.sql`
- Authentication configured for password-only (no OTP/magic link)

### Recent Infrastructure Changes

- Upgraded Node.js from 20.19.5 to 22.21.1
- Added urlpattern-polyfill for Next.js 16 compatibility
- Modified proxy.ts to use global URLPattern

---
*Generated by Claude Debug Assistant*
*Test Environment: Docker (node:22-slim)*
*Playwright Version: 1.56.1*
*Test Execution: 2025-10-30T16:33:54Z*

---
**GitHub Sync Metadata**

- GitHub Issue: #543
- GitHub URL: <https://github.com/MLorneSmith/2025slideheroes/issues/543>
- Synced: 2025-10-30T16:37:00Z
- Status: open
