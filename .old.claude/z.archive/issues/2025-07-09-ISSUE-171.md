# Issue: Payload Admin Panel Database Query Error - Missing 'name' and 'role' columns

**ID**: ISSUE-171
**Created**: 2025-07-09T13:30:02Z
**Updated**: 2025-07-09T13:30:02Z
**Reporter**: MLorneSmith
**Status**: open
**Labels**: bug, high, needs-investigation, payload-cms, issue, database

## GitHub Issue Content

# Issue: Payload Admin Panel Database Query Error - Missing 'name' and 'role' columns

**ID**: ISSUE-[github_issue_number]
**Created**: 2025-07-09T20:32:00Z
**Reporter**: User
**Severity**: high
**Status**: new
**Type**: bug

## Summary

When accessing the Payload admin panel at localhost:3020 after running `pnpm run dev`, clicking the "go to admin panel" button results in a database query error. The query attempts to select columns 'name' and 'role' from the payload.users table, but these columns do not exist in the current schema.

## Environment

- **Application Version**: 2.11.0
- **Environment**: development
- **Browser**: Not specified
- **Node Version**: Not specified
- **Database**: PostgreSQL (Supabase)
- **Last Working**: Unknown
- **Current Branch**: dev
- **Last Commit**: d0db9363 fix: resolve merge conflict - keep staging build fix

## Reproduction Steps

1. Run `pnpm run dev` locally
2. Navigate to localhost:3020 (Payload app)
3. See "Welcome to your new project" screen
4. Click "go to admin panel" button
5. Error appears in console

## Expected Behavior

The admin panel should load successfully, displaying the Payload CMS interface.

## Actual Behavior

A database query error is thrown with the following stack trace showing a failed query attempting to select non-existent columns.

## Diagnostic Data

### Console Output

```
Error: Failed query: select "id", "name", "role", "updated_at", "created_at", "email", "reset_password_token", "reset_password_expiration", "salt", "hash", "login_attempts", "lock_until" from "payload"."users" "users" order by "users"."created_at" desc limit $1
params: 1
    at resolveErrorDev (webpack-internal:///(app-pages-browser)/../../node_modules/.pnpm/next@15.3.4_@opentelemetry+api@1.9.0_@playwright+test@1.53.2_babel-plugin-react-compiler@19.1_6vi3jc4dlwhfso2hnxh6thhc7q/node_modules/next/dist/compiled/react-server-dom-webpack/cjs/react-server-dom-webpack-client.browser.development.js:1865:46)
```

### Database Analysis

```sql
-- Current payload.users table columns:
id (uuid)
email (varchar)
reset_password_token (varchar)
reset_password_expiration (timestamptz)
salt (varchar)
hash (varchar)
login_attempts (numeric)
lock_until (timestamptz)
first_name (varchar)
last_name (varchar)
updated_at (timestamptz)
created_at (timestamptz)
downloads_id (uuid)
media_id (uuid)
parent_id (text)
path (text)
private_id (uuid)

-- Missing columns from query:
- name
- role
```

### Recent Migration Activity

```
064dd775 feat(db): add migrations to fix user deletion constraints
e26cc052 fix(security): implement function search path security fix v2
80cae5d4 fix(migration): add database migrations for auth permissions and function security
```

## Error Stack Traces

```
Error: Failed query: select "id", "name", "role", "updated_at", "created_at", "email", "reset_password_token", "reset_password_expiration", "salt", "hash", "login_attempts", "lock_until" from "payload"."users" "users" order by "users"."created_at" desc limit $1
params: 1
    at resolveErrorDev (...)
    at processFullStringRow (...)
    at processFullBinaryRow (...)
    at progress (...)
    at InnerLayoutRouter (...)
    at OuterLayoutRouter (...)
```

## Related Code

- **Affected Files**:
  - Payload configuration files
  - User model/schema definitions
  - Database migration files
- **Recent Changes**: Multiple recent migration commits suggest schema changes
- **Suspected Functions**: Payload CMS user query/authentication logic

## Initial Analysis

The error indicates a schema mismatch between what Payload CMS expects and the actual database schema. The query is attempting to select columns 'name' and 'role' that don't exist in the payload.users table. This suggests either:

1. Missing database migrations that should add these columns
2. Payload configuration mismatch with the database schema
3. Recent schema changes that weren't properly synchronized

## Suggested Investigation Areas

1. Check Payload CMS collection configuration for the Users collection
2. Review migration history to see if 'name' and 'role' columns were removed or never added
3. Verify if there are pending migrations that need to be run
4. Check if Payload configuration expects different field names than what's in the database

## Additional Context

- The table has first_name and last_name columns instead of a single 'name' column
- No 'role' column exists, which is typically required for user permission management
- Recent migration commits suggest ongoing schema changes that may not be fully synchronized

---

_Generated by Claude Debug Assistant_
_Tools Used: mcp**postgres**pg_execute_sql, Bash_

---

**Auto-Generated Local Cache**

- GitHub Issue: #171
- GitHub URL: <https://github.com/MLorneSmith/2025slideheroes/issues/171>
- Cached: 2025-07-09T13:32:53.577Z
