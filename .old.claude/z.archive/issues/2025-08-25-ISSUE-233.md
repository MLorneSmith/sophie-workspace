# Issue: Build Warning: Critical dependency warnings in webpack for OpenTelemetry and Edge Runtime compatibility

**ID**: ISSUE-233
**Created**: 2025-08-25T12:56:29Z
**Updated**: 2025-08-25T12:56:29Z
**Reporter**: MLorneSmith
**Status**: open
**Labels**: bug, dependencies, build, webpack

## GitHub Issue Content

## Issue Description

Multiple webpack warnings are occurring during the build process related to critical dependencies and Edge Runtime compatibility issues.

## Environment

- **Date**: 2025-08-25
- **Build Command**: `pnpm build`
- **Affected Apps**: web

## Warnings Observed

### 1. Critical Dependency Warnings

```
Critical dependency: the request of a dependency is an expression
```

**Affected modules:**

- `@opentelemetry/instrumentation` (via Sentry integration)
- `@baselime/node-opentelemetry`

### 2. Edge Runtime Compatibility Issues

Multiple Supabase modules are using Node.js APIs that are not supported in Edge Runtime:

```
A Node.js API is used (process.versions) which is not supported in the Edge Runtime
```

**Affected modules:**

- `@supabase/realtime-js` - Uses `process.versions` at lines 34, 35, 36
- `@supabase/supabase-js` - Uses `process.version` at line 24

## Impact

- These warnings don't prevent the build from completing but indicate potential runtime issues
- Edge Runtime compatibility issues may cause problems if these modules are used in Edge API routes
- Critical dependency warnings may impact tree-shaking and bundle optimization

## Suggested Resolution

1. **For OpenTelemetry warnings**:
   - Consider updating to latest versions of `@sentry/nextjs` and `@baselime/node-opentelemetry`
   - Check if there are ESM-compatible builds available
   - May need to exclude these from Edge Runtime bundles

2. **For Supabase Edge Runtime issues**:
   - Update to latest Supabase packages if available
   - Consider using conditional imports to avoid loading these modules in Edge Runtime
   - May need to create separate clients for Edge vs Node.js environments

## Related Files

- `packages/monitoring/sentry/src/`
- `packages/monitoring/baselime/src/`
- `packages/supabase/src/clients/middleware-client.ts`

## Build Output Reference

Full warning output available in build logs from 2025-08-25 12:54

---
**Auto-Generated Local Cache**

- GitHub Issue: #233
- GitHub URL: <https://github.com/MLorneSmith/2025slideheroes/issues/233>
- Cached: 2025-08-25T18:31:14.359Z
