# E2E Test Execution Inconsistency - Test Controller vs Direct Commands

**ID**: ISSUE-351
**Created**: 2025-01-19T20:06:00Z
**Reporter**: system/claude-debug
**Severity**: medium
**Status**: new
**Type**: bug

## Summary

E2E tests execute successfully with direct pnpm commands but fail systematically when run through the custom test-controller.cjs script. This creates inconsistent testing behavior and prevents reliable E2E test execution through the /test command interface.

## Environment

- **Application Version**: 2.13.1
- **Environment**: development
- **Node Version**: v18.18.0+
- **Playwright Version**: Latest (from apps/e2e package)
- **Test Framework**: Playwright with 132 tests across 12 workers
- **Last Working**: Direct pnpm commands work consistently

## Reproduction Steps

1. Run direct E2E tests: `cd apps/e2e && pnpm test`
   - ✅ **Result**: Tests execute with live progress ([1/132], [2/132], etc.)
   - ✅ **Behavior**: Real-time output, proper timeout handling

2. Run via root script: `pnpm test:e2e`
   - ✅ **Result**: Tests execute successfully via turbo

3. Run via test controller: `.claude/scripts/testing/infrastructure/test-controller.cjs`
   - ❌ **Result**: Tests fail with timeout errors, high retry counts (166 retries)
   - ❌ **Behavior**: Background execution, no live progress, systematic failures

## Expected Behavior

The test-controller.cjs should execute E2E tests with the same reliability and visibility as direct pnpm commands, providing:

- Real-time progress feedback
- Proper timeout handling
- Consistent test execution environment
- Visible error reporting

## Actual Behavior

Test-controller.cjs exhibits:

- Background execution preventing progress visibility
- Timeout errors ("page.goto: Timeout 15000ms exceeded")
- High retry counts (166 retry attempts detected)
- Systematic test failures requiring multiple retries
- No real-time progress feedback to users

## Diagnostic Data

### Direct Command Success Evidence

```bash
# Working command
cd apps/e2e && pnpm test --max-failures=5 --timeout=10000

# Output shows healthy execution:
Running 132 tests using 12 workers
[1/132] [chromium] › tests/accessibility/accessibility-hybrid-simple.spec.ts:73:7
[2/132] [chromium] › tests/accessibility/accessibility-hybrid.spec.ts:79:6
[3/132] [chromium] › tests/accessibility/accessibility-hybrid-simple.spec.ts:171:6
# Tests proceed with real-time progress...
```

### Test Controller Issues

```bash
# Problematic execution through test-controller.cjs
- File size: 538 lines (complex orchestration)
- Dependencies: 10+ custom modules
- Logging: Custom logging vs native Playwright output
- Execution: Background process without live feedback
```

### Error Patterns from Test Controller

```
❌ Error testing Homepage: page.goto: Timeout 15000ms exceeded.
🔄 Tests with retries: 166 retry attempts
⚠️  Some tests still running after extended wait
🔍 Last few lines from E2E tests: [timeout errors]
```

### Package.json Script Comparison

```json
// Root package.json - Works via turbo
"test:e2e": "turbo test --filter=web-e2e"

// E2E package.json - Works directly  
"test": "playwright test"

// Test command - Uses problematic controller
TEST_CONTROLLER=".claude/scripts/testing/infrastructure/test-controller.cjs"
```

## Related Code

- **Affected Files**:
  - `.claude/scripts/testing/infrastructure/test-controller.cjs` (538 lines)
  - `.claude/commands/core/test.md` (test command definition)
  - `apps/e2e/package.json` (working test scripts)
  - `package.json` (root test:e2e script that works)

- **Recent Changes**:
  - Recent commits show testing infrastructure reorganization
  - Test controller and related infrastructure files modified

- **Suspected Functions**:
  - Test controller orchestration logic
  - Environment setup differences between direct and scripted execution
  - Progress reporting and output handling

## Related Issues & Context

### Infrastructure Complexity

The test-controller.cjs is a 538-line complex orchestration system with:

- 10+ custom module dependencies  
- Custom logging instead of native Playwright output
- Background execution preventing user interaction
- Complex phase coordination and infrastructure management

### Working Alternatives

Direct commands that work reliably:

- `pnpm test:e2e` (via turbo, filters to web-e2e package)
- `cd apps/e2e && pnpm test` (direct Playwright execution)
- `cd apps/e2e && pnpm test:smoke` (subset testing)

## Initial Analysis

**Root Cause**: Environment/execution context differences between:

1. **Direct execution**: Native Playwright with live output
2. **Controller execution**: Custom orchestration with background processing

**Key Issues**:

1. **Visibility**: Test controller runs in background without live progress
2. **Environment**: Different execution context causing timeout issues  
3. **Complexity**: 538-line orchestration vs simple direct commands
4. **Output**: Custom logging vs native Playwright real-time feedback

## Suggested Investigation Areas

1. **Execution Environment Differences**
   - Compare environment variables between direct and controller execution
   - Investigate timeout handling differences
   - Analyze process spawning and stdio handling

2. **Progress Reporting System**
   - Implement real-time progress forwarding from controller
   - Add live output streaming from Playwright processes
   - Improve error visibility and reporting

3. **Simplification Assessment**
   - Evaluate if test-controller.cjs complexity is necessary
   - Consider using direct pnpm/turbo commands instead
   - Assess benefits vs complexity of custom orchestration

## Additional Context

The E2E test suite (132 tests across 12 workers) executes reliably when run directly but fails systematically through the custom test controller. This suggests the issue is in the orchestration layer rather than the tests themselves.

**Direct execution shows**: Live progress, proper timeouts, successful completion
**Controller execution shows**: Background processing, timeout failures, retry loops

This inconsistency undermines the reliability of the /test command and creates confusion about E2E test health.

---
*Generated by Claude Debug Assistant*
*Tools Used: Bash, filesystem analysis, process monitoring*
*Evidence: Direct execution success vs controller failures*
