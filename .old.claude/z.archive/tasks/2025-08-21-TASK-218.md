# Task: Update CI/CD Pipeline to Use New Hybrid Accessibility Testing Solution

**ID**: TASK-218
**Created**: 2025-08-21T18:31:38Z
**Assignee**: Unassigned
**Priority**: medium
**Status**: OPEN
**Type**: enhancement
**GitHub URL**: <https://github.com/MLorneSmith/2025slideheroes/issues/218>

## Summary

Following the implementation of the hybrid accessibility testing solution in #216, we need to update our CI/CD pipeline to use the new tests instead of the deprecated axe-core implementation.

## Current State

- ✅ New hybrid accessibility testing solution implemented (#216)
- ✅ Package.json updated with new `a11y:test` scripts
- ⚠️ CI/CD still references old axe-core tests
- ⚠️ GitHub Actions workflows need updating

## Required Changes

### 1. Update PR Validation Workflow (`pr-validation.yml`)

The accessibility test job currently runs `pnpm --filter e2e a11y:test` which now correctly points to our new hybrid tests. However, we should verify:

- [ ] Ensure Chrome launcher works in CI environment
- [ ] Update reporter format for CI (use `a11y:test:ci` for junit output)
- [ ] Verify Lighthouse can be skipped gracefully in headless CI

### 2. Update E2E Matrix Workflow (`e2e-matrix.yml`)

- [ ] Add accessibility tests to the matrix run
- [ ] Consider running accessibility tests only on chromium to save CI time
- [ ] Update test glob patterns to include new test files

### 3. Package Updates Already Completed

✅ **apps/e2e/package.json** - Updated with:

```json
"a11y:test": "playwright test accessibility.spec.ts accessibility-hybrid-simple.spec.ts --project=chromium --reporter=html,list",
"a11y:test:ci": "playwright test accessibility.spec.ts accessibility-hybrid-simple.spec.ts --project=chromium --reporter=junit,html",
"a11y:test:quick": "playwright test accessibility-hybrid-simple.spec.ts --project=chromium --reporter=list"
```

### 4. Dependencies Already Updated

✅ Removed: `@axe-core/playwright`
✅ Added: `lighthouse`, `chrome-launcher`

## Implementation Steps

1. **Update PR Validation Workflow**

   ```yaml
   - name: Run accessibility tests
     run: pnpm --filter e2e a11y:test:ci  # Use CI-specific script
     continue-on-error: true
   ```

2. **Add Environment Variables for Lighthouse (if needed)**

   ```yaml
   env:
     CHROME_FLAGS: '--no-sandbox --disable-dev-shm-usage --disable-gpu'
   ```

3. **Update Test Reporting**
   - Ensure JUnit XML output for CI reporting
   - Keep HTML reports for artifact upload

## Testing Checklist

- [ ] Run `pnpm --filter e2e a11y:test:ci` locally
- [ ] Verify tests pass in CI environment
- [ ] Check that test reports are generated correctly
- [ ] Confirm artifacts are uploaded properly
- [ ] Validate that failing accessibility tests don't block PR (continue-on-error: true)

## Benefits

- 🎯 No more false positives from axe-core color computation bugs
- ⚡ Faster test execution (~30 seconds vs previous times)
- 📊 Better separation of critical vs minor issues
- 🔧 More reliable CI/CD pipeline

## Related Issues

- #216 - Implement Hybrid Accessibility Testing Solution
- #213 - E2E Accessibility Tests Failing (original issue)

## Notes

The new hybrid solution includes:

- Custom WCAG validators with accurate contrast calculations
- Optional Lighthouse integration (can be skipped in CI)
- Graceful degradation when tools aren't available
- Clear reporting of actual vs false-positive issues

The tests are already working locally and just need CI environment verification
---

**Sync Metadata**

- Synced: 2025-08-21T18:38:28.470Z
- Source: GitHub Issue #218
- Cache Duration: 1 hour
