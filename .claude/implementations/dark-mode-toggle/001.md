# Task 001: Theme Infrastructure Setup

**Feature**: dark-mode-toggle
**Type**: infrastructure
**Agent**: react-expert
**Dependencies**: None
**Parallel**: Can run in parallel with 002, 003
**Estimated Time**: 2 hours

## Summary

Create the core theme management infrastructure including React Context, Provider component, and useTheme hook with localStorage persistence.

## Acceptance Criteria

- [ ] ThemeContext created with proper TypeScript types
- [ ] ThemeProvider component wraps the application
- [ ] useTheme hook provides theme state and toggle function
- [ ] localStorage persistence implemented
- [ ] Default theme handling implemented
- [ ] SSR-safe implementation

## Implementation Steps

### 1. Create Theme Context

Create `apps/web/components/theme/theme-context.tsx`:

```typescript
import { createContext } from 'react';

export type Theme = 'light' | 'dark';

export interface ThemeContextType {
  theme: Theme;
  toggleTheme: () => void;
}

export const ThemeContext = createContext<ThemeContextType | undefined>(undefined);
```

### 2. Create Theme Provider

Create `apps/web/components/theme/theme-provider.tsx`:

```typescript
'use client';

import { useState, useEffect, ReactNode } from 'react';
import { ThemeContext, Theme } from './theme-context';

const THEME_KEY = 'theme-preference';

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [theme, setTheme] = useState<Theme>('light');
  const [mounted, setMounted] = useState(false);

  useEffect(() => {
    setMounted(true);
    const stored = localStorage.getItem(THEME_KEY) as Theme;
    if (stored) {
      setTheme(stored);
      document.documentElement.classList.toggle('dark', stored === 'dark');
    }
  }, []);

  const toggleTheme = () => {
    const newTheme = theme === 'light' ? 'dark' : 'light';
    setTheme(newTheme);
    localStorage.setItem(THEME_KEY, newTheme);
    document.documentElement.classList.toggle('dark', newTheme === 'dark');
  };

  if (!mounted) return <>{children}</>;

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}
```

### 3. Create useTheme Hook

Create `apps/web/components/theme/use-theme.ts`:

```typescript
import { useContext } from 'react';
import { ThemeContext } from './theme-context';

export function useTheme() {
  const context = useContext(ThemeContext);
  if (!context) {
    throw new Error('useTheme must be used within ThemeProvider');
  }
  return context;
}
```

### 4. Export from Index

Create `apps/web/components/theme/index.ts`:

```typescript
export { ThemeProvider } from './theme-provider';
export { useTheme } from './use-theme';
export type { Theme } from './theme-context';
```

## Files to Create

- `apps/web/components/theme/theme-context.tsx`
- `apps/web/components/theme/theme-provider.tsx`
- `apps/web/components/theme/use-theme.ts`
- `apps/web/components/theme/index.ts`

## Files to Modify

- `apps/web/app/layout.tsx` - Wrap with ThemeProvider

## Testing

```bash
# Unit tests
npm test -- theme-provider
npm test -- use-theme

# Manual testing
# 1. Toggle theme and verify localStorage
# 2. Refresh page and verify persistence
# 3. Check SSR behavior
```

## Definition of Done

- [ ] All files created with proper TypeScript
- [ ] No TypeScript errors
- [ ] Theme toggles properly
- [ ] Preference persists across reloads
- [ ] SSR-safe (no hydration errors)
- [ ] Unit tests pass

---
*Task ID: 001*
*Can be executed in parallel with tasks 002 and 003*