# Issue: Payload CMS Lexical Editor Initialization Failure

**ID**: ISSUE-8
**Created**: 2025-06-06T21:30:00Z
**Reporter**: user
**Severity**: medium
**Status**: new
**Type**: bug

## Summary
The Payload CMS has an underlying lexical editor configuration error that causes `"Cannot read properties of undefined (reading 'lexical')"` during initialization. While error handling has been added to prevent crashes, the lexical editor functionality is likely non-functional, affecting the CMS editing capabilities.

## Environment
- **Application Version**: 2.9.0
- **Environment**: development
- **Browser**: N/A (server-side error)
- **Node Version**: v22.16.0
- **Database**: PostgreSQL (local)
- **Last Working**: Unknown, likely broken after merge conflict resolution (commit a8d76113)

## Reproduction Steps
1. Start the Payload development server: `pnpm --filter payload dev`
2. Visit http://localhost:3020
3. Observe fallback UI with error message
4. Check browser console or server-side rendering for detailed error
5. Try to access `/admin` panel (lexical editor likely affected)

## Expected Behavior
- Payload CMS should initialize with working lexical editor configuration
- Frontend page should show normal welcome screen with user authentication
- Admin panel should have functional rich text editing capabilities

## Actual Behavior
- Payload CMS shows fallback error UI: "There was an error initializing the CMS"
- Console error: `"Cannot read properties of undefined (reading 'lexical')"`
- Error occurs during `getPayload({ config })` initialization at server-side rendering
- Page returns HTTP 200 due to error handling but functionality is compromised

## Diagnostic Data

### Console/SSR Error Output
```
TypeError: Cannot read properties of undefined (reading 'lexical')
Stack: [["HomePage","webpack-internal:///(rsc)/./src/app/(frontend)/page.tsx",34,40]]
Environment: Server
```

### Network Analysis
- HTTP 200 status (due to error handling)
- Error visible in Next.js server-side rendering output
- No network connectivity issues

### Database Analysis
- PostgreSQL running normally on localhost:54322
- No database connection errors
- Database not related to this configuration issue

### Performance Metrics
- Server startup: Normal (~2 seconds)
- Response time: Fast (error occurs during SSR initialization)
- No memory leaks or resource issues detected

### Package Versions
```
@payloadcms/richtext-lexical: 3.41.0
@lexical/list: 0.28.0  
@lexical/rich-text: 0.28.0
lexical: 0.28.0
payload: 3.41.0
```

### Configuration Analysis
- Lexical editor configured in `apps/payload/src/payload.config.ts` using `lexicalEditor({})`
- Import: `import { lexicalEditor } from "@payloadcms/richtext-lexical"`
- Editor setting: `editor: lexicalEditor({}),`

## Error Stack Traces

### Payload CMS Lexical Editor Error
```
TypeError: Cannot read properties of undefined (reading 'lexical')
    at HomePage (webpack-internal:///(rsc)/./src/app/(frontend)/page.tsx:34:40)
    [Full Next.js SSR stack trace available in HTML response]
```

## Related Code
- **Affected Files**: 
  - `apps/payload/src/payload.config.ts` (lexical editor configuration)
  - `apps/payload/src/app/(frontend)/page.tsx` (error occurs during getPayload initialization)
  - `@payloadcms/richtext-lexical` package integration

- **Recent Changes**: 
  - `f57e7f82` - Added error handling (masks the issue)
  - `660aa376` - Previous attempt to fix "Lexical editor undefined error"
  - `7c590e2a` - Attempted "Payload admin panel server function errors" fix
  - `a8d76113` - Merge conflicts resolution (likely introduced the issue)

- **Suspected Functions**: 
  - `lexicalEditor({})` initialization in payload config
  - `getPayload({ config })` call in frontend page
  - Lexical editor module resolution or dependency loading

## Initial Analysis
1. **Version Mismatch**: The lexical packages (0.28.0) might not be fully compatible with payload lexical plugin (3.41.0), creating undefined property access.

2. **Configuration Issue**: The `lexicalEditor({})` empty configuration might be missing required properties that are accessed during initialization.

3. **Module Loading**: During server-side rendering, lexical editor modules might not be properly loaded or initialized, causing undefined object access.

4. **Dependency Resolution**: Import path resolution for lexical modules might be failing in the server environment.

## Suggested Investigation Areas
1. **Package Compatibility**: Verify lexical package versions are compatible with Payload 3.41.0
2. **Lexical Editor Configuration**: Check if lexicalEditor({}) needs additional configuration parameters
3. **SSR Compatibility**: Investigate if lexical editor has server-side rendering compatibility issues
4. **Module Resolution**: Verify import paths and module resolution for @payloadcms/richtext-lexical
5. **Previous Fix Review**: Analyze why commit 660aa376 didn't resolve the lexical editor issue
6. **Admin Panel Testing**: Test if the /admin panel lexical editor is also affected

## Resolution Progress

**Status**: In Progress - Root cause identified, partial fixes applied

### Changes Made:
1. **Removed BlocksFeature imports**: Removed unused `BlocksFeature` imports from all collection files:
   - `/src/collections/Surveys.ts`
   - `/src/collections/Courses.ts` 
   - `/src/collections/Private.ts`
   - `/src/collections/Documentation.ts`
   - `/src/collections/Posts.ts`
   - `/src/collections/CourseLessons.ts`

2. **Enhanced i18n configuration**: Added debug flag to i18n configuration in payload.config.ts

3. **Regenerated import map**: Successfully regenerated Payload import map with no BlocksFeature imports

### Root Cause Analysis:
The error `"Cannot read properties of undefined (reading 'lexical')"` occurs in the @payloadcms/translations package during translation initialization. The issue is in `deepMergeSimple.ts:17` when trying to merge lexical editor translation objects.

**Key Finding**: The problem is not with the lexical editor configuration itself, but with incomplete translation data for lexical features during the translation merge process.

### Current Status:
- ✅ Removed problematic BlocksFeature configurations
- ✅ Import map regeneration successful (no new imports found)
- ❌ Translation error still persists during Payload initialization
- ❌ Database migrations cannot run due to translation error

### Next Steps Required:
1. Investigate @payloadcms/translations package configuration
2. Ensure lexical editor translation data is properly initialized
3. Create and run database migrations once initialization issue is resolved
4. Test full Payload CMS functionality including admin panel

## Additional Context
- Issue is currently masked by error handling added in ISSUE-5 resolution
- Page returns HTTP 200 and shows user-friendly fallback UI
- Actual CMS editing functionality (rich text editing) likely broken
- May affect content creation and editing workflows
- Development environment functional for non-CMS features

## Links
- Related: ISSUE-5 (error handling implementation)
- Previous fix attempt: commit 660aa376

---
*Generated by Claude Debug Assistant*
*Tools Used: Bash, curl, git log, pnpm ls*
*Playwright Session: N/A (server-side error)*
*GitHub Issue: https://github.com/MLorneSmith/2025slideheroes/issues/8*
*Issue ID: ISSUE-8*
*Last Updated: 2025-06-06T19:47:00Z*