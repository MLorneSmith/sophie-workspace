# Issue: ReferenceError: getLogger is not defined in DatabaseAdapterManager constructor

**ID**: ISSUE-63
**Created**: 2025-06-18T15:42:01Z
**Updated**: 2025-06-18T15:42:01Z
**Reporter**: MLorneSmith
**Status**: open
**Labels**: high, error, needs-investigation

## GitHub Issue Content

# Issue: ReferenceError: getLogger is not defined in DatabaseAdapterManager constructor

**ID**: ISSUE-[github_issue_number]
**Created**: 2025-06-18T00:00:00.000Z
**Reporter**: system
**Severity**: high
**Status**: new
**Type**: error

## Summary

When running `pnpm run dev` and navigating to the payload server on port 3020, the application crashes with `ReferenceError: getLogger is not defined` in the DatabaseAdapterManager constructor at line 60.

## Environment

- **Application Version**: Latest dev branch
- **Environment**: development
- **Node Version**: [Current Node version]
- **Database**: PostgreSQL
- **Last Working**: Unknown
- **Current Branch**: dev
- **Last Commit**: dea43f7f feat: comprehensive build system and development improvements

## Reproduction Steps

1. Run `pnpm run dev` from project root
2. Navigate to `http://localhost:3020` (payload server)
3. Error occurs during server initialization

## Expected Behavior

The payload server should start successfully and serve the admin interface without errors.

## Actual Behavior

Server crashes with the following stack trace:

```
ReferenceError: getLogger is not defined
    at new DatabaseAdapterManager (webpack-internal:///(rsc)/./src/lib/database-adapter-singleton.ts:35:9)
    at getDatabaseAdapterManager (webpack-internal:///(rsc)/./src/lib/database-adapter-singleton.ts:287:51)
    at getDatabaseAdapter (webpack-internal:///(rsc)/./src/lib/database-adapter-singleton.ts:300:21)
    at eval (webpack-internal:///(rsc)/./src/payload.config.ts:264:93)
```

## Diagnostic Data

### Root Cause Analysis

The issue is in `apps/payload/src/lib/database-adapter-singleton.ts` at line 60:

```typescript
// Line 6-7: getLogger is imported from createServiceLogger
const { getLogger: _getLogger, getContextLogger: _getContextLogger } =
  createServiceLogger('DB-ADAPTER');

// Line 60: getLogger is used but not defined in scope
this.logger = getLogger();
```

The `getLogger` function referenced on line 60 is **not defined in the local scope**. The import on lines 6-7 creates `_getLogger`, but line 60 tries to use `getLogger` (without underscore).

### Code Analysis

- **File**: `apps/payload/src/lib/database-adapter-singleton.ts`
- **Line**: 60 in DatabaseAdapterManager constructor
- **Issue**: Variable name mismatch - `getLogger` vs `_getLogger`
- **Import**: `createServiceLogger("DB-ADAPTER")` returns `{ getLogger: _getLogger, ... }`
- **Usage**: Code attempts to call `getLogger()` but should call `_getLogger()`

### Recent Changes

Git log shows recent commits affecting this file:

- ca40dd08 feat: comprehensive codebase updates and fixes
- c9e96d67 feat: comprehensive codebase updates including build system, logging, and templates

## Suggested Investigation Areas

1. **Immediate Fix**: Change line 60 from `getLogger()` to `_getLogger()`
2. **Code Review**: Check for similar variable name mismatches in other files
3. **Testing**: Verify the logger initialization works correctly after fix
4. **Consistency**: Ensure consistent naming patterns across logger usage

## Additional Context

- The `createServiceLogger` function from `@kit/shared/logger` is working correctly
- The import statement properly destructures the returned functions with renamed variables
- This appears to be a simple variable naming inconsistency introduced during recent refactoring
- Other files in the codebase may have similar issues if the same refactoring pattern was applied

---

_Generated by Claude Debug Assistant_
_Tools Used: File reading, git analysis, code inspection_

---

**Auto-Generated Local Cache**

- GitHub Issue: #63
- GitHub URL: https://github.com/MLorneSmith/2025slideheroes/issues/63
- Cached: 2025-06-18T15:44:28.505Z
- Status: auto-sync cache (read-only)
- Source: GitHub (authoritative)
