# Issue: [CI/CD] Deploy to Dev workflow failing due to linting violation in test-controller.cjs

**ID**: ISSUE-251
**Created**: 2025-08-26T17:36:25Z
**Updated**: 2025-08-26T17:36:25Z
**Reporter**: MLorneSmith
**Status**: open
**Labels**: bug, code-quality, ci/cd, pipeline-failure

## GitHub Issue Content

## Summary

The "Deploy to Dev" workflow is consistently failing during the "Pre-deployment Validation" step due to a linting violation in `.claude/scripts/test-controller.cjs`. The failure occurs in the parallel validation checks where both TypeScript checking and linting run simultaneously.

## Failure Details

- **Workflow**: Deploy to Dev (`dev-deploy.yml`)
- **Failed Job**: Pre-deployment Validation 
- **Failed Step**: "Run validation checks in parallel"
- **Run ID**: 17245586122
- **Failure Time**: 2025-08-26T17:22:36Z
- **Duration**: ~1m6s

## Root Cause Analysis

**Issue Type**: Code Quality - Linting Violation
**Tool**: Biome linter
**Error**: `lint/correctness/noUnusedVariables`

The linting process failed due to an unused variable `e` in a catch block:

```javascript
// File: .claude/scripts/test-controller.cjs:374:13
} catch (e) {  // ❌ Variable 'e' is unused
    // Server not ready yet
}
```

**Validation Results from Run 17245586122:**
- ✅ TypeScript check: **PASSED** (36 tasks successful, all cached)
- ❌ Lint check: **FAILED** (1 error found)

## Impact Assessment

**Severity**: Medium
**Affected Operations**: 
- All deployments to dev environment blocked
- Developers cannot merge changes to dev branch
- CI/CD pipeline success rate impacted

**Deployment Status**: 
- Deploy Payload CMS to Dev: **Skipped**
- Deploy Web App to Dev: **Skipped**  
- Notify Monitoring: **Skipped**

## Technical Context

The failure occurs in the parallel validation step which runs:
```bash
pnpm typecheck & TYPECHECK_PID=$!
pnpm lint & LINT_PID=$!
# Wait for both and fail if either fails
```

This design ensures that both type safety and code quality standards are enforced before deployment.

## Steps to Reproduce

1. Make any commit to the `dev` branch
2. Push the commit to trigger the "Deploy to Dev" workflow
3. Observe the failure in the "Pre-deployment Validation" job
4. Check the logs for the linting error in `test-controller.cjs:374:13`

## Recommended Fix

**Option 1: Use Underscore Prefix (Recommended)**
```javascript
} catch (_e) {  // ✅ Underscore indicates intentionally unused
    // Server not ready yet
}
```

**Option 2: Use the Error Variable**
```javascript
} catch (e) {
    // Server not ready yet
    console.debug('Server connection attempt failed:', e.message);
}
```

**Implementation**: The fix is straightforward and can be applied immediately. Biome provides an automatic fix suggestion to prefix with underscore.

## Prevention Measures

1. **Pre-commit Hooks**: Ensure local linting catches these issues before commit
2. **IDE Integration**: Configure development environments to show linting errors
3. **Faster Feedback**: Consider separating quick lint checks from comprehensive validation

## Pipeline Architecture Notes

This failure demonstrates the effectiveness of the parallel validation design:
- TypeScript compilation succeeded (all 36 packages cached, 1.285s)
- Linting caught the code quality issue  
- Deployment was correctly prevented
- No resources wasted on failed deployments

## Related Context

- Pipeline Design: Based on updated CI/CD architecture with parallel validation
- Caching: Turbo remote cache is working effectively (all TypeScript checks cached)
- Runner: Using runs-on with m7a.large spot instance (efficient resource usage)

## Action Items

- [ ] Fix the unused variable in `test-controller.cjs:374:13`
- [ ] Verify fix with local `pnpm lint` command
- [ ] Test deployment after fix
- [ ] Consider adding linting to pre-commit hooks if not already present

---

**Generated by CI/CD Pipeline Investigation Orchestrator**  
**Investigation completed**: 2025-08-26 at 17:35 UTC  
**Evidence**: GitHub Actions Run #17245586122

---
**Auto-Generated Local Cache**
- GitHub Issue: #251
- GitHub URL: https://github.com/MLorneSmith/2025slideheroes/issues/251
- Cached: 2025-08-26T17:39:17.417Z
