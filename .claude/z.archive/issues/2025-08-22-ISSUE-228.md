# Issue: Fix Dependabot workflow failures and missing workflow issues

**ID**: ISSUE-228
**Created**: 2025-08-22T17:06:42Z
**Updated**: 2025-08-22T17:06:42Z
**Reporter**: MLorneSmith
**Status**: open
**Labels**: bug, dependencies, automated

## GitHub Issue Content

## Problem
Dependabot workflows are failing due to:
1. Outdated `pnpm-lock.yaml` file causing PR validation failures
2. References to a non-existent "Dependabot Updates" workflow
3. Lockfile mismatches preventing Dependabot PRs from passing CI

## Current Failures
- **PR #222**: `chore(deps): bump esbuild and wrangler` - failing due to lockfile mismatch
- **Error**: `ERR_PNPM_OUTDATED_LOCKFILE Cannot install with "frozen-lockfile"`
- Multiple phantom "Dependabot Updates" workflow failures appearing in GitHub Actions

## Recommended Fixes

### 1. Immediate: Fix Current PR Lockfile
```bash
# Checkout the Dependabot PR branch
git fetch origin
git checkout dependabot/npm_and_yarn/packages/playwright-mcp/multi-f0869ee958

# Update lockfile
pnpm install --no-frozen-lockfile

# Commit and push
git add pnpm-lock.yaml
git commit -m "fix: update lockfile for Dependabot compatibility"
git push origin dependabot/npm_and_yarn/packages/playwright-mcp/multi-f0869ee958
```

### 2. Add Auto-Fix to Dependabot Workflow
Update `.github/workflows/dependabot-auto-merge.yml` to automatically fix lockfiles:

```yaml
# Add after line 11 (before metadata step)
- uses: actions/checkout@v4
  with:
    token: ${{ secrets.GITHUB_TOKEN }}
    ref: ${{ github.event.pull_request.head.ref }}

- uses: pnpm/action-setup@v4
- uses: actions/setup-node@v4
  with:
    node-version: '20'
    cache: 'pnpm'

- name: Fix lockfile if needed
  run: |
    pnpm install --no-frozen-lockfile
    if git diff --quiet pnpm-lock.yaml; then
      echo "Lockfile is up to date"
    else
      git config user.name "dependabot[bot]"
      git config user.email "49699333+dependabot[bot]@users.noreply.github.com"
      git add pnpm-lock.yaml
      git commit -m "fix: update lockfile for dependency updates"
      git push
    fi
```

### 3. Investigate Phantom Workflow
Check for and remove any references to the non-existent "Dependabot Updates" workflow:
- Check GitHub Settings → Actions → Workflow permissions
- Review scheduled workflows for orphaned references
- Contact GitHub Support if phantom workflows persist

### 4. Daily Sync Process Documentation
Add to `README.md` or team docs:

```markdown
## Staying in Sync with Dependabot

Dependabot automatically updates dependencies on the `dev` branch weekly (Mondays 10:00 UTC).

### Daily Sync Routine
```bash
git checkout dev
git pull origin dev
pnpm install  # Critical - updates your local dependencies
```

### Before Starting Work
1. Check GitHub for merged Dependabot PRs
2. Pull latest `dev` changes
3. Run `pnpm install` to sync dependencies
```

## Benefits
- Eliminates lockfile-related CI failures
- Reduces manual intervention for Dependabot PRs
- Improves team workflow with clear sync processes
- Maintains security update automation

## Priority
**High** - Currently blocking dependency updates and causing CI failures

## Labels
- `bug`
- `dependencies`
- `ci/cd`
- `automated`

## Assignee
@MLorneSmith

---
**Auto-Generated Local Cache**
- GitHub Issue: #228
- GitHub URL: https://github.com/MLorneSmith/2025slideheroes/issues/228
- Cached: 2025-08-22T18:32:35.261Z
