---
name: Create CLI interface with Commander
status: completed
created: 2025-09-30T18:02:52Z
updated: 2025-09-30T19:14:00Z
completed: 2025-09-30T19:14:00Z
github: https://github.com/MLorneSmith/2025slideheroes/issues/461
depends_on: [459, 460]
parallel: false
conflicts_with: []
type: task
agent: nodejs-expert
effort: S
hours: 2
priority: high
tags: [foundation, cli, commander, interface]
---

# Task: Create CLI interface with Commander

## Description
Implement the command-line interface using Commander, providing a user-friendly interface for seeding operations with support for dry-run, verbose, and collection filtering modes.

## Acceptance Criteria
- [x] CLI entry point created at `index.ts`
- [x] Commander.js integration complete
- [x] All flags implemented: `--dry-run`, `--verbose`, `-c/--collections`
- [x] Help text clear and comprehensive
- [x] Environment check prevents production execution
- [x] Exit codes properly set for success/failure scenarios
- [x] Unit tests for argument parsing passing

## Technical Details
### Implementation Approach
1. Create `index.ts` as CLI entry point:
   - Use Commander for argument parsing
   - Implement main command: `seed:run`
   - Add flags: `--dry-run`, `--verbose`, `-c <collections>`
   - Add help text with examples
2. Add environment safety checks:
   - Block execution in production environment
   - Validate required environment variables
   - Check Supabase connection before proceeding
3. Wire up to orchestrator (placeholder for now)

### Files to Create
- `apps/payload/src/seed/seed-engine/index.ts` - CLI entry point
- `apps/payload/src/seed/seed-engine/index.test.ts` - CLI argument parsing tests

### API Changes
- None (new infrastructure)

### Package.json Scripts
Add to `apps/payload/package.json`:
```json
{
  "scripts": {
    "seed:run": "tsx src/seed/seed-engine/index.ts",
    "seed:dry": "tsx src/seed/seed-engine/index.ts --dry-run",
    "seed:validate": "tsx src/seed/seed-engine/index.ts --dry-run --verbose"
  }
}
```

## Dependencies
### Task Dependencies
- [x] Task 459 - Types and config needed
- [x] Task 460 - Logger and initializer needed

### External Dependencies
- [x] Commander 14.0.0 available (confirmed in package.json)
- [x] tsx 4.20.3 available (confirmed in package.json)

## Testing Requirements
### Unit Tests
- `index.test.ts`:
  - Test CLI argument parsing
  - Test flag combinations
  - Test help text generation
  - Test environment validation
  - Test production environment block

### Integration Tests
- Test full CLI execution with `--dry-run`
- Verify environment checks work correctly

### E2E Tests
- None at this stage

### Manual Testing
1. Run `pnpm seed:run --help`
2. Test `--dry-run` flag
3. Test `--verbose` flag
4. Test `-c courses,lessons` flag
5. Verify production block works

## Risk Assessment
### Potential Issues
- Commander API may have breaking changes
- Flag parsing may be ambiguous
- Production check may be bypassed

### Mitigation Strategies
- Lock Commander version
- Provide clear help text and examples
- Multiple checks for production environment
- Clear error messages for all scenarios

## Definition of Done
- [x] CLI interface working with all flags
- [x] Help text comprehensive and clear
- [x] Production environment blocked
- [x] Package.json scripts added
- [x] Unit tests passing (33/33 tests pass)
- [x] Code passes `pnpm typecheck` (no errors specific to our code)
- [x] No linting errors
- [x] JSDoc comments complete

## Implementation Summary

### Files Created
1. `/apps/payload/src/seed/seed-engine/index.ts` (271 lines)
   - CLI entry point with Commander integration
   - Argument parsing for all required flags
   - Environment validation and safety checks
   - Production environment blocking
   - Graceful error handling and cleanup
   - Comprehensive JSDoc comments

2. `/apps/payload/src/seed/seed-engine/index.test.ts` (427 lines)
   - 33 comprehensive unit tests
   - Tests for all CLI flags and combinations
   - Environment validation tests
   - Production blocking tests
   - Edge case handling tests
   - All tests passing

### Package.json Scripts Added
- `seed:run` - Run seeding with default options
- `seed:dry` - Dry-run validation only
- `seed:validate` - Dry-run with verbose logging

### Features Implemented
✅ Commander 14.0.0 integration
✅ Flags: --dry-run, --verbose, -c/--collections, --max-retries, --timeout
✅ Comprehensive help text with examples
✅ Environment variable validation (DATABASE_URI, PAYLOAD_SECRET)
✅ Production environment blocking (NODE_ENV check)
✅ Database connection health check
✅ Graceful cleanup on errors
✅ Exit codes: SUCCESS(0), VALIDATION_ERROR(1), INITIALIZATION_ERROR(2), SEEDING_ERROR(3)
✅ Colored terminal output via Logger
✅ Placeholder for orchestrator integration (Task #466)

### Test Results
- All 33 tests passing
- Coverage includes: argument parsing, environment validation, production blocking, flag combinations, edge cases
- Manual testing verified: help text, production blocking, environment validation

### Integration Points
- Uses Logger from utils/logger.ts for colored output
- Uses initializePayload/cleanupPayload from core/payload-initializer.ts
- Uses validateEnvironment for environment checks
- Uses types from types.ts (SeedOptions)
- Uses config from config.ts (ENV_VARS, DEFAULT_OPTIONS)
- Ready for orchestrator integration (Task #466)

### Notes
- Orchestrator placeholder implemented to allow parallel development
- Database health check uses Payload's find() API
- Exit codes properly set for different failure scenarios
- Production blocking has multiple safety checks
- Help text includes examples and environment variable documentation
