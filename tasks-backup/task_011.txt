# Task ID: 11
# Title: Develop Core Content Seeding System (Stage 2)
# Status: pending
# Dependencies: 8, 10
# Priority: high
# Description: Create scripts to populate main collection tables with non-relationship data from Single Source of Truth (SSOT) files.
# Details:
1. Create a Node.js module for parsing different SSOT file formats (YAML, TS objects)
2. Implement functionality to generate SQL INSERT statements for bulk data loading
3. Develop utilities for transforming Markdown to Lexical JSON for rich text fields
4. Create collection-specific seeding scripts that use either SQL or Payload's Local API based on complexity
5. Implement proper error handling and detailed logging
6. Add transaction support for SQL operations to ensure atomicity
7. Include progress reporting for long-running operations
8. Create a main orchestration script that runs all seeding scripts in the correct order

# Test Strategy:
Run the seeding scripts and verify that data is correctly inserted into the database. Check a sample of records to ensure all fields are populated correctly. Test with malformed SSOT files to ensure error handling works correctly. Verify that Markdown content is correctly transformed to Lexical JSON and renders properly in the Payload admin UI.
